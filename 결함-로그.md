# 결함 등록부 (Defect Register) — 버그 클래스 → 근본원인 → 재발방지

> Six Sigma 정신: **결함을 "측정"하고, 근본원인을 없애고, 재발을 "도구로" 막는다.**
> 새 버그를 칠 때마다 여기에 한 줄 추가하고, 가능하면 `tests/diagnose.js`에 체크를
> 넣는다 (정밀하게 — 오탐 내는 체크는 무시당해서 더 해롭다).
>
> **게이트:** `node tests/verify.js` = 인라인 파싱 + `tests/*.test.js` + `diagnose.js`.
> 🟢 아니면 "완료" 금지.

---

## 반복한 버그 클래스 (Pareto — 위에서부터 임팩트 순)

| # | 클래스 | 실제 증상 | 근본원인 (5-why) | 재발방지 (poka-yoke) |
|---|---|---|---|---|
| 1 | **가짜잔고** (A1–A8) | 손실캐시백 머니프린팅, 클라 ledger insert, 듀얼정산 | 클라가 서버 재검증 없이 돈을 적을 수 있었음 | RLS 잠금(ledger/accounts/holdings) + 모든 이동 서버 RPC + `diagnose` A2/A6 체크 |
| 2 | **이중차감** | 스포츠 $60(실제 $80) | baseline(`__sbLastPushed`) drift — "이 변수는 한 곳만 바뀌겠지" 가정 | 델타 경로 자체 제거(표시전용) + `sports-balance.test.js` |
| 3 | **제약 변경 사고** | PK 바꿔서 가입 막힘 (`on conflict` 깨짐) | 제약/키 바꾸며 **참조처를 안 셈** | CLAUDE.md "스키마/키/제약 바꿀 땐 트리거·RPC·on-conflict 먼저 grep" |
| 4 | **중복 생성** | 크론 정산/이자 2개씩 | 추가 전에 **기존 것을 안 봄** | 추가 전 `select * from cron.job` 확인 (체크리스트 B8 경고) |
| 5 | **표시≠진실** | 단일 푸시가 "Won"으로 표시; ALPXS 가격 wobble | 표시값을 **서버 플래그/추측**에서 파생 | 돈 기준 파생(`dispRes`=payout 비교) + `push-display.test.js` |
| 6 | **차익 의심** | ALPXS 스왑이 클라경로(낡은 `isSec` 분기) | "서버가 가격 못 매김" 낡은 가정 | swap_crypto RPC 라우팅 + `alpxs-swap.test.js` |
| 7 | **게이트 스킵** | 규칙은 MD에 있는데 버그가 새 나감 | 규칙만 있고 **강제 도구가 없었음** | `verify.js` + `diagnose.js`를 "완료" 정의에 못박음 |
| 8 | **클라 배당 신뢰** (D12) | `place_bet`이 클라 배당 그대로 저장→정산 지급 = 머니프린팅 | 돈코드인데 **클라 입력을 서버에서 재검증 안 함** | 제공배당 서버저장 + place_bet 대조거절 / 정산 서버라인 재계산 (출시 전 필수) |
| 9 | **돈을 localStorage에 캐시** | 한 브라우저 2계정→A 잔고가 B에 보임($90 누수); 과거 $60/$80 이중차감도 同 뿌리 | 돈을 **로컬에 "진실"로 저장**(서버 전용 위반). 1차 잔고만 메모리로 옮기고 **크로스앱 `serverBalances` 캐시 + login/signup 씨드**를 놓침 | **로컬에서 돈을 뺀다** — 매 로드 서버패치+메모리표시만(`window.__srvBal`/React state). 씨드·cacheOwner 제거. **`diagnose` LS9 정적체크로 money-truth `setItem` 영구차단**. CLAUDE.md #5 강화 |
| 10 | **MD 안 읽고 코딩** | #5를 안 보고 돈 캐시에 `cacheOwner` *추가*(정반대 방향) | **코딩 전 CLAUDE.md 미확인** | 손대기 전 MD 먼저 읽기(#1,#5)를 #5에 명문화 |
| 11 | **이메일당 중복계정** | 같은 이메일 재가입→player+계정 8개 | signup이 **기존 player 확인 없이** 매번 insert + **DB 유니크 제약 없음** | signup이 auth_id로 기존 재사용 + `players.auth_id` UNIQUE(불법상태 불가) — `dedupe_players.sql` |
| 12 | **어드민 세션 충돌** | 매니저 어드민 로그인이 손님 세션을 덮어씀(그 반대도) → 손님 1명만 보임/어드민 풀림 | 매니저가 손님앱과 **같은 Supabase storageKey**(기본키) 공유 — 한 브라우저=한 세션 | 매니저는 **격리된 storageKey**(`alpexa-admin-auth`)로 클라 생성(`window.ALPEXA_ADMIN_SESSION`) → 어드민 비번 하나로만 어드민 접근, 손님 세션과 불충돌 |
| 13 | **베팅 무차감(공짜베팅)** | 포지션은 서버에 있는데 stake 차감 0 (SP-895264 $25, 잔고 그대로 $100) | **베팅 생성 경로가 둘** — 서버 `place_bet`(차감) vs 클라 `insertBetRow`/`syncOwnBets`(무차감). 클라가 race를 이기면 place_bet 멱등가드가 차감을 **건너뜀** | 클라 positions.insert **삭제**(베팅은 place_bet 한 곳만). place_bet 실패=베팅 롤백. `diagnose` BET-client-position-insert + `bet-server-only.test.js`로 영구차단 |
| 14 | **세션 정체성 드리프트** | 어드민(zbnyme/edaf03b8)으로 로그인된 브라우저가 손님(zbnyme008/SP-895264) 계정을 표시 → place_bet "not your account" 거부, 베팅 안 들어감 | 표시 정체성(`alpexa.me`)과 실제 auth 세션이 **어긋남**(어드민 세션이 손님 앱에 묻음). admin RLS로 읽기는 되나 소유권 검사 실패 | `assertIdentity` — 로드 시 `auth.uid()`가 표시 계정 주인인지 검증, 불일치(확정 시만)면 **재로그인 강제**. `identity-guard.test.js` |
| 15 | **FX 마진 거부 무시** | $100 잔고에 0.01 BTC(마진 ~$120)가 열림 → 과레버리지·마진콜 팝업 | fx_open이 `ok:false`(마진 거부)를 줘도 클라가 **`finalize()`를 무조건 호출**해 포지션 표시(#13과 同 클래스 — 서버 거부 무시) | 마진 거부면 **롤백**(finalize 안 함)+토스트. no-spec/stale만 클라fill fallback 유지. `fx-open-reject.test.js` |
| 16 | **마진콜 모달 가둠** | 마진콜이 화면 덮는 블로킹 모달로 떠 → 닫아도 조건 그대로라 매 틱 재발 → **포지션을 못 닫게 가둠** | 자동 경고(마진콜/스탑아웃)를 사용자-동작 에러와 같은 **블로킹 모달**로 렌더 | 자동 경고는 `soft` 플래그 → **안 막는 상단 배너**로 분리 렌더. 모달은 사용자 동작 에러만 |
| 17 | **전역 시크릿 영향점 미확인** | B8로 `CRON_SECRET` 설정 → 가격피드 5개(crypto/fx/stock/games/odds)가 401 → stale → "price unavailable" 거래거부 | `CRON_SECRET`은 **전역 env** — 그걸 검사하는 *모든* 함수에 영향. 돈 함수 2개만 보고 바꿈(#0 "모든 영향점 grep" 위반) | `cron_secure.sql`을 **7개 전부 토큰** 거는 정본으로 + 주석에 "전역 = 모든 cron 함수 영향" 경고 명문화 |
| 18 | **Send 화면 데모 잔고** | 송금 "보내는 코인" 목록에 가짜 BTC $21,903·ETH $2,290(보유 0인데) | `SEND_ASSETS`에 **하드코딩 데모 잔고** — 실제 `balances`(crypto_holdings) 대신 표시 | 드롭다운·available을 **실제 `balances`로** + 보유한 것만 필터, `SEND_ASSETS` 금액 0, `no-demo-balance.test.js` |
| 19 | **저장형 XSS (어드민)** | 매니저가 손님 이름/이메일을 `innerHTML`로 렌더, `esc()`가 `"`만 막고 `<>` 통과 → 손님이 이름에 `<img onerror=…>` 넣으면 어드민 브라우저에서 실행 | escape 함수가 **불완전**(quote-only) | `esc()` 4개를 **전체 HTML escape**(`& < > " '`)로 + `escape-xss.test.js` |
| 20 | **이중소스 잔고 → 표시 drift** | 크립토 Account "Sport $0" vs 같은 화면 서버전환 드롭다운 "Sports $254" (한 잔고, 두 값). 포트폴리오 총액도 $254 적게 | **#5 메모리소스(`__srvBal`) 마이그레이션 절반** — 드롭다운만 `__srvBal`로 옮기고 ACCOUNTS·총액은 옛 `serverBalances` prop 그대로 → **두 소스 공존**. pull 하나(14383)는 `__srvBal`만 갱신 → 어긋남 ("같은 일 두 경로" #3) | Account `effBalances`/`baseTotal` + Withdraw `totalUsd`를 **`__srvBal` 단일소스**(`Object.assign({},serverBalances,__srvBal)`)로. RED→GREEN `crypto-balance-source.test.js`. `diagnose-ui`에 **UI-balance-dual-source** 스캔 → 나머지 6곳 추적. **마스터 감사에 "단일소스 추적" 차원 추가**(이 클래스를 못 보던 사각지대) |
| 21 | **놓은 베팅 배당 표시 drift** | 오픈 티켓 "Yankees ML -103" 이 -100↔-103 흔들림 (베팅한 가격은 고정인데) | 캐시아웃 재계산 루프가 **놓은 베팅 leg의 저장 배당을 라이브로 덮어씀**(`l.am=cur`) + 티켓이 잠긴 `am0` 대신 라이브 `l.am` 표시. (#20과 같은 클래스 — 저장 진실 대신 라이브 파생값 표시. 마스터 감사 차원6으로 잡음) | 캐시아웃 계산은 **로컬변수로 라이브 사용**(`l.am` 안 건드림) + 티켓 `fmtAm(am0)`. RED→GREEN `bet-odds-locked.test.js`. (odds 시뮬은 데모경기만 gated 확인됨 — real 안전) |
| 22 | **크립토 이중계산(2배 잔고)** (#20 재발) | 5M ALPXS 선물 후 어드민 총액이 **2배 $18.2M**, Gap +$9.1M **가짜 🚩**(출금 오차단 위험) | 어드민 총액/무결성에 `cryptoUsdVal`(crypto_holdings) 를 더했는데 **`accounts.balance(crypto)`가 이미 같은 홀딩의 서버 미러**(`sync_crypto_balance`=Σholdings) → **같은 돈 두 소스 합산.** "크립토 accounts.balance는 항상 0" **가정**(John=0만 보고)이 뿌리. **차원6(단일소스)를 내 변경에 안 돌림**(#7 게이트스킵) | 크립토 값은 **단일 소스 `cryptoOnce(cid,mirror)`** 로만 — live 홀딩 있으면 그걸, 없으면 미러, **둘 다 금지**. 모든 총액이 이걸 통과 + accounts.balance 합산서 크립토 **제외**. RED→GREEN `admin-integrity-crypto.test.js`(2배→1배). 마스터 감사 차원6에 **"크립토는 미러+live 이중소스 주의"** 명문화 |
| 23 | **뷰 크래시 → 탭 전환 불가** | 백오피스 History 탭 클릭해도 페이지 안 넘어감(입출금 기록 생긴 뒤부터) | `esc()`가 **전역 없이 함수 로컬 4곳만** 정의 → `viewHistory`/`viewApprovals`는 로컬 없이 esc 호출 → 행 렌더 순간 `esc is not defined` → `render()` 사망 → 콘텐츠 안 바뀜. **빈 DB면 일찍 반환해 잠복**, 행 생기자 발현. 렌더 스모크는 **기본 뷰만** 열고 탭 네비 안 함 → 못 잡음 | **전역 `esc` 헬퍼** 추가(usd 옆). RED→GREEN `admin-esc-scope.test.js`(esc()쓰는 뷰는 전역/로컬로 esc 해소돼야 — verify 게이트) |
| 24 | **이중지불 레이스(음수잔고)** | 동시 요청 2개가 같은 잔고 읽고 둘 다 통과 → 잔고보다 많이 베팅/출금 가능(음수잔고) | 돈 RPC가 **check-then-act** (`select balance ... if <amt reject ... insert ledger`)인데 **`for update` 행잠금 없음** + apply_ledger 트리거·accounts에 **음수방지 없음** → 스냅샷 잔고로 둘 다 통과 | **단일 지점 방어**: `apply_ledger` 트리거가 UPDATE 행잠금으로 현재잔고 재읽기 → 음수면 `raise`(txn 롤백=디빗 취소). `ledger_nonneg_guard.sql`(사용자 배포). RED→GREEN `concurrency-integrity-harness.test.js`(−$20→$40). **크립토 holdings 후속 완료**: `crypto_holdings_nonneg_guard.sql` — 테이블 `CHECK(qty>=0) NOT VALID`(라이브안전, UPDATE 행잠금으로 재읽기 → crypto_trade·crypto_send_internal·stake 전 경로 커버). 둘 다 사용자 Supabase Run 필요 |

## 근본원인 공통분모 (진짜 적)
1. **"한 곳만 바뀌겠지" 가정** → 항상 **모든 읽기·쓰기 지점 grep** 먼저 (#2, #3, #4).
2. **추측을 증거로 착각** → 실행/쿼리로 **눈으로 확인** (#5, #6, 그리고 $40 "차이"가 실은 정상이었던 건).
3. **강제 안 되는 규칙** → 도구(게이트)로 강제 (#7).

---

## `tests/diagnose.js` — 결함 클래스 자동 스캔 (현재 6종)

| 체크 | 심각도 | 막는 것 |
|---|---|---|
| `A2-client-ledger-insert` | CRITICAL | 클라가 `ledger`에 insert |
| `A6-client-balance-update` | CRITICAL | 클라가 `accounts.balance` 직접 update (ledger 우회) |
| `SEC-service-role-key` | CRITICAL | service_role 키/JWT가 브라우저로 노출 |
| `ADDR-demo-crypto-address` | HIGH | 데모/하드코딩 코인주소 주입 |
| `EVAL-in-app` | HIGH | 배포 앱에 `eval(` |
| `LS9-money-in-localstorage` | HIGH | 돈/잔고/보유/포지션을 `localStorage`에 진실로 `setItem` (#5 계정간 누수원) |
| `B8-failopen-cron` | HIGH | 돈 엣지함수 fail-open 크론 시크릿 |
| `DEMO-login-creds` | HIGH | 로그인 폼에 데모계좌 하드코딩 (fillDemo: `pwInput.value='1234'`) — 실유저가 로그인페이지서 남의 데모계좌/로컬데이터 봄 |
| `DEMO-hardcoded-balance` | HIGH | 하드코딩 `$N,NNN.NN` 잔고/가격 (가짜 데모계좌 잔고) — 돈은 서버에서만 |
| `DEMO-fake-email` | HIGH | 데모/테스트 이메일(alpexa-demo.com·@alpexa.app) 하드코딩 |

### #33 정산 고아 — settle이 "오늘 스코어보드"만 읽어 늦은 경기 영영 미정산 (2026-06-30)
- **증상:** 일일 감사 C1이 묵은 미정산 2건(축구 MLS, SP-726759) 잡음 — 경기는 끝났는데 36h+ open. **감사(마스터)가 버그를 잡아낸 첫 실사례.**
- **근본원인:** `sports-settle`의 ESPN 호출이 `/scoreboard`(파라미터 없음=**오늘 경기만**). 정산이 그 날 못 잡으면(크론 다운/늦은 종료) 다음날 경기가 피드에서 빠짐 → 결과 영영 못 받음 → 오픈 베팅 **고아**. 모든 종목 해당(축구·UFC 등 settle 적게 맞는 종목서 더 잘 보임).
- **수정:** scoreboard 호출에 **`?dates=<최근 6일>` catch-up 윈도우** → 늦게라도 정산(자가치유). 리그·UFC fetch 둘 다.
- **재발방지:** `tests/settle-catchup.test.js` — settle이 `scoreboard?dates=` 없이 "오늘만" 가져오면 **게이트 빨강**. ("버그는 마스터가 잡아야지" — 운영 C1 + 정적 게이트 둘 다.) 후속: 6일 넘게 지난 고아는 event-id 직접조회 or N일 후 자동 void(환불).

### #32 가짜 포트폴리오 차트 히스토리 (조작된 오프닝 + 랜덤워크) (2026-06-30)
- **증상:** 크립토 앱 리프레쉬→계정 진입 시 보이던 "옛날/이상한 차트". Account(총액) 차트가 **실데이터 시도조차 없이 100% 합성**: `open=baseTotal×RANGE_BASIS`(ALL=0.10 → "오늘의 10%에서 시작" = **가짜 10배 상승**) + `makeChartSeries` 랜덤워크 + `useLiveTick` 시뮬 틱. Dashboard도 `RANGE_OPEN_MULT`로 같은 조작 잔재.
- **근본원인:** "차트엔 데이터가 있어야 하니 합성한다"는 옛 사고 → **돈 화면에 없는 과거를 지어냄**(#5 가짜움직임). 멀티계좌 총액의 진짜 가치 히스토리는 저장 안 함.
- **수정:** Account·Dashboard 차트를 **진실**로 — 실시세(보유×시세, 1D/1W) 또는 **현재값 평평선**. 합성·조작 오프닝·시뮬틱 제거. 죽은 `RANGE_OPEN_MULT` 정의째 삭제.
- **재발방지:** `diagnose.js` **`CHART-fabricated-portfolio-history`** — `RANGE_BASIS`·standalone `RANGE_OPEN_MULT` 금지(돈 차트 오프닝 조작 시그니처). 코인 시세차트 `BUY_RANGE_OPEN_MULT`는 실fetch 후 로딩 placeholder라 정규식서 제외. 시각-감사 VIS-fake-motion에 연결. **정적이 못 잡던 클래스를 이제 정적으로 차단.**

### #31 "Reset all demo data" footgun — 전체 localStorage 일괄삭제로 어드민 세션 날림 (2026-06-30)
- **증상:** 백오피스 Config의 "🗑 Reset all demo data" 버튼을 누르면 **백오피스가 로그아웃** → RLS가 데이터를 다 가려 **빈 화면**. ("백오피스에 아무것도 안 떠"의 진범으로 의심.) 메시지는 "Apps start fresh — Sports $50,000"라 **데이터를 지운 줄 오해**(공포).
- **근본원인:** `Object.keys(localStorage).forEach(...removeItem)`로 **`alpexa`로 시작하는 모든 키**를 쓸어담음 → 어드민 세션 키 `alpexa-admin-auth`(+세션토큰)까지 삭제. 돈이 localStorage에 있던 **옛 시절 잔재** 버튼(지금 돈은 서버전용이라 실데이터는 안 지워짐 — 즉 쓸모는 0, 위험만 남음).
- **수정:** 버튼·핸들러 **제거**. (서버 데이터는 애초에 안 건드렸으니 **실제 손실 0** — 어드민 재로그인이면 복구.)
- **재발방지:** `diagnose` **`LS-wipe-all-keys`** — DEPLOYED에서 `Object.keys(localStorage)` 순회 자체를 차단(특정 키만 named로 지워라). 전수 grep 결과 이 버튼이 **유일한 사용처**라 정밀.

### #30 로그인이 자동 전진(stay-signed-in) → 닫은 브라우저서 유령 로그인 (2026-06-29)
- **증상:** 브라우저를 다 닫았다가(또는 새 탭에서) 다시 열어 alpexa-sports.com에 들어가면 **로그인 폼이 아니라 앱**이 뜨고, 잔고가 ■■■·"No upcoming games"인 유령 로그인 상태. 로그인 페이지를 봐야 하는데 자동 로그인됨.
- **근본원인(5-why):** 앱 진입 게이트와 login.html "stay-signed-in"이 **영구 localStorage 태그 `alpexa.me`** 를 "로그인됨" 증표로 신뢰. 그런데 진짜 auth **세션은 sessionStorage**(탭별, **브라우저 닫으면 소멸**, CLAUDE.md #5). → 브라우저 닫음=세션 소멸인데 태그는 남음 → login.html이 태그만 보고 앱으로 `location.replace` → 세션 없으니 RLS가 전부 가림 = 유령 ■■■. 게다가 앱의 `?expired=1` 리다이렉트가 login으로 가면 login이 **또** 자동 전진 → `sessionStorage.sessChecked` 플래그로 두 번째엔 세션검사 건너뜀 → **앱에 눌러앉음(루프 종착)**. (idle logout #26은 `signOut`만 하고 `alpexa.me`는 **안 지워** 태그가 살아남은 것도 한 원인.)
- **수정:** (1) **login.html은 항상 로그인 폼** — load-time 자동 전진 블록 삭제. 앱 진입은 **로그인 제출 핸들러(실제 인증 후)에서만**. (2) idle `logout()`이 수동 sign-out과 동일하게 **`alpexa.me`/`userName`/`userEmail`도 removeItem**(태그 잔존 제거). **localStorage에 새로 저장한 것 없음 — 읽기/의존을 제거하고 지우기만 추가**(#5 위반 아님; 돈은 원래 서버전용 유지).
- **재발방지:** `tests/login-no-autoforward.test.js` — login.html의 **load-time(\<body\> 이전) 스크립트가 앱페이지로 redirect하거나 `alpexa.me`를 읽어 전진하면 FAIL**, 단 제출 후 정상 리다이렉트는 보존 확인. (15분 idle 자동로그아웃은 `idle-logout.test.js`로 별도 보증.) verify 🟢.

### #29 가짜 잔고/데모데이터 대청소 + 스캐너 (2026-06-29)
- **증상:** 로그인 전/폴백 화면에 가짜 계좌·잔고가 보임 ($1,187,077.40 랜딩, FX 출금 "Available $11,248.10", 계정스위처 $5,000/$500, "Christian Kim/Demo User", `deposits@alpexa-demo.com` 데모 Zelle).
- **청소:** index.html·site/(wallet/settings/dashboard/my-bets) 가짜잔고·이름·데모이메일 제거. 진짜 FX앱(trading.html): 출금폼 defaultValue 4개 제거·"Available"를 진짜 `getBalances().live`로·"Demo User" 폴백 제거·계정스위처 잔고 0. crypto-live 가격알림 정적→`cryptoPriceOf` 동적. (진짜 입금 은행정보=crypto-live 실값은 안 건드림.)
- **재발방지(마스터감사):** `diagnose.js`에 **`DEMO-hardcoded-balance`**(`$\d+(,\d{3})+\.\d{2}`) + **`DEMO-fake-email`** 추가. **모든 사용자-노출 파일**(DEPLOYED+site/) 스캔. 허용: crypto-live 마이그레이션 1줄·manager 주석. verify 🟢(active 0).
| `CRYPTO-client-holdings-write` | CRITICAL | 클라가 `crypto_holdings`를 직접 insert/update — 코인 qty는 RPC만 주인(#25) |

### #28 크립토 Send가 모바일서 USDT만 — 가격 안 뜨면 코인 사라짐 (2026-06-29)
- **증상:** 모바일 Send는 USDT만, 데스크탑은 다른 코인도 보임.
- **근본:** Send 자산목록이 **USD값(`balances[id]>0.005`)으로 필터**. 비USDT USD값=수량×가격인데 `cryptoPriceOf`는 가격 안 떴을 때 **1로 폴백** → 모바일(느린 로드)에선 비USDT가 작은/0 USD값 → 필터 탈락. USDT만 살아남음.
- **고침:** ① 목록을 **보유 수량(`holdingsSyncRef.serverQty`)** 기준으로(가격 무관, 보유코인 다 표시) — Send에 `holdingsQty` prop 전달. ② **가격 안 뜬 코인은 전송 차단**(`cryptoHasRealPrice`) — 안 그러면 USD→qty가 `usd/1`이 돼 $100을 100코인으로 오송금(돈 버그).
- **증명:** `tests/send-asset-list.test.js` RED(USD필터가 보유코인 누락)→GREEN(수량기준 표시 + 무가격 전송차단). verify 🟢.

### #27 받기/입금 QR이 가짜 — 아무것도 인코딩 안 함 (2026-06-29)
- **증상:** P2P 전송 테스트 중 "QR을 못 읽음." 카메라·jsQR은 정상.
- **근본:** `QrPattern` 컴포넌트가 **진짜 QR이 아니라 장식**이었음 — `data`를 해시해 **랜덤 흑백 셀**을 찍고 모서리 마커 3개만 그림(실제 주소 인코딩 0). 받기 화면·외부 입금주소 QR(3688·9100)이 전부 가짜. (인트로 "My pay QR" 3075만 qrserver로 진짜였음 → 그래서 일부만 되는 것처럼 보임.)
- **클래스:** "보이는데 안 함"(차원3) — **인코딩/바코드/QR은 *디코드해서* 진짜인지 확인**해야 함. 눈으로 QR처럼 보인다고 통과시키면 안 됨.
- **고침:** `QrPattern` → qrserver `create-qr-code` 이미지(인트로와 동일, 표준 스캔가능 QR). + 스캐너 견고화: 프레임 ≤640 축소(속도), 후면 없으면 아무 카메라, **사진 폴백**(웹뷰/카톡서 카메라 막혀도 OS카메라로 찍어 디코드), 받기 QR 크게.
- **증명:** 샌드박스는 CDN/qrserver 차단으로 라운드트립 자동검증 불가 → **실기기 확인 필요**. 근본(가짜 패턴)은 코드로 확정.

### #26 모바일 자동 로그아웃 먹통 — 복귀가 유휴 타이머를 리셋 (2026-06-29)
- **증상:** 데스크탑은 자동로그아웃 됐는데 **모바일은 15분 방치해도 안 됨.** 세션이 죽어 잔고 `•••`·게임 안 뜨는 "멈춘" 화면으로 박힘.
- **근본:** ① 모바일은 백그라운드면 `setInterval` **정지** → 유휴 카운트 안 됨. ② `visibilitychange`가 복귀를 **활동으로 처리(`onActivity` 리셋)** → 자리 비운 시간이 사라져 영영 로그아웃 안 됨. 데스크탑은 화면이 안 꺼져 타이머가 계속 돌아 우연히 통과.
- **클래스:** **모바일 생명주기/시간 기반** — 정적 스캐너·node 테스트가 못 잡음(실제 모바일 복귀를 돌려야 보임). 마스터 감사 신규 차원 7.
- **고침:** 복귀(visibilitychange/focus/pageshow)는 리셋 안 함 → **실제 경과시간(`Date.now()-last`)**을 재서 한도 넘었으면 즉시 로그아웃, 아니면 경고. (틱 누적 아님.)
- **증명:** 헤드리스로 복귀 이벤트 반복 발사해도 로그아웃 정상 발동. `tests/idle-logout.test.js` RED(복귀=리셋→안 터짐)→GREEN. 마스터-감사 차원7 + §2 "모바일 복귀 시나리오" 추가.

### #25 크립토 내부전송(P2P 페이)이 가짜 — 서버 이동 없음 (2026-06-29) 🚨돈
- **증상:** "다른 Alpexa 유저에게 보내기"가 실제로 코인을 안 보냄. 보낸이 차감·받는이 크레딧 **둘 다 로컬만** → 새로고침하면 복구. `payments` 테이블엔 돈 트리거 0. 받는이는 차감 없는 +크레딧이라 **무에서 돈 생성** 가능.
- **근본:** `sendPayment`이 `payments`에 행만 insert(알림용). 실제 `crypto_holdings` 이동이 없음. (이메일 전송용 `acct_for_email` RPC도 호출만 하고 **정의 자체가 없었음** → 이메일 전송 깨짐.)
- **불변식:** 내부전송은 **코인 qty 보존** — 보낸이 `crypto_holdings[asset].qty −Q`, 받는이 `+Q`, 합계 불변. 진실은 서버. 단위=qty(USDT는 USD 1:1, 나머지 코인단위).
- **고침:** 서버 RPC `crypto_send_internal(p_ref,p_to_acct,p_asset,p_qty)` (원자·멱등 `crypto_transfers`·소유권·잔액검증) + 누락 `acct_for_email` 생성 → `crypto_send_internal.sql`(**사용자 배포**). 클라: Send가 RPC 호출(USD→qty 환산), **받는이 로컬 크레딧 제거**(서버 holdings adopt로 반영), `payments`는 알림 전용.
- **증명:** `tests/crypto-internal-transfer.test.js` RED(로컬크레딧=돈생성)→GREEN(qty보존·멱등·잔액부족·새로고침유지). 포카요케 `CRYPTO-client-holdings-write`. verify 🟢.

### #24 보너스 이체 우회 — 출금 불가 보너스가 이체로 새나감 (2026-06-29) 🚨돈
- **증상:** 이체 페이지에서 보너스까지 같이 이체됨 → 다른 서버(크립토)로 옮긴 뒤 출금 = 출금불가 보너스 우회.
- **근본:** **출금엔 `guard_withdraw_request`(=`balance−bonus`) 가드가 있는데 이체엔 없었음.** `app_transfer` RPC가 `v_bal < p_amount`(전체잔고)만 검사하고 `accounts.bonus`를 안 봄(`_deployed_rpcs.sql:70`). 클라도 전체잔고(`balOf`/`effBal`)로 Max를 잡아 보너스가 칸에 포함됨.
- **불변식:** `이체가능액(sports/fx) == 출금가능액 == max(0, balance − bonus)`. 보너스는 출처 계정에 묶여 real money(입금+당첨)만 나감.
- **고침:** ① 서버 `app_transfer`에 sports/fx 보너스 가드 추가 → `transfer_bonus_guard.sql`(**사용자 배포**) + `_deployed_rpcs.sql` 동기화. ② 클라 sports(`transferableOf`)·crypto(`withdrawableBy`) Max/avail/cap을 transferable 기준으로. FX는 서버가드로 보호(클라에 보너스-채우는 Max 없음).
- **증명:** `tests/transfer-bonus.test.js` RED(옛 전체잔고 cap이 보너스 내보냄)→GREEN(`balance−bonus` cap이 막음 + real money는 통과). verify 🟢.
- **재발방지:** 행위테스트 `transfer-bonus.test.js` + 마스터-감사 차원6에 "이체=출금 동일 가드" 추가.

### #23 로그인 페이지 데모/로컬데이터 누수 (2026-06-29)
- **증상:** 로그인 페이지에 다른 데모계좌(`44219982330`)·이전 계좌번호가 떠 보임.
- **근본 3개:** ① `login.html` 죽은 `fillDemo()`가 데모creds(`44219982330`/`1234`) 하드코딩. ② 저장 cred를 로그인 칸에 **자동채움**(`loginInput.value=cred.login`) → 이전/데모 계좌 노출. ③ `alpexa-sync.js me()`가 로그인 없을 때 **가짜 Guest 계좌(P-/SP-/CR-/FX-)를 `localStorage.alpexa.me`에 저장**(#5 가짜 시드).
- **고침:** ① 삭제. ② 자동채움 제거(은행식 빈칸 유지, Face ID는 내부적으로만 cred 사용) + 기존 데모 cred/bio/me **1회 퍼지**. ③ `me()` 가짜 게스트 **비저장**(인메모리 placeholder만).
- **증명:** RED(칸에 44219982330 뜸)→GREEN(빈칸+데모 퍼지) 헤드리스, 실계정은 cred 유지 확인. 스캐너 `DEMO-login-creds` 추가(옛 fillDemo 잡고 `value=''` 오탐 안 함).

**검토됨 예외(허용):** manager-mobile 크립토 `accounts.balance` 직접쓰기 — 백오피스는 admin(RLS 허용), 크립토 진짜 돈은 `crypto_holdings`(commands 경로), `accounts.balance`는 표시 캐시.

### 새 버그를 칠 때 (Control 루프)
1. 위 표에 한 줄 추가 (클래스·근본원인).
2. **행위 버그** → `tests/*.test.js`로 RED→GREEN.
3. **패턴 버그**(정밀 regex로 표현 가능) → `diagnose.js`에 체크 추가.
4. `node tests/verify.js` 🟢 확인 후에만 "완료".

---
## 마스터 감사 (2026-07-04, 4에이전트 병렬)
- **P0 백오피스 stored XSS** (manager 780·989): 고객이 `update_my_name`로 이름에 `<img onerror>` 주입 → 어드민 세션 탈취. 근본원인: `esc()`가 같은 함수에 있는데 표시부만 누락. 재발방지: 6곳 esc() + 그 클래스는 근본적으로 "어드민 DOM에 넣는 모든 고객유래 문자열 esc()" 습관 (regex 오탐 우려로 diagnose엔 안 넣음).
- **가짜 신원 클래스** (trading 2FA/리포트, crypto 프로필, site/settings): 개발자 이메일 zbnyme@·가짜은행 KEB Hana·가짜폰 +1 415 555·"Connected May 12" 를 사용자 실데이터처럼 표시. 근본원인: 폴백 리터럴. 재발방지: `diagnose.js DEMO-hardcoded-identity` 추가 (내 1차 수정 후에도 3곳 더 잡음 — poka-yoke 유효 입증). 실데이터(alpexa.me) or "Not set"만.
- **FX 출금/이체 가드** (trading 2695·2710): withdrawable 대신 원본현금·하드코딩 12847. 수정: `fxAvail()` = min(cash, free-margin) 로 통일 (#24).
- **시뮬 시세 라벨** (trading 주식/지수): `!s.real` 심볼에 "SIMULATED" 배지.
- 돈정확성 P0 0 · 모바일생명주기(#26)·QR(#27) 검증 통과.

## 에이전트 커미션 서버 무검증 (2026-07-05, 마스터감사 🟥 #3 마감)
- **클래스: 서버 재검증 없는 돈 요청.** 커미션 계산·출금가능액 검사가 브라우저에만 존재(agent.html + manager-mobile 중복) — 서버는 `agent_payouts`에 뭐가 오든 저장. 위조 금액·자가승인(status:'approved')·가짜 adjust 크레딧 전부 가능(RLS 미잠금 상태 기준). 근본원인: 표시 로직을 진실로 사용, 진실(서버) 부재.
- **수정:** `agent_payout_guard.sql` — ①`agent_commission_for()` 서버가 settlements×요율×share로 재계산(공식 단일 진실) ②`guard_agent_payout()` 트리거: 포털 insert는 pending 강제+adjust 금지+min $50+불변식(Σ(approved+pending) ≤ 커미션+adjusts), **승인 순간 재검증**, 비어드민 UPDATE/DELETE 차단. 행위증명: `tests/agent-payout-guard.test.js` (RED: 가드 없는 서버가 위조 저장 → GREEN: 차단).
- **잔여(백로그):** agents PIN anon 열람(클라 PIN 비교)·agent.html의 전 고객 settlements 열람·클라 settlements insert 경로(`trg_settlement_balance` kind 필터 라이브 확인 필요) → RLS 일괄 잠금 패스(리마인드-리스트)와 함께.

## 🔴 CRITICAL — settlements FX 손익 돈-프린팅 (2026-07-05, 라이브 RLS/트리거 확인)
- **클래스: 신뢰 못 할 클라 insert가 잔고 트리거를 발화.** `apply_settlement_to_balance()`(trg_settlement_balance)가 settlements insert 시 server='fx'면 소유권/kind/멱등 검사 없이 `accounts.balance += pnl`. RLS `settlements_ins`가 {authenticated} 전체 허용 + 크립토/스포츠 앱이 클라에서 settlements 직접 insert → 로그인한 누구나 `{server:'fx',acct_no:<아무거나>,pnl:1e6}` POST로 잔고 발권. 근본원인: 돈 트리거의 입력을 클라에 개방(불법상태 표현 가능).
- **수정:** `rls_money_lockdown.sql` — settlements INSERT with_check `is_admin() OR server<>'fx'`(fx는 fx_close RPC=정의자권한/RLS우회만) + UPDATE/DELETE admin-only + agents/agent_links/agent_payouts `_all_demo` ALL-public 정책을 write=admin(agents 요율 변조가 페이아웃 가드 무력화하던 것 차단)로 교체. 불변식: FX 손익은 fx_close RPC만 기록, 클라 저작 fx settlement는 표현 불가능. 증명: `tests/settlements-rls.test.js`(RED 발권 → GREEN 거부, 정상경로 유지).
- **배포 후 필수 스모크:** FX 소액 open→close 해서 손익이 여전히 반영되는지(=RPC의 RLS우회 확인). 안 되면 fx_close 소유자 BYPASSRLS 점검.
- **잔여(백로그, 돈 아님):** settlements/agents/agent_links/agent_payouts SELECT 공개 + agents PIN 브라우저 노출 → 에이전트 포털 진짜 인증 도입 시 일괄 잠금(리마인드-리스트 RLS 패스).

## FX앱 플로팅 P&L 가짜 스프레드(항상 슬립페이지) (2026-07-05)
- **클래스: 화면 손익이 서버 실현손익과 다른 기준.** 플로팅 청산가를 클라가 `BUY→mid, SELL→mid+m.spread`(시뮬 스프레드, 전 클래스)로 계산. 그러나 서버 `fx_close.sql`은 **비FX(크립토/주식/지수)를 MID로 청산**(v_half는 cls='FX'만), FX는 mid∓half(spr_pts+markup_pts). → 크립토 매도가 열자마자 시뮬 스프레드만큼 **영구 가짜 손실**(실제 청산 땐 없음). 근본원인: 시뮬 스프레드(표시용)와 서버 스프레드(돈)라는 두 진실.
- **수정:** `trading.html` `fxClosePx(m,side)` = fx_close.sql v_close 정확 복제(실제 피드 spr_pts+markup_pts·pip 동일, 비FX는 half=0). 플로팅(3627)·로컬청산표시(3837) 두 곳이 같은 함수 사용(진실 한 곳). 증명: `tests/fx-floating-spread.test.js` (RED: 크립토 매도 가짜 −스프레드 → GREEN: client==server v_close, 크립토 mid 양방, FX mid∓half, JPY pip 일치, 신규 크립토 플로팅 0).
- **주의(락스텝):** pip/half 공식은 fx_close.sql·fx-prices Edge와 동일 유지. 서버가 fx_open을 bid/ask로 바꾸면(스프레드 온오픈) 이 헬퍼도 같이 갱신.

## 피드 없는(시뮬) 심볼을 거래 목록에서 제외 (2026-07-05)
- **클래스: 거래 불가한데 거래 가능처럼 보임(정직성).** 서버 피드 없는 심볼은 `fx_open`이 거부해 못 여는데, 워치리스트·피커·차트드롭다운·알림목록에 그대로 노출되고 빨간 SIMULATED 배지만 달렸음.
- **수정:** `trading.html` `alpexaHasFeed()/alpexaFeedReady()/alpexaTradable()` 헬퍼 하나로 브라우즈/선택 목록을 서버-피드 심볼만 노출(워치리스트 rows·카테고리카운트·빈 클래스탭 숨김·피커·차트 SymPicker·알림 select). **열린 포지션/히스토리는 제외 안 함**(피드 끊겨도 보유분 표시). 콜드스타트(피드 로드 전)엔 전량 통과로 깜빡임 방지. 증명: `tests/feed-filter.test.js` (RED: SPX 노출 → GREEN: 피드만, 빈 INDEX탭 사라짐, 콜드스타트 통과, mid=0 제외).

## 전 상품 스프레드(MT5 표준) + 플로팅 실제-mid 마크 (2026-07-05)
- **클래스: 업계 관례 이탈 + 시뮬가로 손익 계산.** (1) 크립토/주식/지수가 무스프레드(서버 fx_close가 FX만 부과) — 규제 브로커 관례 이탈. (2) 플로팅이 `s.last`(시뮬, 피드 사이 드리프트)로 계산돼 실제 체결가와 어긋나 **가짜 플러스**가 떴다(`applyReal`이 s.bid=실제mid만 갱신, s.last는 시뮬 유지).
- **수정:** (a) `trading.html` `fxClosePx`가 **실제 피드 mid(m.bid)** 로 마크(시뮬 드리프트 제거). (b) 전 상품 스프레드: 비FX는 bps(CRYPTO 10·STOCK 8·INDEX 6, one-way=bps/2), 서버 `fx_close.sql` v_half else브랜치 ↔ 클라 `ALPEXA_SPREAD_BPS`/`fxHalfSpread` **락스텝**. 결과: 전 상품 열자마자 −half(스프레드 손실), 플로팅==서버 실현. 증명: `tests/fx-floating-spread.test.js`(client==server 전 클래스, fresh<0, 실제mid 마크, 크립토 0.05% one-way).
- **거버넌스:** 마스터 감사 **차원 9(금융 업계 표준/MT5 구조 준수)** 신설 + CLAUDE.md `🏦 금융 업계 표준` 섹션 — "규제 브로커가 이렇게 하나?" 기준, 임의 발명 금지. (사용자 지시 2026-07-05.)

## 크립토 하이브리드 스프레드 (floor + 실제 거래소 스프레드 passthrough) (2026-07-05)
- **개선(MT5 구조):** 크립토 스프레드를 고정 bps에서 **하이브리드**로 — `greatest(house floor, 실제 거래소 스프레드 bps)`. 평시엔 floor(크립토 10·주식 8·지수 6bps), 변동성/저유동 페어에서 거래소 스프레드가 floor 넘으면 자동 확대. FX는 그대로 실시간 pip 스프레드.
- **구현:** `crypto-prices` Edge를 Binance `bookTicker`로 전환(실제 bid/ask) → mid=(bid+ask)/2, `spr_pts`=실제 스프레드 **bps**. 서버 `fx_close.sql` 비FX 브랜치 `greatest(floor, spr_pts)` ↔ 클라 `fxHalfSpread` `Math.max(floorBps, feedBps)` **락스텝**. 주식(Finnhub `/quote`는 bid/ask 없음)·지수는 spr_pts=0 → floor 유지. 증명: `tests/fx-floating-spread.test.js`(floor 지배 + 30bps passthrough + client==server).
- **주의:** 크립토 `spr_pts`는 **bps** 단위(FX는 pips) — 같은 컬럼 클래스별 다른 단위, fx_close가 cls로 분기. 크립토 raw 스프레드는 평시 ≈0이라 사실상 floor(리테일 CFD 표준), 급변 시에만 확대.

## 축구 1X2 베팅 place_bet에서 전량 거부 (2026-07-07)
- **클래스: 클라 market 라벨 ↔ 서버 배열 키 매핑 누락(신규 시장 추가 시 서버 재가격 경로 미갱신).** 축구 1X2 도입으로 클라가 `data-market="1X2"`를 보내는데(sel=`<팀> ML`/`Draw`), `place_bet_server_odds.sql`의 라벨→키 case가 `moneyline/spread/total`만 매핑 → `1x2`는 `v_key=null` → **모든 축구 베팅이 "market not offered: 1X2"로 거부**(선택은 되는데 Place Bet에서 막힘). 근본원인: live_games에 `threeWay` 배열은 있었으나 place_bet 재가격 매핑에 새 시장을 안 넣음(렌더·정산은 넣었는데 배팅 관문만 빠짐).
- **수정:** `place_bet_server_odds.sql` case에 `when '1x2' then 'threeWay'` 한 줄(JSON 키 대소문자 구분 → 정확히 `threeWay`). 축구 leg는 이제 서버 threeWay 라인에서 재가격·서버 am 스탬프 → 정산(settle 1X2 분기)과 일관. 증명: `tests/place-bet-odds.test.js` 축구 섹션(RED: team/Draw pick 거부 → GREEN: 서버 threeWay로 재가격, 미제공 sel은 여전히 fail-safe 거부).
- **불변식:** place_bet은 모든 leg를 live_games 서버 라인에서 재가격한다 — 클라가 보내는 **모든** market 라벨은 서버 배열 키(ml/spread/total/**threeWay**)로 매핑돼야 하고, 안 되면 새 시장은 통째로 막힌다. 새 시장 추가 시 렌더+정산+**place_bet 매핑** 3곳을 같이 갱신.
- **배포:** 사용자가 `place_bet_server_odds.sql`을 SQL Editor에서 실행(RPC 교체). Claude은 SQL만 제공.

## 실라인 없는 경기에 가짜 -140/120 라인으로 베팅 가능 (2026-07-08)
- **클래스: 프로바이더 라인 부재 시의 플레이스홀더를 진짜처럼 노출(하우스가 지어낸 라인을 honor).** The Odds API가 일부 MLB 경기(예: Phillies@Reds, Royals@Mets) 라인을 안 올리면 `sports-games`가 `mkCore` 기본값 **-140/+120**으로 채우고, 클라는 그대로 베팅 버튼을 그리고 `place_bet`은 live_games의 그 값으로 재가격 → **하우스가 오프마켓 가짜 라인을 그대로 honor**(규제 브로커 원칙 "피드 없으면 제외" 위반, 돈 리스크). feed-check가 `ml==[-140,120]` 휴리스틱으로 "가짜"를 잡아냄(임박 5경기).
- **수정:** 소스에 **명시적 진실 플래그** `oddsReal`을 심음(숫자 스니핑 금지 — 진짜 라인이 우연히 -140/120일 수 있음). `sports-games`: mkCore는 ESPN ml 있을 때만 real=true, overlay 적용 시 `g.oddsReal=true`, 아니면 false. 클라: `oddsReal(g)===false`면 셀을 `sus` 잠금(🔒 "Odds unavailable")·data-am 미부여로 베팅 불가. 서버 `place_bet`: `coalesce((game->>'oddsReal')::boolean,true)=false`면 leg 거절(돈 관문). 증명: `place-bet-odds.test.js`(RED $34.29 honor → GREEN 거절, 실라인 로드 시 정상), `sports-render.test.js`(no-line 경기 🔒·data-am 없음).
- **불변식:** 실라인(overlay/ESPN)이 적용 안 된 경기는 베팅 불가 — 진실은 sports-games가 심는 `oddsReal` 플래그 하나, 클라 잠금과 서버 place_bet 거절이 같은 플래그를 본다(클라는 UX, place_bet이 돈 관문). 플래그 없음=real(배포 전 하위호환).
- **배포:** 사용자가 (1) `sports-games` Edge 재배포, (2) `place_bet_server_odds.sql` 재실행. 클라(sports-live.html)·테스트는 Claude 커밋. 세 개 다 배포돼야 완전 보호(클라만은 우회 가능, 서버가 진짜 관문).
- **후속(같은 날):** ① 첫 수정의 클라 잠금이 **죽어 있었다** — 서버피드→GAMES 변환부 `lgToGame`가 `oddsReal`을 안 넘겨줘서 클라 게임 객체에 플래그가 안 붙음(sports-render 테스트는 객체에 직접 넣어 통과 → "테스트 통과·프로덕션 실패" 함정). 변환부에서 `oddsReal` 전달로 수정. ② 더 근본: 가짜 -140/120을 **만들었다가 잠그는** 대신 `sports-games`가 **애초에 빈 배열**을 내보내게(발원 차단). 단 클라 `lgToGame`가 마켓 불완전 경기를 드롭(`return null`)하던 게 placeholder 존재 이유였어서, `oddsReal===false` 경기는 드롭 말고 잠근 채 살리도록 필터 완화 + 셀 잠금을 빈 배열에도 걸게 순서 조정. ③ **마스터 감사에 이 체크가 없었다**(C7은 sports_odds 테이블 신선도만). → `스포츠-마스터-감사.md`에 **C10**(라이브 live_games 스캔: oddsReal!=false인데 ml 없음/옛 sentinel = 베팅가능 가짜) 추가, `sports-feed-check.js`에 🚨샘 열 추가(데일리 CI가 매일 검증).

## 축구 실배당이 있는데 잠김 — nick() 팀매칭이 클럽 접미사에서 깨짐 (2026-07-08)
- **클래스: 두 독립 피드(ESPN 일정 · The Odds API 배당)를 "팀명 마지막 단어(nick)"로 결합 → 축구 클럽명 접미사(FC/SC/City/Sounders)에서 실패.** ESPN "Vancouver" vs Odds "Vancouver Whitecaps FC" → 마지막단어 `vancouver`≠`fc` → 실배당이 있는데도 오버레이가 안 붙어 그 경기가 oddsReal:false로 잠김(회수 가능한 매출 손실). 라이브 대조: 잠긴 66경기 중 4(전부 축구)가 이 케이스, 62는 진짜 프로바이더에 없음(잠금이 정답).
- **수정:** `sports-games`의 팀 매칭을 nick(마지막단어)→**teamMatch(토큰 부분집합)**: 클럽 접미사 제거·유의미 토큰(len>2)·짧은이름 토큰이 긴이름에 전부 포함되면 매칭. 2군데 다 교체 — 이벤트 찾기(`overlayRealOdds`, **유일매칭+커밋시각 6h 이내** 아니면 잠금 유지) + 결과→팀 매칭(`oddsToCore`의 bestOutcome h2h/spreads). US 스포츠는 그대로("Reds"⊆"Cincinnati Reds"), 크로스매칭 방지("Red Sox"⊄"Chicago White Sox", NY Yankees≠NY Mets). 증명: `tests/odds-match.test.js` + 라이브 시뮬(회수 4·오매칭 0·애매0).
- **불변식:** 서로 다른 피드의 경기를 결합할 땐 마지막 단어가 아니라 **정규화 토큰 부분집합 + 유일매칭 + 시각근접**으로. 애매하면(2+ 매칭) 붙이지 말고 잠금(잘못된 배당 첨부 금지). 근본적으론 일정+배당을 **공유 이벤트 ID 한 소스**로 받으면 이 결합 자체가 사라짐(설계 B안).
- **클래스: 클라 DB SELECT가 실제 테이블에 없는 컬럼을 요청 → PostgREST가 쿼리 전체를 거절 → UI가 조용히 빈 목록.** webtrade `loadPos`가 `positions.select('...,stake,...')` — 실 테이블엔 `stake` 컬럼 없음(메인 앱은 `size`만). 데모(WT_DEMO=true)일 땐 이 쿼리가 안 돌아 숨어 있다가, 실거래(WT_DEMO=false)로 켜고 실계정 로그인·주문 후 처음 실행되며 발현: 서버엔 포지션이 멀쩡히 들어갔는데(모바일엔 보임) webtrade만 "No open positions". 하네스는 실 세션·DB·CDN이 없어 이 실쿼리를 못 돌려 파싱/로직/렌더 테스트를 전부 통과함(사각지대).
- **수정:** `loadPos` SELECT를 trading.html의 검증된 쿼리와 정확히 일치(`local_id,symbol,side,size,open_price,pnl,status`)로 교체. `stake`·`created_at`(positions엔 미증명) 제거.
- **불변식/포카요케:** 클라가 `positions`/`accounts`에서 SELECT하는 모든 컬럼은 **증명된 실스키마 집합**(fx_open INSERT + trading.html 실셀렉트) 안이어야 함. `tests/webtrade-db-columns.test.js`가 정적으로 강제(실 DB 없이도 유령 컬럼 차단). 근본적으론 스키마-클라 컬럼을 한 소스에서 생성하면 어긋남 자체가 사라짐.

## MW 스프레드 전종목 이상(주식·BTC 0.0 / DOGE 28% 광폭 / FX 10× 얇음) — spr_pts 한 컬럼 다중 단위 (2026-07-13)
- **클래스: 공유 컬럼(`prices.spr_pts`)의 단위가 작성자마다 다른데(암묵), 독자가 단일 단위로 일괄 해석 + 소스 주석이 코드와 달라 오독을 유도.** 실제 단위는 fx-prices=FX 핍((ask−bid)/pip — 주석은 "points"라고 거짓말), crypto-prices=거래소 실스프레드 bps, stock-prices=항상 0(하우스 플로어는 체결 시 서버만). webtrade가 전부 "MT5 정수 포인트 ×tickSize"로 읽어 FX 10×얇음(EURUSD 0.1핍), ETH/주식/BTC bid==ask(무스프레드 상품 표시), DOGE 1.41bps→$0.0141 오독(0.06/0.08, 28% 광폭). 파생: 주식·BTC 플로팅이 서버 실현손익(8~10bps 플로어)과 어긋남(락스텝 위반). 근본원인: dd1330a "포인트 규격화"가 **작성자(Edge 소스·라이브 DB)를 grep하지 않고** 가정으로 진행됨(READ-BEFORE-WRITE #1 위반).
- **수정:** `halfPx(sym,mid,spr,mk)` — `fx_close.sql` v_half의 정확한 미러(FX `greatest(0.1,spr+markup)*pip/2`, 비FX `mid*greatest(FLOOR,spr)/10000/2`, FLOOR=Crypto 10·Stocks 8 bps, trading.html `ALPEXA_SPREAD_BPS` 락스텝). `_apply`/`_simulate`가 spr을 작성자 단위 그대로 보관하고 halfPx로만 변환. markup은 trading.html과 동일하게 `pricing_marks`에서 로드([0,50] 클램프). MWTrading의 원시 `m.spr` 표시는 MW 테이블과 같은 표시가격 유래 계산으로 통일.
- **불변식:** 클라 표시/플로팅 half-spread == 서버 `fx_close` v_half, 동일 공식·동일 단위(락스텝). spr_pts는 작성자 단위 그대로 저장·전달하고 변환은 halfPx 한 곳에서만.
- **포카요케:** `tests/webtrade-spread-units.test.js`(halfPx==v_half 전 자산군 케이스, 라이브 감사값 포함, RED→GREEN) + diagnose `SPREAD-uniform-unit`(spr×tickSize 일괄 변환 금지; 전제가 틀렸던 `SPREAD-pip-not-ticksize` 대체). 교훈: 공유 컬럼 단위를 바꾸거나 해석할 땐 **모든 작성자(Edge/SQL)를 먼저 grep + 라이브 DB 실측**, 주석은 증거가 아니다.

## FX앱 호가창이 webtrade·체결과 다른 스프레드/가격 표시 — 표시 전용 시뮬 트랙 분리 (2026-07-13)
- **클래스: 표시 경로와 체결 경로가 다른 데이터 트랙을 달림(표시=하드코딩 테이블+연출, 체결=라이브).** trading.html이 호가목록·주문티켓·포지션·펜딩트리거·스프레드뱃지 10곳에서 `last(시뮬 드리프트)+s.spread(하드코딩 표 + "숨쉬는" 랜덤 연출)`를 표시 — 실제 체결(`fxClosePx`=라이브 spr+마크업)과 다르고 webtrade와도 다름. 고객이 보는 스프레드를 데스크가 딜링하지 않음(미스쿼트 클래스). 마크업을 올려도 화면에 안 보임(시뮬값이라).
- **수정:** `fxQuoteMid/fxBidPx/fxAskPx` 3함수 — fxClosePx와 같은 실 mid 앵커 + 같은 `fxHalfSpread`. 표시 10곳 전부 이 함수로 교체(`last+spread` 전면 퇴역), 펜딩 트리거 감시가격도 딜링 호가로. 시뮬 스프레드 엔진(브리딩)은 슬리피지 스케일용으로만 잔존.
- **불변식:** 고객이 보는 bid/ask == `fx_close`가 체결하는 bid/ask == webtrade가 보여주는 bid/ask (같은 mid·spr·마크업이면 세 값이 틱 단위로 동일). 증명: `tests/fx-display-lockstep.test.js` — fxClosePx(BUY)==표시bid 항등식 + webtrade `halfPx`와의 크로스앱 패리티를 행위로 강제.
- **교훈:** "표시는 대충 시뮬로" 가 허용되는 곳은 돈과 무관한 장식뿐. 호가는 돈이다 — 표시·체결·타앱이 같은 진실(서버 prices)을 읽어야 하며, 그 감시는 정적 grep(`last+spread` 금지)이 가장 싸다.

## 부팅 시 차트 4개 전부 빈 화면 — 실봉우선 배포가 드러낸 숨은 경합 (2026-07-13, Claude 회귀)
- **클래스: (a) 마운트 시에도 실행되는 부가 이펙트([chartType])가 방금 만든 리소스(시리즈)를 교체 + (b) 비동기 응답의 스테일 판정을 "객체 정체성"으로 함 → 합법적 교체를 스테일로 오판.** 옛 코드에선 시드가 동기로 먼저 그려져 증상이 숨었고(대신 부팅 실봉이 항상 조용히 버려지고 있었음), 실봉우선(482719d) 배포로 시드 타이머까지 취소되자 **빈 차트로 표면화**. 재현: 프로덕션급 실봉 페이로드를 헤드리스에 주입 → REAL-RESOLVE 200봉인데 `series.current===s` false로 전량 폐기 로그 확보.
- **수정(04889bf):** 스테일 판정 = 셀별 **로드 epoch 카운터**(심볼/TF 로드마다 증가, 타입 스왑은 유지) · `applyBars`는 닫힌 s가 아닌 **현재 시리즈**에 그림(스왑 생존) · [chartType] 이펙트는 마운트 1회 스킵. 부수: 부팅 실봉 폐기 버그(옛날부터 있던)도 함께 완치.
- **불변식:** 비동기 로드의 취소 기준은 "그 로드를 대체하는 **새 로드의 시작**"(epoch)뿐이다 — 대상 객체의 교체는 취소 사유가 아니다.
- **교훈:** ① 순서만 바꾸는 "안전해 보이는" 변경도 숨은 경합을 표면화한다 — 배포 전 헤드리스 재현은 **성공 경로(실데이터 주입)**까지 돌려야 했다(실패 경로만 돌려서 초록으로 착각). ② 롤백 먼저 제안한 판단은 옳았고, 사용자가 증상을 좁혀줘("TF 클릭 시 생성") 전진 수정이 가능했다.

## 원클릭 패널 소멸 — React 소유 DOM을 innerHTML=''로 청소 (2026-07-13, Claude 회귀 2)
- **클래스: 프레임워크(React)가 소유한 컨테이너를 명령형 DOM API로 통청소 → 프레임워크의 가상DOM과 실DOM 괴리, 자식 컴포넌트 증발.** 겹침 방지용 `box.innerHTML=''`이 차트 컨테이너 안의 React 자식(drawlayer·OrderBox)까지 삭제 — React는 존재한다고 믿어 다시 안 그림.
- **수정(핫픽스):** 청소를 `querySelectorAll('.tv-lightweight-charts')`로 **차트 루트만 표적 제거**. 테스트가 innerHTML 통청소를 금지 핀.
- **불변식:** React 컨테이너 안에서 명령형 제거는 **내가 만든 노드(라이브러리 루트)만**, 셀렉터로 표적해서. `innerHTML=''`은 React 트리 안에서 금지.
- **교훈:** 회귀 스크린샷(boot-fixed.png)에 패널 부재가 찍혀 있었는데 차트만 보고 지나쳤다 — 캡처 검증 시 **바뀐 것 외의 요소도 체크리스트로** (패널·MW·하단바).

## 차트 과거 스크롤이 매 틱마다 현재로 강제 복귀 (2026-07-14)
- **클래스: "멱등해 보이는" 옵션 재적용이 실은 동작을 수반 — `timeScale.applyOptions({rightOffset})`는 설정이 아니라 스크롤 명령.** 틱 핸들러가 우측 여백 고정용으로 매 틱(~2.5/초) rightOffset:15를 재적용 → LightweightCharts는 그때마다 뷰를 "마지막 봉+15"로 이동 → 과거를 보러 드래그한 사용자가 0.4초 안에 현재로 끌려옴. 주석엔 "never yanks"라고 적혀 있었음(검증 없는 주장).
- **수정(6d16347):** MT5 오토스크롤 규칙 — `ts.scrollPosition()>=13`(라이브 엣지)일 때만 재고정, 과거 열람 중(<13)엔 타임스케일 불가침. 엣지 복귀 시 자동 재개.
- **불변식:** 사용자가 과거로 스크롤한 뷰는 사용자의 것 — 틱/피드 경로는 뷰포트를 이동시키지 않는다(라이브 엣지에서의 여백 유지만 예외).
- **교훈:** 라이브러리 옵션 적용이 부작용(스크롤·리레이아웃) 없는지는 주석이 아니라 행위 테스트로 증명 — `webtrade-history-scroll.test.js`가 스텁 timeScale로 두 경우(엣지=재고정 / 과거=무접촉)를 실행해 강제.

## 크립토 스프레드 0.0 거짓 표시 — 일률 소수점이 서브센트 스프레드를 증발 (2026-07-14)
- **클래스: 자산 가격대와 무관한 일률 표시 정밀도(전 크립토 2자리) → 반올림이 진실을 삼킴.** XRP 1.07의 10bps 하우스 스프레드(0.00107)가 화면에서 bid==ask로 반올림 → Spread 칸 0.0. 서버(fx_close)는 부과하는데 화면은 "무스프레드"라고 말함 = "보는 호가==체결 호가" 불변식의 표시측 위반. (BTC 62.6 고정은 결함 아님 — 10bps 플로어가 거래소 실스프레드(서브bp)를 지배하는 설계.)
- **수정(dbdbe88):** digits() 가격대별(BTC/ETH 2·SOL 3·XRP 4·DOGE 5) + 스프레드 표기 3곳(MW·팝업·원클릭)을 단일 sprText로 통일(크립토는 digits-1 소수).
- **불변식:** 고객에게 부과되는 스프레드는 어떤 종목에서도 화면에서 0으로 보이면 안 된다.
- **교훈:** 표시 정밀도도 돈 코드다 — 정밀도가 요금을 숨기면 그 화면은 거짓말을 한다. 새 종목 추가 시 "최소 호가 단위로 스프레드가 보이는가"를 체크.

## H4/W1 딥 히스토리 무음 실패 — API 파라미터 의미 오해 (2026-07-14, Claude 버그)
- **클래스: 외부 API 파라미터의 "단위"를 문서 확인 없이 직관으로 가정.** Polygon `limit`은 출력 봉 수가 아니라 **스캔하는 베이스 집계(분봉) 수** → limit=5000의 H4 요청 = 분봉 5천 개 = 7일 = 34봉. 게다가 페이지 종료 조건 `results.length<5000`이 출력 봉수를 베이스 한도와 비교해 1페이지에서 조기 종료. W1 n=1560은 `from = 지금−120년 = 음수 epoch` → Polygon 400. 실측 RED: H4 34봉 · H1 128봉 · W1 0봉(400) — 클라 실봉 검증(≥160)에 걸려 **합성 차트로 무음 폴백**(사용자는 실봉인 줄 앎).
- **수정:** limit=50000(최대) · 종료는 빈 페이지일 때만 · `from`을 2000-01-01로 클램프 · 베이스 단위 기준 페이지 예산(≤8p). `tests/fx-prices-deep-history.test.js`가 핀. **배포는 사용자**(Edge).
- **불변식:** 페이지네이션 종료 판정은 API가 세는 단위와 같은 단위로 비교한다.
- **교훈:** ① "배포 후 각 TF를 실측"까지가 배포다 — M1·D1만 확인하고 초록 선언했으면 H4/W1 무음 폴백을 못 잡았다. ② 무음 폴백(합성 대체)은 편하지만 결함을 숨긴다 — 헬스 프로브는 폴백 이전 원천을 본다.

## 대시보드가 앱의 RG 돈 관문을 빠뜨림 — 부분 복제 클래스 (2026-07-14, 출시 전 감사로 차단)
- **클래스: 기존 화면을 새 화면으로 복제할 때 "보이는 문법"은 옮기고 "안 보이는 가드"를 빠뜨림.** 대시보드 placeBetCore가 잔고·min/max·halt는 가져왔지만 자기배제(isLocked)·일일 손실한도(checkLossLimit)를 누락 — 자기배제 고객이 대시보드로 우회 베팅 가능했던 구멍. 사고 전 발견 경로 = **앱 문법 분석 에이전트의 차이 감사(스펙 §8)**.
- **수정(fa3baf5):** players.self_exclude_until/loss_limit(서버 소유) pull + placeBetCore 차단. 손실한도 합산은 서버 ledger(kind=bet) 파생으로 앱보다 강화. 캐시아웃 버튼도 앱 락스텝(숨김)으로 정렬.
- **불변식:** 새 진입면(화면/앱)이 돈 경로를 노출하면, 원본의 **모든 사전 가드 목록**을 먼저 표로 만들고 전 항목 이식 확인 후 연다.
- **교훈:** 복제의 위험은 다르게 만드는 게 아니라 **덜 만드는 것**이다 — 차이 감사를 기능 완성 "후"가 아니라 돈 경로 여는 "직전"에 돌린다. 하네스에 RG 4핀 영구 장착.
- 2026-07-15 · **게스트 자동생성으로 로그인 판정 오염** (sports-dashboard) — `AlpexaSync.me()`는 alpexa.me 부재 시 게스트 신원을 만들어 반환 → 대시보드 `authed`가 영원히 참, 로그아웃 UI(로그인 유도 문구·버튼) 전부 죽은 코드 + 랜덤 게스트 계정으로 서버 조회(빈 결과가 '···'로 가려 은폐). 근본원인: 판정 함수가 "자동 폴백이 있는 API"를 신뢰. 수정: 실제 신원 태그(localStorage alpexa.me) 직접 판독. 재발 방지: fidelity에 alpexa.me 직접 읽기 pin + AlpexaSync.me() ban. 발견 경로: 로그인 통합 E2E RED (하네스가 먼저 잡음 — 고객 아님).
- 2026-07-15 · **유령 세션 — 태그≠세션** (sports-dashboard, 고객 보고 "돈 기능 전멸") — 신원 태그(alpexa.me)는 영구 localStorage, 세션은 탭별 sessionStorage인데 대시보드가 태그만으로 authed 판정 → 예전에 로그인했던 브라우저의 새 탭에서 "로그인된 것처럼 보이는데 RLS가 전 행을 숨겨 잔고·베팅·활동 전부 빈" 상태. 앱은 이미 같은 클래스를 게이트로 막고 있었음(sports-live:725-741, #30) — **부분 복제 클래스 재발**(보이는 문법만 옮기고 안 보이는 가드 누락, 2026-07-14와 동일 클래스). 수정(6ecd195): moneyStore가 auth.getSession() 실존 확인, 태그+세션 둘 다 있어야 authed + "세션 만료" 안내 UI. 재발 방지: fidelity에 getSession 핀. 교훈: 새 진입면 체크리스트에 "원본의 로드/세션 게이트"도 돈 가드와 같은 급으로 포함.
- 2026-07-16 · **body.className 통째 대입이 테마 클래스 파괴** (sports-live) — 슬립 모드 전환이 `body.className='m-…'`로 클래스 전체를 갈아치워 설정의 `dark` 테마 클래스까지 삭제 → 실기기에서 "팔레이 누르면 다크→라이트" (레전드 다크 채택 직후 고객 노출). 근본원인: 상태 클래스(모드)와 테마 클래스가 같은 엘리먼트를 공유하는데 한쪽이 전체 대입 사용. 수정(baaf283): setModeClass()로 m-single/m-parlay/m-sgp만 classList 스왑. 재발 방지: diagnose BODY-classname-assign(패턴) + mode-dark E2E(행위). 교훈: className 대입은 "그 시점의 다른 클래스 전부"를 암묵 삭제하는 연산 — 상태 클래스는 언제나 classList로.
- 2026-07-18 · **유령 layout 항목이 도킹/스냅에 참여 — 패널 전폭 부풀림** (dev/fx-terminal FX 터미널) — `DEFAULT_LAYOUT`에 하단 도크(#dock)용 `toolbox:{w:1884,h:240}` 항목이 남아 있었는데 대응하는 `w-toolbox` DOM은 없음(도크는 스테이지 위젯 아님). Navigator를 하단 빈 공간에 놓으면 `maybeDock`이 이 유령 사각형과 겹쳐 그룹 생성 → Navigator가 1884px 전폭으로 부풀어 차트 영역으로 넘치고 탭 라벨이 "toolbox"로 뜸. 근본원인: 도킹/스냅 후보를 `Object.keys(layout)`로 순회하며 **DOM 실존을 확인하지 않음** — 죽은 layout 키가 물리적 위젯처럼 취급됨. 수정: `isStageWidget(id)=!!document.getElementById('w-'+id)` 게이트를 `maybeDock`·`othersOf` 양쪽에 + `DEFAULT_LAYOUT`에서 유령 toolbox 제거(옛 저장 레이아웃은 게이트가 무력화). 재발 방지: fx-smoke "drop into empty space does NOT grab phantom toolbox" 회귀 핀(전폭 부풀림·차트 침범·bogus 탭 3중 검사). 발견 경로: 사용자 스샷 → 헤드리스 드래그 재현(빈 공간 드롭 → nav.w 1884 실측). 교훈: 좌표 기반 상호작용(도킹/스냅/충돌)의 후보 집합은 layout 데이터가 아니라 **실제 렌더된 DOM**을 진실로 삼는다 — data와 DOM이 어긋나면 유령이 물리 법칙에 참여한다.
- 2026-07-19 · **sticky wsLive가 폴백을 영구 억제 — 시세 얼어붙음** (dev/crypto-dashboard 크립토 대시보드, 고객 보고 "시세가 안움직임") — 마켓 워치가 Binance 미러 WS(1차) + 서버 `prices` 8s 폴링(폴백) 구조인데, 폴백 스킵 게이트가 `if(mk.wsLive && q.last!=null) return`. `wsLive`는 WS 메시지마다 true로 세팅되고 **close/error에서만** false. 소켓이 close 이벤트 없이 조용히 정지하면(프록시·유휴에서 흔함) `wsLive`가 영원히 true로 굳어 폴백이 절대 안 돌아 → 마지막 값에서 시세 정지(값은 있는데 안 움직임). 근본원인: 라이브 판정을 **신선도**가 아니라 sticky 플래그로 함. 수정: 폴백 스킵을 `mk.lastTick<12s`(이미 기록 중이던 실틱 시각) 기준으로 전환 + 15s 무틱 시 강제 재접속 워치독. 검증: 스텁 피드로 RED→GREEN(stale WS→폴백이 얼어붙은 값 복구 / fresh WS→라이브 값 미침범) — cb-fixtest.js. 세션 프록시가 실피드 차단이라 라이브 확인은 사용자 브라우저 필요. 교훈: "라이브"는 플래그가 아니라 **최근성**으로 판정 — 조용히 죽는 소켓을 close 이벤트로만 감지하면 폴백이 죽는다(#5 fail-closed의 연장).
- 2026-07-19 · **지갑 헤드라인이 히스토리 RPC에 묶여 "로그인했는데 Sign in" 표시** (dev/crypto-dashboard, 사장님 스샷: 총자산 $3.3M KV는 다 뜨는데 헤드라인만 "Sign in to view balance") — `loadWallet`이 `wal.real`(=헤드라인 표시 여부)을 **`get_portfolio_history`가 points≥2 반환할 때만** true로 세팅. 세션은 유효(KV·포트폴리오·스테이킹 전부 실데이터 로드됨)한데 히스토리 RPC가 비면(신규계정·레인지 무데이터·RPC 이슈) 로그인 유저가 미로그인 UI를 봄. 근본원인: "로그인했나"(세션)와 "차트 이력이 있나"(RPC)를 **한 플래그로 뭉갬** — 신원 판정을 데이터 가용성에 종속. 수정: `wal.authed=!!sess`로 헤드라인 판정 분리, 총액은 KV Total과 동일 소스(cash+cryptoV+staked)로 표시, 히스토리 RPC는 차트선·실 P&L에만. 검증: 스텁 세션+빈 히스토리 RED→GREEN(cb-authwallet.js — authed+empty→실총액 표시, 미로그인→CTA 복귀). fidelity 핀 3개(authed=!!sess·헤드라인 분기·총액 동일소스). 교훈: 신원/인증 상태는 **오직 세션**으로 판정 — 부수 데이터(이력·잔고행)의 유무로 로그인 여부를 추론하면 "데이터 없는 로그인 유저"가 유령처럼 미로그인 취급된다(#5 유령세션 클래스의 역방향).
- 2026-07-19 · **차트 얼어붙음 2클래스 + 매매바가 캔들 가림** (dev/crypto-dashboard·dev/fx-terminal, 사장님 보고 "시세만 바뀌고 차트가 안 움직여·네모시세에 가려져") — ① W4 차트는 mk.q에서 2s마다 마지막 캔들을 갱신하므로 **피드가 얼면 차트도 언다**(sticky wsLive의 하류 증상). ② `chLoad()` 실패 시 `ch.candles=[]` 후 **재시도 없음** → 일시적 네트워크 딸꾹질 한 번에 차트 영구 정지. ③ FX 터미널 원클릭 매매바(ocbar)가 plot 위 absolute라 캔들 상단 ~30px을 덮음. 수정: **시세 하네스 `feedHz`**(단일 제어루프 2.5s) = 12s 무틱→폴백 즉발+WS 재접속 · 25s 복구불능→⚠ FEED STALE 배지(정직 표시, 복구 시 자동 숨김) · 캔들 소실→chLoad 재시도(3회+60s 백오프), 치유 이력 링버퍼+콘솔. FX는 padT 6→34로 매매바 아래부터 그림. 검증: `feed-harness.test.js`(피드 죽임→감지→치유→복구 RED→GREEN 6핀, verify 게이트) · fx-smoke "bar does not cover candles" 핀 · fidelity에 feedHz 5핀 · 외부 감시 `feed-liveness-check.js`+`feed-liveness.yml`(6h 크론, main 필요). 교훈: 파생 소비자(차트)는 피드 얼어붙음을 **그대로 상속**한다 — 치유는 소스(피드)에 한 곳, 소비자는 재적재 훅만. 그리고 실패한 fetch에 재시도 없는 초기화는 "한 번 삐끗=영구 정지"다.
- 2026-07-19 · **지갑 평가액이 로드 스냅샷에 고정 — 시세는 흐르는데 잔고 정지** (dev/crypto-dashboard, 사장님 보고 "시세가 움직여도 발란스 그대로") — Overview KV(Crypto value·Total assets)와 헤드라인 총액을 `loadWalletKvs` 호출 시점의 서버 mid로 **한 번만** 계산하고, 시세 틱에는 재평가하지 않음. Portfolio 위젯은 2s 라이브 재마크가 있었는데 지갑은 없던 **비대칭**(부분 복제 클래스 — 보이는 KV만 옮기고 안 보이는 재평가 루프 누락). 근본원인: "평가액=파생값"인데 파생 소스(가격)의 변화에 재계산 훅이 없음 — MT5 규율 "손익/평가액은 실시간 피드로 마크"의 지갑판 위반. 수정: 3s 재평가 루프 — 수량(wal.hold)·스테이크는 서버 로드분 그대로, 가격만 kvLivePrice(mk.q 라이브 → pf.mid 폴백, Portfolio pfPrice와 동일 서열)로 재계산, 0.5센트 이상 변할 때만 KV+헤드라인 재렌더(표시 전용, 돈/RPC 무접촉). 검증: wallet 게이트에 RED→GREEN 행위핀(BTC +10% 틱 → 총액 64100→70500 동행, 헤드라인=KV 동일) + fidelity 핀 2. 교훈: 파생 표시값(평가액·플로팅·노출)은 "무엇으로부터 파생되는가"를 적고 **그 소스가 움직일 때 재계산되는 경로**가 있는지를 복제 체크리스트에 포함 — 스냅샷 계산은 그 자체로 미래의 정지화면이다.
- 2026-07-19 · **정렬 헬퍼가 지역 별칭(ms)을 전역서 참조 — 오픈벳 위젯 전멸** (sports-dashboard, 사장님 보고 "오픈벳이 다 사라졌다") — 오픈벳 정렬을 넣으며 `sortedOpenBets`를 전역에 선언했는데 본문이 `ms.openBets` 참조. `ms`는 renderBets **안의** 지역 별칭(`const ms=moneyStore`)이라 전역엔 없음 → 렌더 호출 즉시 ReferenceError → try 밖이라 오픈벳 위젯 통째로 빈 화면. **정적 파스·fidelity 전부 통과했는데 샌 이유: 문법은 합법(런타임에만 죽는 참조)이고, 바꾼 위젯을 실제로 그려보는 런타임 게이트가 스포츠 대시보드엔 없었다** — verify+feed-check만 돌리고 렌더 실측을 건너뜀(fx-smoke가 있는 FX와 달리). 수정: `sortedOpenBets(bets)` 인자화 + 호출부 `sortedOpenBets(ms.openBets)`. 재발 방지: `sports-dashboard-bets.test.js` 신설(실페이지+스텁 티켓 3장 → 무예외·3/3 렌더·LIVE→이른킥오프→늦은 순서·빈 games 무크래시, verify 게이트 편입) + fidelity 핀 시그니처 갱신. 교훈: **"어느 스코프의 이름인가"는 파서가 안 잡아준다** — 헬퍼를 밖으로 뺄 땐 의존값을 인자로 받게 하고, UI를 바꿨으면 그 UI를 **실제로 한 번 그려보는 게이트**가 반드시 있어야 한다(스포츠 대시보드에 이번에 신설).
- 2026-07-19 · **정산 팀명 매칭 실패 — MLS 팔레이 영구 미정산** (sports-settle Edge, 사장님 발견 SP-100058: 금요일 경기 2일 지나도 "Awaiting result") — 크론은 5분마다 생존(cron.job 실측), 창(6일)·리그(usa.1)·gid 전부 정상인데 `teamSide`가 ESPN **shortDisplayName**("Sporting KC"·"Whitecaps")에 정확/부분문자열 매칭만 사용 → 픽 이름("Kansas City"·"Vancouver")과 불일치 → gradeLeg null → allDone=false → **영구 보류**(두 leg 다 MLS라 티켓 통째 스턱). 근본원인: 오즈 매칭은 2026-07-08 오즈 불변식에서 "마지막단어(nick) 금지 → 정규화 토큰 부분집합+유일매칭"으로 고쳤는데 **정산 쪽 팀 매칭엔 같은 규율을 미적용**(부분 수리 클래스 — 같은 결함이 두 곳에 있는데 한 곳만 고침). 수정: Result에 이름 변형 전부(homeAll/awayAll = short/display/name/location) 수록 + teamSide를 정규화 토큰 부분집합 + **유일매칭**(양쪽 동점=null, 돈은 추측으로 안 움직임)으로 재작성, 구형 Result 하위호환. 검증: `settle-team-match.test.js` — 구버전 RED 재현("Kansas City"→null) → 신버전 GREEN(13핀: 사고 케이스·풀네임·축약·모호 fail-safe·하위호환·DNB push 회귀). 배포: Edge라 사장님(`supabase functions deploy sports-settle`) — 배포 후 5분 크론이 자동 정산. 교훈: 한 결함 클래스를 고칠 땐 **같은 매칭이 사는 모든 곳**(오즈·정산·렌더)을 grep으로 세고 전부에 적용 — 반쪽 수리는 반대쪽에서 같은 사고로 돌아온다.
- 2026-07-19 · **연기 경기 leg가 팔레이 무한대기 + 죽은 팔레이도 대기** (sports-settle, SP-100058 후속 — debug 실측으로 확정) — 정산이 "전 leg 최종"일 때만 동작해 ① VAN@CHI 미개최(6일 파이널에 부재) → 티켓 영구 보류, ② KC leg가 이미 확정 패배(2-3)라 팔레이가 죽었는데도 대기. 수정(사장님 승인): 규칙 A=한 leg 확정 패배 → pending 무시 즉시 LOST(북메이커 표준 — 죽은 팔레이는 안 기다림) · 규칙 B=킥오프+48h 무결과 && 6일 증명창 안 → leg void(배당 1.0, 곱셈 제외), 전부 void면 스테이크 환불. fail-safe: kt 없음/48h 미만/증명창 밖은 계속 보류(돈은 추측 금지). 검증: settle-void-earlylost.test.js — 구버전 무한대기 RED 재현 → 신규칙 GREEN 11핀(조기패배·void 제외·전void 환불·3중 fail-safe·전승/미래/push 회귀). 교훈: 정산기의 "기다림"에는 반드시 **출구 조건**(조기 확정·void 시한)이 있어야 한다 — 현실의 스포츠는 연기·취소되는데 코드가 이상적 일정만 가정하면 돈이 영구 부유한다.
- 2026-07-19 · **C1 묵은-미정산 오탐 — 선물 베팅이 영구 빨강(늑대소년 알람)** (sports_audit.sql, 사장님 이메일 스샷으로 발견: 진짜 묵은 티켓 0건인데 C1 🔴 2) — C1이 `updated_at < now()-36h`로 판정 → 오래 전에 걸어둔 미래 경기 베팅(코번트리 8/21을 7/5에 베팅)이 정산일까지 매일 빨강. 알람이 매일 거짓으로 울리면 진짜 사고가 묻힌다(알람 시스템의 최악의 죽음). 수정(v2, 승인): 판정을 **티켓의 마지막 leg 킥오프(meta.legs[].kt) max가 36h+ 경과했는데 아직 open**으로 교체 — 미래/오늘 경기는 조용, 경기 끝나고도 안 닫힌 것만 빨강. kt 없는 구형 행만 updated_at 폴백. 교훈: "얼마나 오래 열려 있었나"가 아니라 **"닫혔어야 할 시점이 지났나"**로 판정 — 시간 기반 알람의 기준은 나이(age)가 아니라 기한(deadline)이다.
- 2026-07-19 · **시세창 더블클릭 주문 티켓 — 핸들러는 있는데 이벤트가 영영 안 옴** (dev/fx-terminal, 사장님 보고 "더블클릭시 주문 넣을수 있게 해줘") — `#mwBody`에 dblclick 위임 핸들러가 처음부터 있었지만 실제 마우스로는 절대 발화 안 함: 첫 클릭이 행 선택→`mwRender()`가 행 DOM을 통째로 교체 + 1초 주기 재렌더도 두 클릭 사이에 끼어듦 → 브라우저 네이티브 dblclick의 "같은 노드에 2클릭" 조건이 영영 안 채워짐. **dispatchEvent(dblclick) 테스트는 통과하는데 진짜 마우스는 실패하는 클래스** — 합성 이벤트는 브라우저의 클릭 카운팅을 안 거친다. 수정: 클릭 위임 안에서 심볼 기준 수동 더블클릭 판정(같은 심볼 450ms 내 재클릭 → openOrderDialog) — DOM이 갈려도 심볼은 불변이라 죽지 않음 + 선택 불변 시 불필요 재렌더 제거. 검증: `fx-terminal-order-dblclick.test.js` — playwright **실제 마우스 2클릭**으로 RED 재현 → GREEN 5핀(미선택행·선택행·Esc·600ms 느린클릭은 미발화). 교훈: 자주 재렌더되는 리스트에서 네이티브 dblclick은 신뢰 불가 — **식별자(심볼) 기준 수동 판정**으로 하고, 클릭류 UI 게이트는 dispatchEvent가 아니라 **실마우스 시퀀스**로 검증해야 이 클래스가 잡힌다.
- 2026-07-19 · **틱차트 페어 셀렉트가 "떴다 금방 사라짐"** (dev/fx-terminal Ticks/Details 탭, 사장님 보고) — 탭 본문을 매초 `innerHTML` 통재생성 → 열려 있는 네이티브 드롭다운이 노드 교체로 즉시 닫힘. 같은 날 더블클릭 버그와 **동일 클래스(주기 재렌더가 진행 중인 상호작용을 죽임)** 의 세 번째 발현. 수정: 스켈레톤 1회 생성 + `mwSyncSel`(옵션/값만 갱신하되 **포커스 중엔 절대 안 건드림**) + 동적 값은 textContent만 교체. 실측: 포커스 1.6s 방치에도 동일 노드·포커스 유지, 페어 전환 정상. 교훈: 주기 재렌더 UI에 폼 컨트롤을 넣을 땐 **컨트롤은 불변 노드로 두고 값만 흘려보내라** — innerHTML 재생성은 드롭다운/포커스/더블클릭/IME를 전부 죽인다 (이 클래스 grep: `setInterval` 안에서 `innerHTML=`를 만드는 곳).
- 2026-07-19 · **feed-liveness 크론 3연속 오탐 빨강 — window.mk 가드** (tests/feed-liveness-check.js, 사장님이 GitHub 실패 메일로 발견) — 프로브가 `window.mk && mk.lastTick`으로 신선도를 읽는데 `mk`는 top-level `const`(전역 lexical) → **window에 안 붙어** 항상 undefined → 시세가 쌩쌩한데도(같은 런에서 20s 움직임 8/8 ✅) lastTick 체크만 매번 🔴. 진짜 정지는 하루 종일 없었음(시간당 자가검진 0.2~3s 신선). 수정: `typeof mk!=='undefined'` 가드로 교체(로컬 실측: window.mk=undefined·lexical mk=존재). 교훈: **top-level const/let은 window 프로퍼티가 아니다** — page.evaluate에서 전역을 짚을 땐 `typeof` 가드가 표준(이 클래스 grep: `window\.[a-z]+ &&`). 오탐 알람은 즉시 잡는다 — 늑대소년이 되면 진짜 정지를 놓친다(C1 v2와 같은 원칙).
- 2026-07-19 · **라이트 테마 저대비 — 툴박스 흰 글자·다크 칩** (dev/fx-terminal, 사장님 스샷 "화이트에 글짜가 안 보여") — ① Trade 표 td가 라이트에서도 #F5F6F7(밝은 잉크)로 렌더 → 흰 배경에 흰 글자. td에 `color:var(--lgd-ink)` 명시로 테마 추종 강제. ② ocbar 칩들이 rgba(18,24,34,.72) 다크 배경 하드코딩 → body.light 오버라이드 추가. 교훈: 테마가 2개면 **색은 전부 토큰(var) 경유** — 하드코딩 다크값·명시 없는 상속 의존은 라이트에서 반드시 깨진다.
- 2026-07-19 · **플로팅 P/L quote→USD 환산 — 얼어붙은 핍밸류(0.0067)·무환산** (webtrade + dev/fx-terminal, 갭감사 에이전트 발견 → 사장님 "고쳐") — 클라 플로팅이 엔 환산을 0.0067(USDJPY=149 시절 고정값)로 하드코딩, USDCHF/USDCAD는 환산 자체가 없음(1:1 취급). 서버 fx_close는 라이브 환율로 정확 → 현 환율 기준 JPY +9%·USDCHF −19%·USDCAD +42% **표시 오차**(플로팅≠실현 — 화면 +$142 보고 청산하면 +$100 입금). 수정: `quoteUsd()`/`fxQuoteUsd()` — 서버와 동일 분기(quote=USD→1 · USD-base→1/현재가 · 크로스→quote→USD 라이브, 환율 없으면 0=fail-safe)를 webtrade·터미널 동시 적용(trading 모바일은 원래 정확). 검증: `fx-pnl-quote-usd.test.js` — 구버전 RED 재현(+9%/+42%) → 신공식 7케이스 서버 미러와 센트 단위 일치 + **MT5 핍밸류 방식과의 등가 증명**(35핍×$6.16 = 동일) + 소스 핀 3. 교훈: 환산 계수는 **상수가 아니라 시세다** — "핍밸류"는 매 틱 재계산되는 값이고, 하드코딩한 순간 환율이 움직이는 만큼 화면이 거짓말한다. 같은 화면 공식이 여러 파일에 살면(웹트레이더·터미널) 전부 한 커밋으로.
- 2026-07-20 · **SL 쳤는데 청산 안 됨 — fx_modify/fx_sltp 미배포(DRAFT 파일)** (사장님 실전 발견) — SL/TP는 ①저장(fx_modify)→②매분 스위프(fx_sltp)가 발동하는 2단 구조인데, 두 함수가 든 fx_modify.sql이 "DRAFT — TEST BEFORE PROD" 상태로 **통째 미배포**. 터미널 SL 드래그 → rpc 실패(함수 부재) → meta.sl 저장 자체가 안 됨(실측: 열린 포지션 3개 전부 sl=null) → 걸린 줄 알았던 SL이 존재하지 않았음. 진단 절차: 크론 확인(fx_sltp 부재 추정) → 사장님 파일 실행(04:07) → 04:08부터 매분 succeeded 실측 → settlements에 오늘 SL 청산 0건 + positions sl 전부 null로 "저장 단계부터 실패" 확정. 교훈: **기능은 "클라 버튼이 있다"가 아니라 "서버 함수가 배포되어 있다"로 완성** — DRAFT SQL은 배포 전까지 그 기능을 클라에 노출하면 안 되거나(정직 거절), 배포 체크리스트에 함수 존재 확인을 넣어야 한다. 재발 방지: 실주문 배선(M4) 때 대기주문은 이 원칙대로 정직 거절해 뒀음 ✓ · fx_modify 오류 토스트는 이미 있었으나 순간 노출 — 실패 시 Journal 로그도 함께 남기고 있어 추적 가능.
- 2026-07-20 · **주문창 SL/TP 입력이 서버로 전달 안 됨 — 보이는 입력칸, 버려지는 값** (webtrade New Order 모달, 사장님 실전 발견 "SL 걸었는데 트리거 안 됨" 2일차) — 주문창에 Stop Loss·Take Profit 입력칸(포커스 시 라이브 시세 주입 + ▲▼ 스피너까지)이 완비돼 있지만, `submit()`이 `placeOrder({symbol,side,volume,leverage,requestedPrice,maxSlippage})`만 호출하고 **sl/tp 값은 어디로도 안 보냄** — fx_open엔 SL 파라미터가 없고 체결 후 fx_modify 후속 호출도 없음. 고객은 SL을 "입력했고 화면이 받아줬으니" 걸렸다고 믿지만 서버 meta.sl은 영원히 null → 스위프는 검사할 게 없어 미발동. 진단 절차(전 구간 실측 소거법): 서버 5부품(함수 배포·grant·매분 크론·시세 신선도·specs/P&L 헬퍼) 전부 정상 → positions sl=null로 "저장 단계" 확정 → 터미널 드래그·Modify 다이얼로그는 실마우스 헤드리스로 fx_modify 정상 호출 재현(클라 배선 무죄) → 주문창 제출 코드에서 sl/tp 미전달 발견. 어제 "fx_modify 미배포" 결함과 **연쇄 은폐**: 어제까지는 어떤 경로로도 저장이 안 됐어서 이 구멍이 안 보였음. 교훈: **입력 UI의 완성도는 배선의 증거가 아니다** — 폼 필드를 만들면 "이 값이 서버 어느 파라미터로 가는가"를 필드마다 한 줄씩 표로 적고, 안 가는 필드는 아예 그리지 않는다(있는데 안 가는 칸 = 고객에게 하는 거짓말이자 리스크 관리 구멍). 수정(승인 "gogo" 후): 체결 성공 직후 `fx_modify(r.local_id, slPx, tpPx)` 후속 호출 + 거절 시 소리·알림·Journal(무음 탈락 금지) — 터미널 주문창의 post-fill 패턴과 동일 규율. 검증: `tests/webtrade-order-sltp.test.js` — 실브라우저 풀부트(CDN→tests/vendor 라우팅으로 프록시 차단 우회)로 구버전 RED(fx_open ✓·fx_modify 0회) 재현 → 수정 후 GREEN 6핀(같은 local_id로 fx_modify·p_sl 정확·미입력 시 0회·거절 시 alert+Journal). verify 게이트 편입.
- 2026-07-21 · **데모 터미널에 SL/TP 실행 엔진 부재 — 표시는 되는데 발동 주체가 없음** (dev/fx-terminal 연습 모드, 사장님 스샷 "TP 넘었는데 청산 안 됨" → "스샷은 데모아냐?"로 판명) — 데모 포지션에 SL/TP를 걸면(드래그·Modify) 값은 저장·표시되지만, 그걸 **검사해서 청산하는 루프가 어디에도 없었음** — demoMarkAll은 P/L 재마킹만. 서버 스위프(fx_sltp)는 server='fx' 실포지션만 보므로 데모는 애초에 관할 밖(사장님 설계 의도대로 데모=서버 무관). 진단이 하루 늦은 이유: 스샷의 EURUSD·GBPUSD를 실포지션으로 오인하고 서버 스위프를 이틀 조사(크론·grant·prosrc까지) — **화면이 LIVE인지 DEMO인지부터 확인했어야** 했다(서버 열린 포지션 목록과 스샷 심볼이 불일치한 시점에 판별 가능했음). 수정: `demoSltpSweep()` — 마스터 1s 루프에서 어느 탭이든 매초, MT5 판정(BUY=bid로 SL/TP, SELL=ask 반전), 히트 시 History(via:SL/TP)+Journal+토스트, 서버 접촉 0. 검증: `fx-terminal-demo-sltp.test.js` RED→GREEN 6핀(TP/SL/SELL반전/무설정 불변/타 탭 스위프). 교훈: ① **"어느 모드의 화면인가"가 돈 사건 진단의 0번 질문** — 심볼·티켓을 서버 실데이터와 대조하면 즉시 갈린다. ② 값을 "설정"하게 해주는 UI를 만들면 그 값을 "집행"하는 주체가 어디인지까지가 한 기능이다(주문창 SL/TP 미전달과 같은 클래스의 데모판 — 받아주고 안 지키는 UI는 거짓말).
- 2026-07-21 · **[종결] 실계좌 SL/TP 첫 자동 발동 실측 확인** — settlements 실데이터: `SL BUY 0.1 @ 1.33773 → 1.3376` (22:28:48 UTC, GBPUSD, P&L −2.30, 스위프 청산 형식). 이로써 SL 사건 3부작 전부 닫힘: ① fx_modify/fx_sltp 미배포(7/20 배포) ② 주문창 SL/TP 미전달(15d8bdb) ③ 데모 실행 엔진 부재(fa1bb51) + 초단위 에지 2종(fx-stream·crypto-price) 배포 완료(피드 생존 실측 0~4.4s). 실돈 체인 완성: 저장 3경로 → 초단위 스위프(1분 크론 폴백) → settlements → 잔고 트리거. 잔여 개선 후보(백로그): 스파이크 워터마크(틱 사이 고저 캡처), fx_sltp/fx_stopout 스왑 미포함 패치(승인 대기 유지).
- 2026-07-22 · **[예방] 차이감사 톱5 일괄 폐쇄 — 터미널 실주문 경로 webtrade 동급화** (사장님 "gogo") — ① 화면 half에 markup_pts 포함(mwHalf+loadMarks, 화면 플로팅=서버 실현 락스텝) ② 주문 3경로(원클릭·주문창·MW Trading)에 세션 게이트 fxMarketOpen(캘린더 판정 — 주말 동결가 체결 구멍 폐쇄) ③ fx_open에 p_leverage=500·p_requested_price·p_max_slippage=15pip 전달(서버 슬리피지 가드 부활) ④ 주문창 SL/TP를 fx_open이 쓴 그 local_id로 즉시 fx_modify(900ms 최신-포지션 추정 폐기 — 레이스 제거) ⑤ settlements Realtime(ch3)+onSettle — SL/TP/STOPOUT 자동청산 즉시 통보(소리+토스트+Journal)+계좌·이력 재조회, MT5 효과음 4종(fxSnd). 검증: `fx-terminal-live-guards.test.js` 9핀(닫힌장 RPC 0회·markup half 수식·같은 local_id 부착·타계좌 무시 포함) + 기존 4게이트 전부 초록 + 게이트 도입에 따른 주말 실행 내성(기존 테스트·스모크에 게이트 스텁). 교훈: 복제 화면의 "안 보이는 가드"는 감사 목록으로 만들면 한 커밋에 닫힌다 — 발견(에이전트 감사)→일괄 폐쇄(승인 1회)가 산발 수리보다 빠르고 안전.
- 2026-07-22 · **자동청산 스왑 정산 누락 — 청산 3경로 중 2경로가 meta.swap 미가산** (백로그→사장님 "1번해") — fx_close(수동)만 적립 스왑을 실현손익에 포함하고, fx_sltp(SL/TP)·fx_stopout(마진콜)은 fx_realized_pnl(가격 P&L)만 정산 → 스왑 쌓인 포지션이 자동청산되면 그 스왑만큼 정산 누락(같은 포지션인데 닫는 경로에 따라 다른 돈). 부분 수리 클래스(정산 팀명 매칭 2026-07-19와 동일) — 스왑 도입 때 fx_close만 고치고 "같은 정산이 사는 모든 곳"을 안 셌다. 수정: 두 함수에 `v_pnl := round(v_pnl + coalesce((r_pos.meta->>'swap')::numeric,0),2)` — fx_close.sql:142와 자구 동일. 검증: `fx-settle-swap-pin.test.js` — 함수 본문 단위 정적 핀(RED 실측: fx_close ✅·나머지 ❌ → 수정 후 6/6 GREEN), verify 게이트 편입 = 앞으로 청산 경로가 추가/수정될 때 스왑 누락이 정적으로 잡힘. 배포: 사장님 실행 완료 2026-07-22 — prosrc 실측 sltp_swap_ok=true·stopout_swap_ok=true (청산 3경로 스왑 포함 라이브). 교훈: 불변식("경로 무관 동일 정산")은 코드가 아니라 **테스트로 존재**해야 한다 — 경로가 3개면 핀도 3개.
- 2026-07-22 · **[예방] 서버 세션 게이트 — 주말/휴장 체결을 서버가 거절** (백로그 2번 → 사장님 "고고") — 장마감 차단이 클라(webtrade marketOpen·터미널 fxMarketOpen)에만 있고 서버 fx_open은 무게이트 → 클라를 우회하면 주말 동결가로 체결 가능했던 구멍. DRAFT(fx_open_session.sql)는 심볼 목록을 하드코딩(진실 이중화 — fx_specs와 어긋나면 새 심볼이 게이트를 새거나 막힘)이라 폐기하고, **fx_specs.cls 기반** `fx_market_open(cls, at)` + fx_open v3 통합(세션 게이트 + 기존 슬리피지·마진·멱등·스프레드 전부 보존)으로 재작성. 검증: `fx-session-gate.test.js` — ① 기능 보존 정적 핀 6(통합이 기능 떨어뜨리는 클래스 차단) ② 클라·서버 캘린더 미러를 2주×30분×3자산군 = 2,016표본 전수 대조 + 미 휴일(주식만 닫힘·FX 열림) — RED(파일 부재)→GREEN 10/10, verify 편입. 배포: 사장님 실행 완료 2026-07-22 — 구 DRAFT 잔재로 42P13(파라미터명 변경 불가) 1회 발생 → drop 2줄 선행으로 해소(파일에 자가치유 drop 편입), fx_market_open 실측 토요일 false 확인 = 세션 게이트 라이브. 교훈2: create or replace는 파라미터 이름을 못 바꾼다 — 시그니처 개편 파일엔 drop을 함께 넣는다. 교훈: 게이트의 자산군 판정은 목록 복사가 아니라 **서버의 기존 진실(fx_specs.cls)을 참조** — 목록이 두 곳이면 언젠가 한 곳만 갱신된다.
- 2026-07-22 · **webtrade 마진 표시가 서버와 5배 어긋남 — 사용자 레버리지로 사용중 마진 계산** (차이감사 ③ 반전 발견 → 사장님 "고고") — 서버의 사용중 마진(마진 게이트 v_used·스탑아웃 공용)은 `Σ notional/fx_lev_cap(cls)`(하우스 캡, FX 500:1 고정)인데 webtrade는 사용자 레버리지 설정(기본 100:1)으로 계산 → 화면 마진 5배 부풀림·프리마진 축소·마진레벨 왜곡 ("화면은 위험이라는데 서버는 건강" — 스탑아웃 판정과 다른 숫자를 고객에게 보여줌). 터미널은 처음부터 캡 기준이라 정확했음. RED 실측: 1.0랏 EURUSD·레버리지 100 → 화면 1,149.85 vs 서버 230. 수정: `usedMarginOf(symbol,vol)=requiredMargin(…,LEV_CAP)` 헬퍼로 3곳(엔진 usedMargin·하단바 marginUsed·Exposure marg) 통일 — 사용자 레버리지는 서버 v_new_margin 규칙 그대로 **신규 주문 증거금(marginFor)에만** 유지. 검증: `webtrade-margin-server-lockstep.test.js` 7핀(RED ≈1,150 → GREEN ≈230 실브라우저 + Free=Equity−Margin 항등식 + 신규주문 $11.50 유지 + 소스 4핀), 구표준을 핀하던 기존 테스트 3개(margin·exposure·terminal-tabs)를 새 표준으로 갱신, verify 🟢. 교훈: "사용중 마진"의 진실은 화면 설정이 아니라 **서버가 청산 판정에 쓰는 그 수식** — 리스크 숫자는 서버 공식의 미러만 허용(플로팅=실현 원칙의 마진판).
- 2026-07-22 · **[기능] 대기주문 서버 매칭 엔진 M4.5** (백로그 4번 → 사장님 "gogo") — 기존엔 대기주문 감시·체결이 클라(trading.html)에만 있어 **브라우저를 닫으면 영영 안 터지는** 구조(클라 정산 금지 원칙의 대기주문판). 서버로 이관: `fx_place_pending`(방향 검증 — Buy Limit 아래/Buy Stop 위/Sell 반전 + sl/tp 트리거 기준 검증, 멱등)·`fx_cancel_pending`·`fx_pending_fill`(스위프: MT5 트리거 4종, 장마감/신선도 fail-safe 스킵, **pending→filled 원자 선점**, 마진 부족 시 rejected+reason 보존)·`fx_sweep_all`(에지 초단위용 fx_sltp+대기 한 방). 구조 핵심: **체결 코어를 fx_fill_internal 하나로 분리** — fx_open v4는 auth+세션 게이트만 하는 얇은 래퍼가 되고 시장가·대기가 같은 코어로 체결(스프레드·신선도·멱등·마진 자구 동일 — "부분 복제" 클래스 원천 차단). 이중 체결 구조적 불가: 포지션 local_id=대기 local_id라 구클라가 경합해도 멱등 1회. 터미널: 접수(4종, Stop Limit은 v2 정직 거절)·취소 ✕·Trade 탭 서버 대기 섹션·차트 주황 라인·Realtime(fx_pending). 검증: `fx-pending-engine.test.js` 13핀(단일 엔진·원자 선점·rejected 보존·fail-safe·진리표 8케이스 미러·터미널 4핀) + 구 "정직 거절" 핀을 M4.5로 갱신 + verify·스모크 🟢. 배포 완료 2026-07-22 + 실전 끝-끝 실증: Buy Limit 0.10 EURUSD @1.14113 접수(fxt-1784731604424-537731) → 가격 교차 → 서버 체결 → 같은 local_id 포지션 @1.1411 전환 실측. 배포 중 실전 발견 1건: 레거시 fx_pending이 PK 없이 Realtime 발행에 등록돼 있어 UPDATE가 55000(replica identity 부재)으로 거부 — 스위프의 filled 선점이 매번 실패. `replica identity using index fx_pending_acct_localid_uidx` 1줄로 해소(파일 편입). 교훈: Realtime 발행 테이블은 PK/replica identity가 UPDATE의 전제 — 발행에 넣을 때 함께 검사한다. 잔여(선택): 에지 sweep을 fx_sweep_all로 교체하면 대기 체결도 초단위.
- 2026-07-22 · **feed-liveness 차트 캔들 오탐 — 6초 단발 비교가 평평한 장을 FROZEN으로 오판** (크론 빨강 메일, 사장님 "1분26초 동안 피드가 멈춘거야?") — 실제 로그: 피드는 전부 초록(행 7/8 이동·틱 0s 신선·FX 이동)인데 W4 차트 마지막 캔들이 6초간 66026.76→66026.76 동일 → FROZEN 판정. BTC가 2dp 기준 6초 평평한 건 조용한 장에서 흔함 — 관찰창이 너무 짧고 "기초 피드가 그 사이 움직였는가"를 대조하지 않은 게 원인(다음 회차는 초록 = 일시 오탐). 참고: 이메일의 "Failed in 1m26s"는 작업 소요시간이지 피드 정지 시간이 아님. 수정: 같으면 8s×3 재관찰 + mk.lastTick 증가 대조 — "피드는 틱이 왔는데 캔들만 그대로"일 때만 빨강, 평평한 장은 'flat market'으로 정상 통과. 교훈: 움직임 검사는 **비교 대상의 변화 원인(기초 피드)이 실제로 움직였는지**와 짝지어야 한다 — 파생값 단독 비교는 조용한 시장을 고장으로 읽는다(C1 v2·window.mk와 같은 늑대소년 방지 원칙). ⚠️ 크론은 main에서 돌므로 main 반영 필요(사장님 승인 대기).
- 2026-07-22 · **MLB 더블헤더 4경기 잠금 — 유일매칭 동점 fail-safe의 커버리지 손실 + 잠재 오배당 발견** (아침 점검 "회수가능 4건" → 사장님 "고고") — 같은 팀 조합이 같은 날 2경기(더블헤더)면 오즈 매칭의 "유일 히트" 규칙이 동점으로 실패해 프로바이더에 실배당이 있어도 둘 다 잠김(±6h 창인데 두 경기가 정확히 6h 간격). 조사 중 잠재 버그 추가 발견: 프로바이더가 1차전 배당만 낸 경우 구규칙은 그 단일 히트를 **2차전에도 부착 가능**(±6h 내) — 오배당 위험이 원래 있었음. 수정(sports-games Edge): **상호 최근접(mutual nearest)** — 복수 히트는 킥오프 최근접(동률=잠금), 선택된 이벤트가 같은 팀 조합의 다른 경기에 더 가까우면 양보(잠금) = 커버리지 회복과 오배당 차단을 동시에. 시각 미상은 구규칙 보수 유지. 검증: `sports-doubleheader-match.test.js` 9핀 — 구규칙 RED(2히트→잠금) 재현 + 신규칙 진리표(각자 제 짝·1차전만 존재 시 2차전 잠금·동률 fail-safe·단일 경기 회귀), 소스핀 3, verify·feed-check 🟢. 배포 완료 2026-07-22 + 실측: 첫 크론에서 오늘 MLB 잠금 4→0 (17경기 전부 실배당), feed-check 회수가능 0·🚨샘 0 — 1차전 시작 전 해제로 고객 베팅 가능. 교훈: fail-safe는 옳지만 **동점의 원인(더블헤더)이 규칙적 패턴이면 판별 정보(킥오프 근접)를 규칙에 승격** — 그리고 모호성 조사는 반대 방향(단일 히트의 오귀속)도 같이 본다.
- 2026-07-22 · **[기능] 스파이크 워터마크 v1 — 틱 사이 순간 고저까지 SL/TP 판정 + 레벨가 정산** (설계 ec455b7 → 사장님 "1번 고") — SL/TP 판정이 기록된 틱(FX 1초)만 봐서 사이를 스친 스파이크를 놓치던 해상도 한계 해소. 구현: prices에 tick_hi/tick_lo 2열, fx-stream이 스로틀로 버려지던 프레임의 mid 고저를 누적해 기록(기록 후 리셋 — 유령 발동 금지), fx_sltp가 coalesce(lo,mid)/coalesce(hi,mid)로 교차 판정 — mid 교차는 기존대로 시장가 정산, "스침"(워터마크만 교차)은 **레벨가 정산**(fx_realized_pnl에 p_close_override, 구 4-인자 drop으로 모호성 방지) + detail '~WM' 감사 표기. 크립토/폴백은 hi=lo=mid=현행 동일(안전 저하 0, 배포 순서 무관). v1.1 이월: 대기주문 워터마크(리밋이 레벨보다 불리 체결되는 것 방지하려 fill 오버라이드 필요). 검증: `fx-watermark.test.js` 14핀(소스 7 + 판정·정산 미러 7 — 폴백 불변·스침 레벨·mid 시장가·반대방향 미발동) + 스왑 3경로 핀 유지 + verify 🟢. 배포 완료 2026-07-22 — prosrc 확인 + 라이브 실측: EURUSD tick_hi 1.140995/tick_lo 1.14098 (1초 창 실고저 캡처 확인). 이후 뉴스 스파이크 장세에서 settlements detail의 SL~WM/TP~WM이 실전 첫 발동 증거가 된다.
- 2026-07-22 · **[기능] 대기주문 워터마크 v1.1 — 스파이크가 스친 리밋/스탑도 서버가 잡아 체결, 지정가보다 나쁜 체결 구조적 불가** (사장님 "고고") — v1이 열린 포지션 SL/TP만 워터마크로 잡고 대기주문은 현재 mid만 봐서, 뉴스 스파이크가 트리거를 스치고 복귀하면 리밋이 안 터지는 비대칭("돈구멍"의 마지막 잔여)이 남아 있었다. 구현: fx_pending_fill이 tick_lo/tick_hi 창에 같은 half를 적용한 창 내 최우호 bid/ask로 판정(BUY LIMIT lo+half≤tr · BUY STOP hi+half≥tr · SELL 반전) — 현재가 교차는 기존대로 시장가 체결, "스침"(워터마크만 교차)은 **트리거 레벨가 체결**(fx_fill_internal에 p_price_override 10번째 인자, 구 9-인자 drop = 42725 모호성 방지 — fx_realized_pnl 교훈 재적용) + fx_pending.meta {wm:true, fill_px} 감사. 워터마크 null(크립토·폴백)이면 판정·체결 자구 동일(폴백 불변). LIMIT은 레벨가=지정가·시장가=지정가 이상 유리라 **나쁜 체결이 표현 불가능**(불법 상태 제거 원칙). 검증: fx-pending-engine.test.js 27핀(기존 19 + v1.1 정적 6·미러 8: 폴백 불변 2·스침 4방향·유령 체결 금지·LIMIT 30조합 속성) + verify 🟢. 배포는 사장님 (fx_pending_engine.sql 전체 재실행, 재실행 안전).
- 2026-07-22 · **[기능] 터미널 차트 깊은 이력 1,000→15,000봉** (사장님 "고고") — 레전드 터미널(dev/fx-terminal.html)의 실봉 로더가 1,000봉 고정이라 줌아웃하면 과거가 금방 끊겼다 (webtrade는 2026-07-13에 HIST_N으로 해소 — 터미널만 미이식). 구현: HIST_N을 webtrade와 **자구 락스텝**(M1/M5 5000 · M15/M30 10000 · H1/H4 15000 · D1 3900 · W1 1560 — H4≈6.8y·D1≈15y), FX=fx-prices ?candles n≤15000(서버 페이지네이션 기배포), 크립토=Binance klines 요청당 1000봉 한계라 endTime 역보행 최대 5페이지(webtrade 동일 한도), 타임아웃 8s→10s(webtrade 동일). 부수 발견: 라이브 봉 추가부의 1,500봉 트림이 실이력도 갉아먹는 구조 → `!s.real` 조건으로 합성 시리즈에만 적용. 검증: fx-terminal-real-candles.test.js 14핀(신규 4: HIST_N 락스텝 자구 비교·n=HIST_N 요청·트림 제외·페이지네이션 5×1000 역보행 엄격 오름차순) + verify 🟢. 비돈(읽기 전용 시장데이터) — 수락 게이트(≥160봉·오름차순) 불변.
- 2026-07-22 · **[기능] 터미널 다국어(en/ko/ja/zh) — webtrade I18N 이식** (사장님 "고고") — 터미널에 셸 문구만 있던 i18n 스캐폴드(LANG/t/applyLang/data-i18n, Settings>Language 버튼)에 webtrade 사전을 병합하고 동적 표면에 t()를 배선: 시세창 하위탭·컬럼 헤더, 툴박스 12탭·Trade/Exposure/History 표 헤더·계좌 바(잔고/순자산/마진/여유 마진/마진 레벨), ☰ 메뉴 전체, 주문·수정 창 라벨, 상태바·Toolbox 세로 라벨, 장마감 토스트. applyLang이 언어 전환 시 탭 라벨(data-sub/data-tab=영문 키) 재도장 + mwRender/renderToolbox/__drawMenu 재호출 = 새로고침 없이 즉시 전환. 함정 2개 회피: ① map 콜백 변수 t가 전역 t()를 가리는 셰도잉(nm으로 개명 후 래핑) ② data-* 키는 영문 유지(로직·테스트는 키로, 표시만 번역). fr은 셸 문구만(부분 폴백). 검증: fx-terminal-i18n.test.js 12핀(기본 en·ko 6표면·ja/zh 스팟·localStorage alpexa.dash.lang 공유·en 복원) + verify 🟢. 비돈(표시 전용) — 거래 로직·키·RPC 인자 무변경.
- 2026-07-22 · **[구조] trading.html 구식 클라 대기주문 감시 폐쇄 — "진실은 한 곳"(#5) 완결** (READ-BEFORE-WRITE 표 승인 후 "고고") — 서버 엔진(M4.5) 가동 후에도 구 모바일 클라가 700ms 인터벌로 대기주문을 자기도 판정(표시가·워터마크 없음)하고, 히트 시 fx_pending 행을 **status 무관 delete**(서버의 filled/rejected 감사 행·meta.wm까지 삭제 가능) + fx_open 호출(마진 거절 시 주문이 토스트 한 번으로 증발), 접수도 테이블 직접 upsert(방향 검증 우회)였다. 멱등(local_id) 덕에 이중 체결은 원래 불가 — 문제는 감사 유실·검증 우회·판정 불일치. 폐쇄: ①접수→fx_place_pending RPC(검증 거절 시 로컬 행 회수+사유 토스트) ②취소→fx_cancel_pending(pending만, 감사 보존) ③수정→취소+새 local_id 재접수(멱등 on-conflict가 cancelled 키 점유로 재-upsert를 침묵 무시하는 함정 회피) ④클라 트리거 감시 블록 삭제(같은 인터벌의 마진콜 감시는 유지) ⑤체결/거절 UX는 fx_pending Realtime status 전환으로(filled→토스트·사운드·알림, rejected→사유). 재발 방지: diagnose `PEND-client-table-write`(CRITICAL, 전 클라 파일) + trading-pending-server-only.test.js 10핀. 트레이드오프: 비로그인 대기주문은 체결 안 됨(종전에도 fx_open 거절로 소멸+에러라 실질 동일). verify 🟢.
- 2026-07-22 · **[구조] 모바일 앱 SL/TP 서버 저장·집행 — "앱 꺼지면 안 터지는 손절" 폐쇄** (사장님 질문 "모바일에는 스탑로쓰가 없는거여?"에서 발견 → "고고") — trading.html이 fx_modify를 한 번도 안 불러 SL/TP가 **폰 메모리에만**(localStorage도 아님 — 새로고침에 증발) 있고, 집행은 클라 1s 감시(앱 열려 있을 때만·워터마크 없음)뿐이었다. SL/TP 3부작에서 웹트레이드·터미널만 고치고 모바일이 남았던 것. 수정: ①설정·수정 = fx_modify RPC(낙관 반영→거절 시 원복+사유) ②시장가 주문 SL/TP는 fx_open 성공 직후 결정적 부착(local_id 확정, 터미널 차이감사 #4 패턴) ③표시 진실 = positions.meta(pullPos가 meta adopt/reconcile + 6s 낙관 유예 — 기기 간 SL/TP 동기화 부수 해결) ④클라 SL/TP 자동청산 판정 삭제 → 서버 fx_sltp 단독(24h 초단위+워터마크 스침+레벨가 정산) ⑤알림 = positions Realtime status='closed'(이 기기발 청산은 __fxClosing 20s 표식으로 중복 방지). 클라 스탑아웃·클라 정산 폴백은 별도 항목(잔여 갭 2)으로 승인 대기. 검증: trading-sltp-server.test.js 10핀 + pending 게이트 10핀 유지 + verify 🟢. 교훈: "같은 기능이지?"라는 사용자 질문은 표면 간 차이 감사 트리거 — 대답 전에 grep으로 실측(fx_modify 0건이 즉답).
- 2026-07-23 · **[구조+발견] 모바일 클라 청산 잔재 3종 폐쇄 — "돈은 서버만"(#5) 모바일 완결** (사장님 "1번 고") — 승인받은 2건(클라 스탑아웃·클라 정산 폴백)을 걷어내다 READ-BEFORE-WRITE grep에서 **3번째 미인지 경로 발견**: push()가 3초마다 열린 포지션을 `status:'open'` 통째 **upsert** → 서버(fx_sltp/fx_stopout)가 방금 청산한 행을 경합 창(≤4s)에서 도로 open으로 **부활**시킬 수 있었다 = 재청산 시 **이중 정산 위험**(서버 SL/TP 청산이 일상이 된 지금 실위험). 폐쇄 3종: ①clientClose/bankFallback/__alpexaSettle 삭제 — fx_close 실패 시 클라 가격·클라 P&L로 settlements insert + positions delete 하던 폴백 → 이제 실패는 정직하게 실패(UI 원복+사유 토스트, 서버 행 무변경=돈 무이동) ②클라 스탑아웃(50%·표시가 자체판정) 삭제 — 강제청산은 서버 fx_stopout(30%) 단독, 클라는 마진콜 경고까지만 ③push() upsert → **pnl-only UPDATE + eq(status,'open')** — 닫힌 행을 구조적으로 못 건드림(부활 불가능). 부수: 호출처 0인 __fxSyncDelete(클라 positions delete)도 제거. 재발 방지: diagnose `FX-client-settlement-insert`·`FX-client-position-statuswrite`(CRITICAL, FX 클라 3파일) + trading-close-server-only.test.js 10핀. verify 🟢. 교훈: 승인받은 항목을 지우러 들어가도 grep 표는 다시 그린다 — 3번째 경로는 표를 다시 그렸기 때문에 보였다.
- 2026-07-23 · **[기능] Legend 1/2 체제 — 다크 폐지 + 레전드 터미널 정식 승격** (사장님 "고고") — webtrade의 테마 토글(다크/레전드)을 **버전 스위처**로 교체: View 메뉴 = "Legend 1 — Classic"(webtrade 레전드 룩, 체크)/"Legend 2 — Terminal"(터미널로 이동). 기본 테마 레전드 고정 — 저장 설정(cfg.theme)으로 다크 복귀 불가, ?theme=dark는 개발 백도어로만 잔존(다크 CSS는 삭제 않고 잠재움 = 즉시 복구 가능). 터미널 dev/fx-terminal.html → **terminal.html 정식 승격**(구 경로는 쿼리 보존 리다이렉트 스텁, login.html dest2 라우팅·테스트 13개·diagnose 파일 목록 일괄 이관, visual-smoke에 terminal.html 추가). 마지막 사용 버전 기억 = `alpexa.fx.version`: 터미널 로드=legend2 기록 → webtrade 진입 시 자동으로 터미널로(stay=1 탈출구), 터미널 Settings "Legend 1" 클릭=legend1 기록 후 webtrade로. **함정 1개 실제로 밟을 뻔**: location.replace 후에도 다음 줄이 실행되므로 legend1 기록을 else로 안 감싸면 리다이렉트 직전에 legend2 기억을 지워버림 — else 필수를 핀으로 고정. 스모크 노이즈 필터에 WebSocket 차단(CI 프록시) 추가. 검증: fx-version-switch.test.js 9핀 + 테마 게이트 2종 재계약 + verify 🟢. 비돈(표시·내비게이션 전용).
- 2026-07-23 · **[기능] 레전드 1(webtrade) 차트 트레이드-레벨 드래그 — 진입선 끌어 SL/TP 설정** (사장님 "밑으로 땡기면 스탑로쓰 설정 없는데") — 터미널(레전드 2)에만 있던 commitSltpDrag를 webtrade에 이식. ①서버 저장 SL/TP(meta)가 차트에 점선 표시(빨강 sl/초록 tp — 기존엔 진입선만 그려서 걸어둔 레벨이 차트에 안 보였음) ②진입선/레벨선 6px 드래그 → 방향 판정(롱 아래=SL·위=TP, 숏 반전 — 터미널과 자구 동일 규칙) + 미리보기 라인 ③커밋 = fx_modify(반대 레벨 보존·digits 라운딩), 거절 시 에러음+Journal+서버 진실 재도장(원위치 복귀 시각화) ④capture-phase로 LWC 팬과 충돌 차단, 그리기 도구 활성 시 양보, 데모 제외. 검증: webtrade-sltp-drag.test.js (7계약, 터미널 락스텝 핀 포함 — 한쪽 규칙 바뀌면 🔴) + verify 🟢. 돈 semantics 신규 0 — 기존 승인 경로(fx_modify 서버 검증→fx_sltp 집행)에 입력 UX만 추가.
- 2026-07-23 · **[기능] 주식 유니버스 확장 — 터미널 7→36종, webtrade 기본뷰 +10** (사장님 "스페이스엑스 포함 AI·반도체·나스닥100") — READ-BEFORE-WRITE 조사 결과 서버는 이미 완비 상태였다: stock-prices/stock-stream 에지가 36종을 펌프 중(SPACEX는 Finnhub 'SPCX' 매핑), fx_specs에 STOCK 36종 전부 등재(거래 관문 통과), 실측 프로브로 36종 전량 1분 신선 확인. 병목은 클라 하드코딩 — 터미널 MW_SYMBOLS.Stocks 7종 고정, webtrade 기본뷰(TOP20) 주식 8종. 확장: 터미널 Stocks 36종(메가캡→SPACEX→반도체 14→AI소프트 8→퀀텀 5→JPM 순) + STOCK_NAME/MW_BASE(실측가 시드), webtrade TOP20에 AI·반도체 리더 10종(AMD·AVGO·TSM·ASML·ARM·MU·QCOM·PLTR·SMCI·NFLX — 전체 풀은 Stocks 탭/검색이 이미 서빙) + 부팅 기본·SYM_NAME·BASE 동기. 모바일(trading.html)은 기확장 상태라 무변경. 핀 수선 1: fx-terminal-live-feed의 고정 '22 symbols' 카운트를 MW_WATCH.length 동적 비교로(리스트 확장마다 깨지는 매직넘버 제거). verify 🟢. 비돈 — 서버 피드·스펙 무변경, 표시 목록만.
- 2026-07-23 · **[기능] 레전드 2 라이트 = "민트 모던"(시안 A) 채택** (사장님 "색깔이 아직 별루야…로빈후드처럼 모던한" → 시안 3종 실물 비교 → "A 고") — 근거 있는 재설계: ①구 브랜드 캔들 페어(#00C805/#FF5000)는 **흰 배경에서 적록색약 구분 실패**(검증기 ΔE 3.6 deutan — dataviz 6체크 실측), 라이트 전용 페어 #00B386/#FF4D4F로 교체(전 체크 통과) ②"모던"의 본질은 색이 아니라 구조 — 위젯 경계선 제거 + 라운드 12px + 이중 그림자(로빈후드 라이트의 실제 문법) ③배경 쿨톤 #F5F7FA·잉크 #111827. 구현: body.light 변수+카드 CSS(라이트 스코프 — 다크 무변경), 캔들 상수 const→let + applyChartPalette()가 테마 전환 시 재대입+renderAllCharts (다크=브랜드색 유지, 라이트=민트). 교훈: "색이 별루"라는 감각 피드백도 검증기로 계량하면 원인이 나온다(저대비·색약 실패였음) — 감으로 고르지 말고 validate_palette 돌리기. verify 🟢.
- 2026-07-23 · **[기능+발견] 레전드 2 MT5 사운드 전면 배선 — "그동안 무음"의 원인 제거** (사장님 "사운드 전부 넣어줘") — 조사에서 발견: 터미널 fxSnd는 .wav 파일 재생만 시도하는데 **/assets/sounds/에 파일이 0개**(README뿐) → 기존 12개 호출부 전부 조용히 실패 = 터미널은 사실상 무음이었다(webtrade는 WebAudio 합성음 폴백이 있어 소리가 났음). 수정: webtrade 사운드 엔진 락스텝 이식 — 합성음 8종(체결 칼틱·청산 확인음·거절 부저·SL/TP 삐삐·대기 또록·수정/취소 틱·알림 딩동·로그인 차임), 파일이 생기면 자동으로 .wav 우선. 신규 배선: 데모 체결/청산/대기/취소, LIVE 수정·취소(성공 틱/거절 부저), 프라이스 알림 히트, 로그인 차임(authed 전환 1회), 첫 제스처 AudioContext 언락(모바일). 경로도 루트 승격 정합(../assets→assets). 검증: fx-terminal-sounds.test.js 12핀 + verify 🟢. 교훈: "사운드 넣어줘"류 요청은 먼저 **왜 안 들리는지**부터 — 호출부는 다 있었고 에셋이 없던 것.

## 2026-07-24 — 대시보드 오픈벳 leg 시간에 날짜 누락 (먼 경기가 임박처럼 보임)
- **증상**: SP-100001(9/10 NFL NE@SEA)이 "Thu 12:20 AM"으로 표시 → 사장님이 "왜 정산 안 됐냐" 오해. 실제 미정산 아님(경기 미시작).
- **근본원인**: sports-games Edge의 time 문자열이 요일+시각뿐(날짜 없음)인데, 대시보드 오픈벳/슬립 렌더가 저장된 `l.tm`을 원문 그대로 출력. 앱(sports-live)엔 이미 `legTimeLabel`(kt→피드 iso 재포맷)이 있었지만 대시보드에 이식 누락.
- **수정**: sports-dashboard.html `legKick()` — kt → 현재 피드 iso → tm 폴백 순으로 날짜(요일·월·일) 포함 재포맷. 오픈벳(1302)·슬립(1617) 적용.
- **핀**: tests/sports-dashboard-bets.test.js "먼 경기 leg = 날짜 포함 라벨" (RED→GREEN 실측 — 수정 전 "Thu 12:20 AM" 원문 노출 확인).
- **클래스**: 표면 간 헬퍼 비대칭(앱엔 있는 로직이 대시보드에 없음) — 앱↔대시보드 락스텝 감사 시 시간 라벨도 체크 항목.

## 2026-07-24 — [기능] 스포츠북 데스크 Phase 1 (sportsbook-desk.html)
- **경위**: 실고객 오픈 결정 → "리스크 감시·헤징·관리감독 백오피스" 지시. 레전드2(RH 다크) 룩 + 위젯 그리드.
- **구조**: 읽기 전용 — 어드민 RPC `sbdesk_report`(is_admin 게이트, sports_liability/sports_pnl 재사용) + 공개 live_games 조회. 새 시크릿/Edge 0. 어드민 세션 = manager 락스텝(ALPEXA_ADMIN_SESSION 격리 키).
- **위젯 9**: 전광판·노출 매트릭스·헤징 보드·알람·베팅 티커·고객 리스크(SHARP)·정산 큐(조회)·P&L(CSV)·라인 뷰어.
- **개발 중 잡은 결함**: 헤징 반대편 자동판별이 팔레이(픽≠ML sel)에서 아무 쪽이나 집음 → 픽 정확 일치 시만 계산, 불일치는 "수동 검토" 표기 (테스트 RED가 잡음).
- **핀**: tests/sportsbook-desk.test.js 23계약 (READ-ONLY 구조 핀 — 쓰기 호출 0·돈 RPC 0·service_role 0 / SQL is_admin·revoke / 렌더·헤징 수식·XSS). diagnose DEPLOYED + visual-smoke PAGES 등재.
- **대기**: Phase 2(한도 서버 강제 — READ-BEFORE-WRITE 승인 필요) · Phase 3(개입 도구+어드민 감사 로그). SQL 배포 = 사장님 (sportsbook_desk.sql).

## 2026-07-24 — 🔴 백업이 3주+ 동안 "초록 빈 파일" (실측 발견) + 재설계
- **증상**: daily-backup 액션 매일 success인데 어제 아티팩트 2,478바이트. 로그 실측: players/accounts/positions/settlements 전부 **0행**, ledger는 목록에 아예 없음.
- **근본원인 4중**: ① GitHub 시크릿(SUPABASE_URL/KEY) 미설정 → publishable 키 폴백 ② RLS 잠금이 anon 읽기를 막음(정상 동작) ③ 스크립트가 실패를 세고도 exit 0 (fail-open) ④ 애초에 PUBLIC 레포 아티팩트로 데이터를 올리는 설계 = 작동했다면 고객정보 유출.
- **재설계**: 데이터는 Supabase 안에서만 — `backup_snapshots.sql`(backup 스키마 = PostgREST 미노출, 매일 09:00 UTC pg_cron, 멱등, 14일 보존, ledger 포함 11테이블) + `backup_status()`(admin/service). GitHub 액션은 **검증 전용**(행수 카운트만, 데이터 저장 0, 실패=빨강 이메일) + `uptime-check.yml` 신설(매시 사이트·REST·Auth 3층 프로브, 침묵 게이트).
- **핀**: tests/backup-verification.test.js 15계약 (fail-closed·유출경로 제거·멱등·보존·권한·프로브) + 데스크 알람 백업 신선도 2계약.
- **교훈(클래스)**: "성공 뱃지"는 증거가 아니다 — 아티팩트 크기/행수 같은 **실물**을 봐야 한다. 침묵 게이트는 반드시 fail-closed로.
- **대기(사장님)**: ① backup_snapshots.sql 배포 ② GitHub 시크릿 2개 설정 ③ main 브랜치에 파일 3개 반영.
