node_modules/

# Local/CI database backups (never commit customer data)
backup/

# Python AI-harness prototype (local experiment — never part of the static-site deploy)
harness-proto/

# generated daily reports (regenerated by the report harness)
reports/alpexa-daily-*.json
