# 마스터 감사 프롬프트 — 4앱 100% 완성도 (50년차 기준)

> 한 번 "돌리면" 4앱(Crypto·FX·Sports·BackOffice)을 5개 차원으로 전수 감사한다.
> 기존 도구를 오케스트레이션 + 보안/업계기능 카탈로그 추가. 결과 = 우선순위 발견목록.

## 0. 대상 & 차원
- **앱:** crypto-live.html · trading.html(FX) · sports-live.html · manager-mobile.html(백오피스)
- **차원 7:**
  1. **돈 정확성** (머니프린팅·이중차감·우회) → `tests/diagnose.js` + `tests/*.test.js`
  2. **데모/가짜 데이터** (진짜처럼 보이는 seed) → `tests/diagnose-ui.js` UI-demo-balance + `diagnose.js` **`DEMO-hardcoded-balance`**(하드코딩 `$N,NNN.NN` 잔고/가격 = 유령 데모계좌) + **`DEMO-fake-email`**(alpexa-demo.com·@alpexa.app). **모든 사용자-노출 파일**(앱+index+site/) 스캔. 돈/가격은 **서버에서**(`usd(...)`/`${...}`) — 정적 숫자 금지. (실제: 로그인 전/폴백 화면이 $1.18M·$11,248·$5,000 가짜잔고를 보여줬다.)
  3. **죽은/미완성 기능** (보이는데 안 함) → `diagnose-ui.js` UI-stub + `UI-감사-프롬프트.md`(추적)
  4. **빠진 기능** (업계 표준 대비) → `기능-완성도-프롬프트.md` 카탈로그
  5. **보안** (키노출·인증·권한·입력검증) → 아래 보안 체크
  6. **표시 일관성 / 단일소스 / 불변성** (#20·#21) — 같은 잔고/숫자가 **두 값**으로 보이거나 **두 소스**에서 읽히면 결함(#20). 그리고 **잠긴 진실(체결가·잠금잔고)은 절대 덮어쓰면 안 됨**(#21): 한 가변 필드가 "진실+라이브"를 겸하면 라이브 코드가 진실을 오염시킴 → **잠긴 값=write-once/읽기전용, 라이브 값=저장 말고 계산(derive)**. → `diagnose-ui.js` UI-balance-dual-source + `diagnose.js` BET-locked-odds-overwrite + 아래 §2
  7. **모바일 생명주기 / 시간 기반** (#26) — 정적/노드 테스트가 **못 잡는** 클래스. 핸드폰은 백그라운드면 `setInterval`/`setTimeout`을 **정지**시킨다 → 시간 카운트는 **틱 누적 말고 타임스탬프 차(`Date.now()-last`)**로 재라. **복귀(visibilitychange/focus/pageshow)는 "활동"이 아니다** — 리셋하면 자리 비운 시간이 사라져 자동로그아웃·자동갱신·세션만료가 영영 안 터진다(#26 모바일 자동로그아웃 먹통). 잠금화면·탭전환·앱전환·기기절전 후 **복귀 시나리오를 반드시** 본다. 데스크탑만 보면 통과해도 모바일에서 깨진다. → `tests/idle-logout.test.js` + 아래 §2

## 1. 자동 스캔 (먼저 돌린다)
```
node tests/verify.js          # 파싱 + 돈 회귀테스트 + diagnose(돈) + diagnose-ui(데모/스텁)
node tests/diagnose-ui.js     # 데모/스텁 상세
```
보안 grep 스윕:
- `service_role|eyJ...JWT` (키 노출) · `eval(` · 하드코딩 주소(`bc1|0x|[13]…`)
- `if (CRON_SECRET &&` (fail-open 크론) · `.rpc(` 호출 vs RLS 잠금
- `innerHTML\s*=` (XSS) · `localStorage.setItem.*alpexa\.(balance|...)`(돈 로컬)

## 2. 추적 감사 (앱·화면마다 — 정적이 못 잡는 것)
각 앱의 모든 화면에서 `UI-감사-프롬프트.md` B1~B4 적용:
- 모든 **버튼/토글** → 핸들러 실동작? 죽은 것?
- 모든 **숫자** → 서버 실데이터? 가짜?
- **단일소스 추적 (#20):** 화면에 보이는 **모든 돈 값을 소스까지 역추적**한다. 같은 논리값(예: 스포츠 잔고)이 **2+ 소스**(`serverBalances` prop vs `window.__srvBal`)에서 읽히거나, 한 화면에서 **두 곳에 다른 값**으로 보이면 결함. (실제: Account "Sport $0" vs 드롭다운 "$254") → 단일소스로.
- **불변성 / 덮어쓰기 추적 (#21):** **잠긴 진실**(체결 배당·진입가·잠금잔고·스테이크 원금)이 라이브 코드로 **재할당(`X = live`)** 되는 곳을 찾는다. 놓인 베팅/포지션 표시값이 라이브에서 파생되면(예: 티켓 배당이 시장 따라 흔들림) 결함. 잠긴 값은 한 번 정하고 **다시 안 씀**, 라이브는 **계산**. (실제: 캐시아웃이 `l.am=cur`로 베팅 배당 덮어씀 → 티켓 -103↔-100)
- 모든 **기능섹션** → 끝까지 연결? 스텁?
- **빈상태** → 가짜 기본값 누출?
- **돈이 나가는 모든 경로는 같은 가드 (#24):** 출금만 막고 **이체를 안 막으면** 우회된다 (이체→타서버→출금). 출금에 `withdrawable`/보너스/한도 가드가 있으면 **이체에도 동일 가드가 서버 RPC(`app_transfer`)에 있어야** 한다. 클라 Max/available은 전체잔고가 아니라 **transferable(=withdrawable)** 기준. (실제: 보너스가 이체로 새 출금됨.)
- **인코딩은 디코드로 검증 (#27):** QR·바코드·체크섬·시그니처 등 "인코딩된 산출물"은 **눈으로 그럴듯하면 통과 금지** — 실제로 **디코드해서 원본과 일치**하는지 본다. (실제: 받기 QR이 랜덤 패턴이라 아무 스캐너도 못 읽었는데 QR처럼 보여서 통과됐었음.) 생성기에 `Math.random`/해시-난수로 셀을 찍는 게 보이면 가짜 의심.
- **모바일 복귀 시나리오 (#26):** 시간/세션 기반 기능(자동로그아웃·세션만료·자동갱신·유휴타이머)을 찾으면 **잠금→오래 방치→복귀**를 직접 돌려본다. (1) 백그라운드에서 타이머가 멈춰도 동작하나(타임스탬프 차로 재나)? (2) 복귀가 시간을 **리셋**하진 않나? 데스크탑(화면 안 꺼짐)만으론 통과한다 — **반드시 모바일/visibilitychange로** 확인. 라이브로 못 띄우면 `visibilitychange`/`focus` 이벤트를 쏴서 헤드리스로라도 검증.

## 3. 빠진 기능 (업계 표준 카탈로그 대조 — `기능-완성도-프롬프트.md`)
- **Crypto:** 멀티코인 송수신·스왑·스테이킹·내역·주소록·가격알림·2FA·출금화이트리스트·KYC
- **FX:** 주문타입·SL/TP·마진·포지션·관심목록·캘린더/뉴스·청산
- **Sports:** 프리/라이브·캐시아웃·팔레이/SGP·내역·책임도박·라이브스탯
- **백오피스:** 고객/계좌·승인·포지션·정산·리스크·한도·로그·에이전트·KYC심사
- **공통:** 알림 실발송·i18n·빈상태/에러·접근성·세션/보안

## 4. 발견 → 분류 (3태그)
- 우선순위 P0/P1/P2 · 범위 🟦프론트만/🟥백엔드 · 돈안전 🚨
- **🟦+안전** → 바로 고침. **🟥/🚨** → 가짜 금지, 요건 정리 보고.

## 5. Control 루프
- 새 버그클래스 → `결함-로그.md` + 테스트/diagnose 체크. `verify.js` 🟢 후 "완료".

> **100% 정의:** 보이는 모든 기능이 진짜 작동 or 정직한 "준비중". 가짜 기능 0. 돈 우회 0.

---
## 실행 로그 (돌릴 때마다 결과 요약 추가)
- (최초 실행 결과는 채팅 발견목록 참조 — `완성도-작업리스트.md`에 반영)
