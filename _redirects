# Cloudflare Pages only (GitHub Pages ignores this file — alpexa-sports.com unaffected).
# alpexa-fx.com serves this same repo via Cloudflare Pages; its landing page is the FX
# site, so the root path REWRITES (200 = URL stays alpexa-fx.com) to fx.html.
/  /fx.html  200
