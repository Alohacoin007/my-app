# 스포츠 앱 문법 스펙 — 대시보드(sports-dashboard.html)가 복제해야 할 계약

> 원본에서 발굴한 **구조·라벨·데이터 계약**. 모든 항목에 file:line 근거.
> 디자인(색·폰트)은 레전드-디자인-스펙.md 소관 — 여기 없음.
> 라인 번호 기준 커밋: 2026-07-14 시점 작업트리 (sports-live.html 3814줄).

---

## 1. 홈 매트릭스 문법 (gameRowsHTML)

### 1.1 컬럼 헤더
- 예정 탭: `Today | Spread | Total | Moneyline` — sports-live.html:810 (`#colheadUp`, i18n 키 `col_today/col_spread/col_total/col_ml`)
- 라이브 탭: `In Play | Spread | Total | Moneyline` — sports-live.html:825 (`#colheadLive`, `col_inplay`)
- 그리드: `minmax(0,1fr) 74px 74px 74px` 4열 — sports-live.html:188
- **축구(SOC) 탭에서는 헤더 숨김** (`display:none`) — sports-live.html:1748-1749 (1X2 셀이 Home/Draw/Away 자체 라벨을 갖기 때문)

### 1.2 행 순서 + 배열 인덱스 규약 (무관용)
- **원정팀이 윗줄, 홈팀이 아랫줄**, 단 배열 인덱스는 **away=1, home=0**: `${row(g.away,1)}${row(g.home,0)}` — sports-live.html:1664
- 근거(서버측 배열 순서): spread/ml 배열은 `[home항목, away항목]` 순으로 생성 — supabase/functions/sports-games/index.ts:76-79 (spread), :79 (ml `[{...home.nm+" ML"},{...away.nm+" ML"}]`), 오버레이도 동일 — index.ts:122, :132
- 셀 순서(한 행): Spread → Total → Moneyline — sports-live.html:1650 (`cell(g.core.spread,'Spread',i)}${cell(g.core.total,'Total',i)}${cell(g.core.ml,'Moneyline',i)`)
- 팀 셀 구조: `.tcell > .bd(약어) + .tinfo > .nm(팀명) + .sc(점수, live면 class에 live 추가)` — sports-live.html:1649

### 1.3 베팅 셀 구조
- 정상 셀: `<div class="bet[ sel]" data-game data-market data-sel data-am><span class="ln">라인||&nbsp;</span><span class="pr [pos]">fmtAm(am)</span></div>` — sports-live.html:1647-1648
  - **`pos` 클래스는 `b.am>0`(플러스 배당)일 때만** `.pr`에 부여 — sports-live.html:1648
  - 선택 중이면 `sel` 클래스: `legs.some(l=>l.uid===g.id+'|'+b.sel)` — sports-live.html:1643
- 배열 항목 없음: `'<div></div>'` (빈 div) — sports-live.html:1642
- 배당 포맷 `fmtAm`: `settings.oddsFormat==='decimal'`이면 소수 2자리, 아니면 `(a>0?'+':'')+a` — sports-live.html:1558-1564

### 1.4 잠금 셀 (정확한 마크업)
- **oddsReal=false (실라인 없음)**: `<div class="bet sus" title="Odds unavailable — no market"><span class="ln">&nbsp;</span><span class="pr">🔒</span></div>` — sports-live.html:1641
  - `data-am`을 **부여하지 않음** = toggle의 `isFinite(am)` 가드에서 무시 — sports-live.html:1912. 이것이 클라 측 돈 관문(서버 관문은 §5.6).
- **라이브 서스펜드**: `<div class="bet sus" title="${liveReason(g)}"><span class="ln">${b.ln||'&nbsp;'}</span><span class="pr">🔒</span></div>` (라인 텍스트는 유지) — sports-live.html:1646

### 1.5 라이브 서스펜드 판정 (liveOK — 조건 3개 전부 충족해야 베팅 가능)
sports-live.html:1713-1719:
1. `window.__alpexaLiveBetting` 마스터 스위치 ON (:1715)
2. `g.suspendUntil`이 없거나 지났음 — 점수 변동 직후 **15초 잠금** (`SUSPEND_MS=15000` — :3567, 부여 로직 applySuspensions :3577-3588)
3. `window.__alpexaOddsStale`이 false — 배당 미갱신 **90초** 초과 시 전체 라이브 잠금 (`ODDS_STALE_MS=90000` — :3568, refreshOddsStale :3573)

liveReason 문구(우선순위 순) — sports-live.html:1720-1725:
- `'Live betting unavailable'` (마스터 OFF)
- `'Suspended — score just changed'` (suspendUntil)
- `'Odds updating — try again'` (stale)

toggle에서도 재검사 + 토스트 `showToast('🔒','Live betting',liveReason(g))` — sports-live.html:1916, :1899.

### 1.6 축구 1X2 분기
- 판정: `const isSoc=g.lg==='SOC'` — sports-live.html:1653
- 헤더 행 `.soc-hdr`: `홈약어 + "홈팀 <b>v</b> 원정팀" + (점수 있으면 .soc-sc "홈–원정") + 원정약어` — sports-live.html:1662
- 셀 3개 `.soc1x2`: `c3(tw[0],'Home')  c3(tw[1],'Draw')  c3(tw[2],'Away')` — sports-live.html:1663 (threeWay 배열 순서 = [홈, 무, 원정] — sports-games/index.ts:63-67, :126-130)
- c3 셀: `data-market="1X2"`, `.ln`에 Home/Draw/Away 라벨, 배당 없음이면 `class="bet dis"` + `—` — sports-live.html:1654-1659
- 잠금 c3: `class="bet sus"`에 `.ln`은 라벨 유지 + 🔒 — sports-live.html:1655, :1658

### 1.7 data-* 속성 계약
- `data-game=g.id` / `data-market∈('Spread','Total','Moneyline','1X2', 프롭명)` / `data-sel=b.sel(서버 sel 문자열 그대로)` / `data-am=b.am` — sports-live.html:1647, :1659, :1878
- 클릭 바인딩: `toggle(b.dataset.game,b.dataset.market,b.dataset.sel,+b.dataset.am)` — sports-live.html:1764
- **market 문자열은 place_bet의 매핑 키** (Moneyline→ml, Spread→spread, Total→total, 1X2→threeWay; 그 외 거절) — supabase/sql/place_bet_server_odds.sql:67-73

### 1.8 카드 껍데기 / 격리 / 정렬
- 카드 헤더: 리그 아이콘맵 `{NBA:🏀,NCAAB:🎓,NFL:🏈,MLB:⚾,NHL:🏒,SOC:⚽,UFC:🥊}` + `g.lg==='SOC'?'Soccer':g.lg`, 우측 `.st`에 `live ? 'LIVE '+g.time : gameTimeLabel(g)` — sports-live.html:1680-1681
- **항목별 try/catch 격리**: 한 경기 렌더 에러 → 그 카드만 `''` (전체 블랙아웃 금지, tests/sports-render.test.js가 강제) — sports-live.html:1638, :1686
- 정렬: 핀 → live(0) → upcoming(1) → post(2), 동순위는 kickoff 빠른 순 — sports-live.html:1698-1704
- 빈 상태: 첫 로드 전 스켈레톤(`__gamesLoaded` false), 이후 `'No upcoming games'` / `'No live games right now'` — sports-live.html:1750-1756
- +More Markets: 프롭 개수 `(N)` 표기, 프롭 있을 때만 — sports-live.html:1665, :1685

---

## 2. 벳슬립 문법 (syncSlip / drawLegs / calc / placeBet)

### 2.1 모드 3종 (라벨·부제 고정)
sports-live.html:1059-1061:
- `data-mode="single"` → **Singles** / "Each separate"
- `data-mode="parlay"` → **Parlay** / "All must win" (**기본 활성**, `mode='parlay'` 초기값 — :1474)
- `data-mode="sgp"` → **Same Game** / "Same-game combo"
- SGP 노트 문구: "Same Game Parlay (SGP): combine multiple markets from one game." — sports-live.html:1063
- **SGP 탭 활성 조건**: `gameCount()===1 && legs.length>=2` — sports-live.html:1945-1946; 깨지면 자동으로 parlay로 강등 — :1947

### 2.2 배당 계산 경로 (숫자 계약)
- `dec(a)`: 미국식→소수 변환 후 **하우스 마진 적용** `1+(d-1)*(1-m)`, `m=clamp(window.__alpexaSportsMargin,0,15)/100` — sports-live.html:1567
- `toAm(d)`: 소수→미국식 역변환 — sports-live.html:1568
- `SGP_HAIRCUT = clamp(window.__alpexaSgpHaircut ?? 0.25, 0, 0.9)` — sports-live.html:1572 (**서버 정산 0.25와 락스텝 필수** — sports-settle/index.ts:228, place_bet_server_odds.sql:37)
- `isSGP(ls)`: 2legs 이상 && 전 leg의 gid 동일 — sports-live.html:1573
- `comboDec(ls) = Π dec(l.am)`, SGP면 `1+(d-1)*(1-SGP_HAIRCUT)` — sports-live.html:1574

### 2.3 leg uid / leg 객체 규약
- **uid = `g.id + '|' + sel`** — sports-live.html:1643, :1913 (같은 market 재선택 시 프롭뷰는 상호배타 교체 — :1900)
- leg 객체: `{uid,gid,market,sel,am, game:'AWY @ HOM', tm,kt(kickoff ISO),live,lg, stake:20(기본)}` — sports-live.html:1917

### 2.4 drawLegs 마크업
sports-live.html:1950-1956:
- 빈 슬립: `tr('slip_empty')` — :1951
- leg: `✕(.x data-uid)` + SGP모드면 `<span class="tag">SGP</span>` + `.l1` = `${l.game} · ${l.market}` + `.l3` = `Pick: <b>${l.sel}</b>` + `fmtAm(l.am)` + 개별 스테이크 input(`min=0 step=5`) + `To win <b>${usd(l.stake*(dec(l.am)-1))}</b>`

### 2.5 calc — 요약/바 표기
sports-live.html:1960-1973:
- **Singles**: 라벨 `Total Wager (N bets)`, totalOdds=합계 스테이크 usd, **Est. Payout = Σstake + Σ(stake×(dec−1))**, barMode `Singles`, barOdds `Nx`, place 버튼 `${usd(tot)} Place Bet` — :1962-1966
- **Parlay/SGP**: 라벨 `SGP Odds (Nlegs)` / `Parlay Odds (Nlegs)`, totalOdds=`fmtAm(toAm(comboDec(legs)))`, **Est. Payout = stake + stake×(comboDec−1)**, barMode `SGP`/`Parlay`, place 버튼 `${usd(stake)} → win ${usd(win)}` — :1967-1972
- 요약 행 라벨: "Est. Payout" (i18n `est_payout`) — sports-live.html:1073

### 2.6 placeBet 가드 (순서대로)
sports-live.html:1976-1989:
1. `window.__alpexaHalt` → 'Trading paused' — :1977
2. `isLocked()` (자기배제 `settings.selfExcludeUntil>Date.now()`) → 'Account locked' — :1978, :1439 (서버 소유 players 컬럼과 동기 — :1502-1507)
3. `checkLossLimit()` (오늘 'Bet placed' 음수 합 ≥ lossLimit) → 'Loss limit reached' — :1979, :1440-1448
4. `stake>balance` → 'Insufficient balance' — :1982
5. `_minBet` (`__alpexaCtrl.minBet`, 기본 1) — :1986-1987
6. `_maxBet` (`__alpexaCtrl.maxBet`, 0=무제한) — :1988-1989 (**UX 가드일 뿐, 권위는 place_bet** — 주석 :1984-1985)

### 2.7 placeBet 제출 계약
- betType: `legs.length===1→'Single'`, `mode single→'Singles'`, `sgp→'Same Game'`, else `'Parlay'` — sports-live.html:1991
- 티켓번호: `'SP-'+연번` (localStorage `alpexa.seq.sp`, 시드 100000) — sports-live.html:1996
- betId(local_id): `'b'+Date.now()` — sports-live.html:1997
- baseCash: `round(stake*0.92,2)` (캐시아웃 기준선) — sports-live.html:1995
- RPC: `place_bet({p_acct, p_stake, p_potential, p_symbol:betType+' · '+첫leg.sel, p_local_id:String(betId), p_meta, p_game, p_pick, p_odds, p_size:legCount})` — sports-live.html:2018
- meta: `{type,cls,stake,potential,baseCash,d0,cashout,ticket,placedTs,legs}` — sports-live.html:2017; leg 저장형은 `{pk,sel,gid,market,gm,am,am0,dec0,tm,kt,live,lg,st:'o'}` — :2001
- **낙관적 행 + 서버 거절/네트워크 실패 시 롤백** (클라 폴백 생성 금지) — sports-live.html:2020-2025, :2028-2037
- 성공 후 서버 balance 재조회로 즉시 채택 — sports-live.html:2027

---

## 3. 티켓 카드 문법 (ticketHTML)

### 3.1 배지 텍스트
sports-live.html:2085-2087:
- `tw = String(b.type||'Bet').split(' · ')[0]` (서버 symbol이 "Parlay · pick +N" 형태라서 첫 단어만)
- 1leg → 배지 `Single` (class `single`); 그 외 → `` `${tw}${legs>1?' '+N+' legs':''}` `` (class = `b.cls` 또는 'Same Game' 매칭 시 `sgp`, else `parlay`)

### 3.2 상태칩 (.res)
- Open이면 `Open`; 정산이면 `dispRes==='won'?'Won' : 'cashed'?'Cashed Out' : 'refunded'?'Push' : 'Lost'` — sports-live.html:2089
- **refunded 규칙**: `result==='won' && payout <= stake+0.001` → 표기상 refunded(칩 'Push', 금액 'Refund') — sports-live.html:2084 (서버는 Draw-No-Bet push도 'won'으로 기록하므로 표기만 변환)

### 3.3 티켓번호·타임스탬프
- 헤더 아래 모노스페이스 줄: `${ticket}` + (둘 다 있으면 `' · '`) + `fmtStamp(placedTs)` — sports-live.html:2090
- `fmtStamp`: `toLocaleString(연/월/일 숫자 + 시:분 12h)`, 설정 타임존 적용 — sports-live.html:1531-1535

### 3.4 leg 줄 (.tleg)
sports-live.html:2056-2078:
- ICONS 맵 `{NBA:🏀,NCAAB:🎓,NFL:🏈,MLB:⚾,NHL:🏒,SOC:⚽,UFC:🥊}` — :2063; lg 소스 우선순위: 피드 게임 → leg.lg → **gid 접두어**(`gid.split('_')[0]`, 알려진 리그일 때만) — :2064-2066
- **라이브 스코어 줄(gm2)** (open && 해당 게임이 live일 때 매치업 대체): `` `${away.ab} ${away.sc??'–'} — ${home.sc??'–'} ${home.ab}` · ${g.time}` `` — :2068
- 예정/일반 줄(gm): `아이콘 + l.gm` + (tm 있으면 `· ${live?'🔴 ':'🕒 '}${legTimeLabel(l)}`) — :2069 (legTimeLabel: live면 스냅샷 tm, 아니면 kt를 타임존 재포맷 — :1544-1550)
- pick 줄: `l.pk||l.sel||b.type||'Selection'` — :2077; 배당은 `fmtAm(l.am0 ?? l.am)` (**체결 시점 am0 우선**) — :2078
- 정산 leg 결과 태그: `Won`(pos색) / `Lost`(neg색) / `Push · refunded`(dim색); 소스는 `l.r` 또는 st(w/l/p) — :2071-2075

### 3.5 Open 상태 위계 (Live > Awaiting result > Upcoming)
sports-live.html:2094-2111:
- 판정: leg마다 — 피드에서 live면 anyLive; 아니면 `kt||g.kickoff` 파싱해 `Date.now()>=t`면 anyStarted; **kt도 없고 피드에도 없으면 anyStarted 간주** — :2098-2105
- `stState = anyLive?'live':(anyStarted?'started':'upcoming')` — :2106
- 표기: live → 깜빡이는 점 + `Live`; started → `Awaiting result`(accent); upcoming → `Upcoming`(dim) — :2109-2111
- Open 정렬: live 우선 → 최소 kickoff → 최신 placedTs (**복사본 정렬**, 원본 불변) — sports-live.html:2131-2134

### 3.6 푸터
- open: `Stake $X <b>Est. $potential</b>` + statusTag — sports-live.html:2115-2116 (**캐시아웃 버튼은 현재 숨김** — 주석 :2116; doCashout·`data-cash`/`data-cashhalf` 델리게이션은 코드 유지 — :2169-2172)
- open+viewOnly: Est. 줄 + `🔄 Open on another device` — sports-live.html:2113-2114
- cashed: `Stake $X <b class="c">Cashed $payout</b>` + `Settled · stamp` — sports-live.html:2117
- 정산: `Win $payout`(g) / `Refund $payout`(무색) / `Loss $stake`(l) + `Settled · stamp` — sports-live.html:2118

### 3.7 캐시아웃 (doCashout)
- viewOnly 거절 토스트 — sports-live.html:2176
- 전액 frac=1 / 절반 `data-cashhalf`→0.5 — sports-live.html:2170, :2177
- **서버 권위**: `AlpexaSync.db.rpc('cash_out',{p_local_id:String(b.id),p_fraction:frac})` — sports-live.html:2184
- 서버 캡: **잔여 스테이크의 92%** `round(stake*0.92*frac,2)` — supabase/sql/cash_out.sql:25; ledger ref `'cashout-'||local_id||'-'||타임스탬프` — cash_out.sql:28-30; 전액=status 'cashed', 부분=stake/potential 비례 감소 — cash_out.sql:31-38
- 실패 메시지에 `not found|already|settled` 매칭 시 로컬 open에서 제거 — sports-live.html:2186-2190
- **로컬 credit 금지** — 서버 balance 재조회로만 반영 — sports-live.html:2209-2213

### 3.8 서버 동기 (open/settled 소스)
- open = `positions`(cust_id, server='sports', kind='bet', status='open')에서 rebuild; meta.legs 있으면 풀 티켓, 없으면 `viewOnly:true` 최소 표시 — sports-live.html:2314-2334; **서버가 진실**, 로컬 잔존은 15초 미만 미동기 베팅만 — :2338-2340
- settled = `settlements`(kind bet_won/bet_lost)에서 rebuild, `payout = won ? round(stake+pnl,2) : 0`, detail JSON의 per-leg r 복원 — sports-live.html:2406-2424 + 로컬 cashed 병합 — :2445-2448

---

## 4. 발란스 표시 문법

### 4.1 서버 전용 원칙 (CLAUDE.md #5)
- `let balance=0` — **localStorage 시드 절대 금지**, 서버 로드 전 0 시작 — sports-live.html:2462-2465
- `syncBalancesFromServer()`: 로드 시 + 10초 폴링 + visibilitychange + accounts Realtime 채널 — sports-live.html:3290-3308, :3310, :3325-3326, :3334-3336
- **7초 로컬 변경 가드**: `|서버-로컬|>0.01 && (미로드 || now-__sbLocalTs>7000)`일 때만 채택 (막 배팅한 잔고 되돌림 방지) — sports-live.html:3301-3303
- 첫 응답은 즉시 채택(`__sbLoaded`) + loadBal 재시도 루프(최대 40회) — sports-live.html:3298-3304, :3315-3324
- `syncSportsBal()`은 **표시 전용** — ledger 델타 push 없음 — sports-live.html:2470-2476

### 4.2 미로드 표기
- 서버 잔고 로드 전(`!window.__sbLoaded`) 잔고 자리 전부 **`'···'`** ($0.00 금지) — sports-live.html:2599-2607

### 4.3 헤더 발란스바 라벨
sports-live.html:766-775:
- `Balance · USD` (i18n `bal_label`) + `#balbarVal`
- `Pending $X` — **의미: open 베팅 스테이크 합** `pendingTotal()=openBets.reduce(a+b.stake)` — sports-live.html:2537, 표시 :2610
- `Credit $X` — 프로모 크레딧; **캐시백 비활성이라 credit>0일 때만 표시**(기본 숨김) — sports-live.html:2612-2615, 클라 credit→balance 이동 차단 — :1627-1629
- `＋ Deposit` 버튼

---

## 5. 데이터 계약

### 5.1 live_games 스키마 (단일 행 id='all', data=jsonb 배열)
게임 항목 필드 — supabase/functions/sports-games/index.ts:216-222:
```
gid      : "<LG>_<espnEventId>"  (예 "NFL_401547321")   — index.ts:217
lg       : NFL|NBA|MLB|NHL|SOC (Edge) — index.ts:21-30 (앱 폴백엔 NCAAB/UFC 추가 — sports-live.html:3353-3373)
sport    : "Football" 등                                — index.ts:217
live     : boolean (state==='in')                       — index.ts:218
time     : live면 ESPN shortDetail, 예정이면 포맷 문자열 — index.ts:218
iso      : 킥오프 UTC ISO (클라가 로컬 타임존 렌더)      — index.ts:219
home/away: {ab, nm, sc?(live만)}                        — index.ts:212-214
spread/total/ml : [{ln, am, sel}] (홈 먼저)             — index.ts:76-80
threeWay : [홈, Draw, 원정] (SOC)                       — index.ts:63-67, :126-130
oddsReal : boolean — false = 실라인 없음 → 베팅 불가    — index.ts:221
```
- finished(post) 게임은 피드에서 제외 — index.ts:211
- 읽기 경로: `feed` Edge(1초 캐시) → 폴백 `live_games.select('data').eq('id','all')` — sports-live.html:3621-3637
- 앱 변환 lgToGame: oddsReal=true인데 마켓 부족(SOC threeWay<3, 기타 spread/total/ml<2)이면 드랍; oddsReal=false면 **잠금 유지 표시** — sports-live.html:3603-3616

### 5.2 sel 문자열 규약 (정산기 gradeLeg 파서 계약 — 절대 변형 금지)
생성 — sports-games/index.ts:62-79, :122-134 / 정산 파싱 — sports-settle/index.ts:133-165:
- 머니라인: `"<팀nm> ML"` (예 "Chiefs ML") — 생성 :79, 파싱 `/ ML$/i` — settle:154
- 토탈: `"Over 45.5"` / `"Under 45.5"` — 생성 :78, 파싱 `/(over|under)\s+([\d.]+)/i` — settle:138-141
- 스프레드: `"<팀nm> -3.5"` / `"<팀nm> +3.5"` — 생성 :76-77, 파싱 끝자리 `/([-+]?[\d.]+)\s*$/` — settle:158-162
- 1X2: 팀은 `"<팀nm> ML"` 재사용 + `"Draw"` — 생성 :64-66/:126-129, **market='1X2'로 라우팅**되어 머니라인 분기보다 먼저 처리(팀=완승만 won, 무승부=lost; Draw픽=무승부만 won) — settle:147-153
- 2-way 머니라인 무승부 = **push(환불)** = Draw No Bet — settle:156
- 팀 매칭 teamSide: nm/ab 완전일치 → 부분포함 — settle:122-131

### 5.3 oddsReal 게이트 (3중)
1. **피드 생성**: 실 ML 없으면 배당 배열 전부 빈 배열 + `real:false` — **가짜 -140/120 발명 금지** — sports-games/index.ts:52-71; 오버레이(The Odds API sports_odds)가 실라인 채우고 true로 — :174-178
2. **클라 렌더**: `oddsReal(g){return !g||g.oddsReal!==false}`(플래그 없음=real, 하위호환) → 🔒 셀, data-am 미부여 — sports-live.html:1712, :1641
3. **서버 place_bet**: `coalesce((v_game->>'oddsReal')::boolean,true)=false` → `'odds unavailable for this game'` 거절 — supabase/sql/place_bet_server_odds.sql:76-80

### 5.4 place_bet RPC 전체 계약
supabase/sql/place_bet_server_odds.sql:27-29 시그니처:
`place_bet(p_acct text, p_stake numeric, p_potential numeric, p_symbol text, p_local_id text, p_meta jsonb, p_game text default '', p_pick text default '', p_odds numeric default 0, p_size integer default 1) returns jsonb`
- 소유권: acct→player→`auth.uid()` 검증 — :41-44
- **멱등**: 같은 (cust,server,local_id) positions 존재 시 `{ok:true,duplicate:true}` — :48-49
- **서버 re-price**: 클라 am 무시, live_games에서 market 매핑(:67-72: moneyline→ml/spread/total/1x2→threeWay, **그 외=프롭 거절** :73) 후 sel 완전일치로 am 조회, 미발견 시 `'line not offered'` 전체 거절 — :83-86
- SGP 해어컷 0.25 (전 leg 동일 gid) — :37, :94-95
- **최대 책임 $25,000** (potential−stake) — :38, :98-105
- 잔고 검사 후 원자 처리: ledger `'betstake-'||p_local_id` 차감 + positions 행(meta.legs는 서버 am으로 덮어씀) — :107-116

### 5.5 정산 (sports-settle) 계약
- 최근 6일 catch-up 윈도우로 final 스코어 수집(+UFC 보정: 승자 1/패자 0) — sports-settle/index.ts:44-53, :89-113
- **선점(claim) = positions 행 DELETE**; 실제로 지운 액터만 지급 진행(이중지급 구조적 차단) — index.ts:216-222
- won = `!anyLost` (push leg는 배수 1로 통과) — index.ts:211, :224
- payout = `round(stake × mult, 2)`, SGP면 `1+(decMul-1)*(1-0.25)` — index.ts:228-231; leg 배수는 `dec0 우선, 없으면 am0→am` — :166-171
- **멱등 ref**: `"betpay-" + local_id` — index.ts:232, 승자만 ledger 'bet_won' — :234-240
- settlements 기록: kind bet_won/bet_lost, pnl, detail=`{legs:[{pk,gm,am,gid,lg,r}]}` — index.ts:241-250
- 클라 settleBets는 **no-op** (서버 단독 정산) — sports-live.html:2249-2260

### 5.6 멱등 ref 총람
- 스테이크 차감: `betstake-<local_id>` — place_bet_server_odds.sql:113
- 승리 지급: `betpay-<local_id>` — sports-settle/index.ts:232
- 캐시아웃: `cashout-<local_id>-<ts>` — cash_out.sql:30

---

## 6. 금지 목록 — 대시보드가 절대 하면 안 되는 것

| # | 금지 | 근거 |
|---|------|------|
| 1 | **클라이언트 ledger INSERT** (어떤 kind든) | ledgerPost 헬퍼 자체를 삭제 + RLS ledger_admin_ins 차단 — sports-live.html:2269-2274; CLAUDE.md #5 "클라가 ledger에 델타 올리는 길은 폐쇄" |
| 2 | **로컬(localStorage) 잔고 시드/저장** | `let balance=0` + "#5: never seed the balance from localStorage" — sports-live.html:2462-2465; CLAUDE.md #5 (이중차감·계정간 누수 근원) |
| 3 | **잔고 델타 추측 push** (syncSportsBal류 재발명) | syncSportsBal은 표시 전용으로 봉인 — sports-live.html:2470-2476; CLAUDE.md 돈 불변식 "알려진 약점" |
| 4 | **배당 자체 발명** (가짜 -140/120, 자체 마진 임의 적용) | 피드가 빈 배열+real:false로만 표기 — sports-games/index.ts:52-56, :70-71; CLAUDE.md 오즈 불변식 2026-07-08 "가짜 라인이 베팅 가능하면 🔴" |
| 5 | **oddsReal=false 게임에 data-am 부여/베팅 UI 활성** | 클라 잠금 계약 — sports-live.html:1641, :1712; 서버도 거절하지만 UI가 열리면 그 자체가 결함 |
| 6 | **클라이언트 정산/지급** (gradeLeg 결과로 credit) | settleBets no-op + "SERVER-ONLY SETTLEMENT" — sports-live.html:2249-2260; 정산은 sports-settle 단독 — sports-settle/index.ts:1-14 |
| 7 | **캐시아웃 금액 클라 계산·로컬 credit** | cash_out RPC만, 서버 92% 캡 — sports-live.html:2179-2184, cash_out.sql:25; RPC 후 로컬 credit 금지 — sports-live.html:2209-2210 |
| 8 | **클라이언트 positions INSERT** (베팅 행 직접 생성) | insertBetRow/syncOwnBets 제거 — "bets are created only by place_bet" — sports-live.html:2304-2312 |
| 9 | **프롭(플레이어 프롭 등) place_bet 제출** | live_games에 없어 서버 검증 불가 → market 매핑 실패로 거절 — place_bet_server_odds.sql:22, :65-73. 대시보드는 프롭을 표시만 하거나 아예 빼야 함 |
| 10 | **place_bet 실패 시 로컬 폴백 베팅 생성** | "There is NO client-side fallback that creates the row"(멱등 가드 경쟁→공짜 베팅) — sports-live.html:2029-2033 |
| 11 | **liveOK 3조건 무시한 라이브 베팅 UI** | 서스펜드/스테일 잠금 — sports-live.html:1713-1719, :1646 (안티 아비트라지) |
| 12 | **SGP_HAIRCUT·마진 파라미터 독자값** | 앱·place_bet·settle 3곳 0.25 락스텝 — sports-live.html:1572, place_bet_server_odds.sql:37, sports-settle/index.ts:228 |
| 13 | **openBets 배열 in-place 정렬 등 원본 변형** | sortedOpen은 slice() 후 정렬 (find/remove가 원본 순서 의존) — sports-live.html:2129-2131 |

---

## 7. 하네스 핀 후보 — fidelity 테스트가 그대로 복사할 핀 문자열

대시보드 파일에 아래 패턴이 존재해야(또는 금지 패턴이 부재해야) 통과. 좌측=정규식(또는 리터럴), 우측=검증 대상 계약.

### 존재 핀 (대시보드에 있어야 함)
| 핀 | 계약 | 원본 근거 |
|---|---|---|
| `col_today[\s\S]*col_spread[\s\S]*col_total[\s\S]*col_ml` | 컬럼 라벨·순서 | sports-live.html:810 |
| `Today` `Spread` `Total` `Moneyline` `In Play` | 헤더 텍스트 | :810, :825 |
| `class="bet sus" title="Odds unavailable — no market"` | 노라인 잠금 셀 리터럴 | :1641 |
| `<span class="ln">&nbsp;</span><span class="pr">🔒</span>` | 잠금 셀 내부 | :1641 |
| `data-game=.*data-market=.*data-sel=.*data-am=` | 셀 데이터 속성 4종 순서 | :1647 |
| `am>0\?'pos':''` (또는 `class="pr pos"` 생성 로직) | 플러스 배당 pos 클래스 | :1648 |
| `row\(g\.away,1\)[\s\S]*row\(g\.home,0\)` | away 위/1, home 아래/0 | :1664 |
| `data-market="1X2"` | 축구 1X2 market 키 | :1659 |
| `c3\(tw\[0\],'Home'\).*c3\(tw\[1\],'Draw'\).*c3\(tw\[2\],'Away'\)` (또는 `'Home'.*'Draw'.*'Away'` 순서) | threeWay 인덱스 계약 | :1663 |
| `soc-hdr` `soc1x2` | 축구 카드 구조 클래스 | :1662-1663 |
| `oddsReal !== false` | 하위호환 게이트(플래그 없음=real) | :1712, :3613 |
| `__alpexaLiveBetting` `suspendUntil` `__alpexaOddsStale` | liveOK 3조건 | :1715-1717 |
| `Suspended — score just changed` | liveReason 문구 | :1722 |
| `Odds updating — try again` | liveReason 문구 | :1723 |
| `SUSPEND_MS\s*=\s*15000` / `ODDS_STALE_MS\s*=\s*90000` | 서스펜드 상수 | :3567-3568 |
| `SGP_HAIRCUT` + `0\.25` | 해어컷 락스텝 | :1572, settle:228, sql:37 |
| `\+'\|'\+` (uid 조합 `gid+'|'+sel`) | leg uid 규약 | :1643, :1913 |
| `stake:20` | 기본 leg 스테이크 | :1917 |
| `Singles` `Parlay` `Same Game` | 모드 라벨 3종 | :1059-1061 |
| `All must win` `Each separate` `Same-game combo` | 모드 부제 | :1059-1061 |
| `Est\. Payout` | 요약 라벨 | :1073 |
| `Total Wager \(` | Singles 라벨 | :1963 |
| `Parlay Odds` / `SGP Odds` | 콤보 라벨 | :1968 |
| `rpc\('place_bet'` | 서버 베팅 경로 | :2018 |
| `rpc\('cash_out'` + `p_fraction` | 서버 캐시아웃 경로 | :2184 |
| `p_local_id` `p_meta` `p_stake` `p_potential` | place_bet 파라미터 | :2018 |
| `'SP-'` | 티켓번호 접두어 | :1996 |
| `Cashed Out` | 상태칩 텍스트 | :2089 |
| `Push · refunded` | leg push 태그 | :2074 |
| `Awaiting result` | Open 위계 중간 상태 | :2110 |
| `Open on another device` | viewOnly 푸터 | :2114 |
| ``` `${g.away.ab} ${...} — ${...} ${g.home.ab}` ``` (`sc">.*—.*` 패턴) | gm2 라이브 스코어 포맷 | :2068 |
| `🕒` + `🔴` | 예정/라이브 leg 시간 접두 | :2069 |
| `NBA:'🏀',NCAAB:'🎓',NFL:'🏈',MLB:'⚾',NHL:'🏒',SOC:'⚽',UFC:'🥊'` | ICONS 맵 | :1680, :2063 |
| `'···'` | 잔고 미로드 표기 | :2602-2603 |
| `Balance · USD` `Pending` `Credit` | 발란스바 라벨 | :768-773 |
| `pendingTotal` (또는 `openBets.reduce.*stake`) | Pending 의미 | :2537 |
| `\.eq\('id','all'\)` | live_games 단일행 읽기 | :3635 |
| `console.error('card render failed'` (또는 map 내부 try/catch) | 항목별 렌더 격리 | :1686 |
| `' ML'` `Over ` `Under ` `'Draw'` | sel 문자열 규약 | sports-games/index.ts:78-79, :65 |
| `betstake-` | 스테이크 멱등 ref | place_bet_server_odds.sql:113 |
| `betpay-` | 지급 멱등 ref | sports-settle/index.ts:232 |
| `0\.92` | 캐시아웃 캡 | cash_out.sql:25, sports-live.html:1995 |

### 부재 핀 (대시보드에 있으면 🔴 — diagnose.js 클래스)
| 금지 패턴 | 잡는 결함 | 근거 |
|---|---|---|
| `from\('ledger'\)\.insert` / `/rest/v1/ledger` POST(클라) | 클라 ledger 쓰기 | sports-live.html:2269-2274 |
| `from\('positions'\)\.insert` (클라) | 클라 베팅 행 생성 | :2304-2312 |
| `localStorage.setItem\(.*(bal|balance)` | 로컬 잔고 저장 | :2462-2465, CLAUDE.md #5 |
| `-140` + `120` 짝 (배당 기본값 리터럴) | 가짜 배당 발명 | sports-games/index.ts:52-56, CLAUDE.md 오즈 불변식 |
| `balance\s*[+\-]=` 후 서버 미동기 (정산 문맥) | 클라 정산/이중 지급 | sports-live.html:2249-2260 |

---

## 부록 — 앱과 대시보드가 공유해야 하는 런타임 파라미터 (window 전역)
| 전역 | 의미 | 근거 |
|---|---|---|
| `__alpexaSportsMargin` | 하우스 마진 %(0–15), dec()에 반영 | sports-live.html:1567 |
| `__alpexaSgpHaircut` | SGP 해어컷(기본 0.25, 0–0.9) | :1572 |
| `__alpexaLiveBetting` | 라이브 베팅 마스터 스위치 | :1715 |
| `__alpexaOddsStale` / `__alpexaOddsTs` | 배당 신선도 | :1717, :3571-3573 |
| `__alpexaHalt` | 전체 베팅 일시정지 | :1977 |
| `__alpexaCtrl.minBet/.maxBet` | 운영자 베팅 한도(UX 가드) | :1986-1988 |

---

## 8. 차이 감사 — 대시보드 누락 목록 (2026-07-14)

> sports-live.html(앱 원본) ↔ sports-dashboard.html(v1, 1078줄) 전수 대조.
> (a)=앱 근거, (b)=대시보드 상태(없음/부분 + 근거줄), (c)=우선순위(P1 돈·핵심 UX / P2 편의 / P3 장식), (d)=위젯 번역 제안.
> 대시보드 라인 기준: 2026-07-14 작업트리. "부분"은 개념은 있으나 앱 문법과 불일치.

### 8.1 P1 — 돈·핵심 UX (다음 단계에서 최우선)

| # | 항목 | (a) 앱 근거 | (b) 대시보드 상태 | (d) 위젯 번역 제안 |
|---|------|-------------|-------------------|--------------------|
| 1 | **자기배제 가드 + 풀스크린 블록** — placeBet 2번 가드 `isLocked()`, 서버 `players.self_exclude_until` 동기, seBlock 화면 | sports-live.html:918-923, :1416-1439, :1978, :1502-1507 | **없음** — placeBetCore(sports-dashboard.html:850-861)에 halt·잔고·min/max만 있고 자기배제 검사 부재 | placeBetCore에 players 테이블 self_exclude_until 조회 가드 + 배제 중이면 전 위젯 위 오버레이 |
| 2 | **Loss limit 가드** — placeBet 3번 가드 `checkLossLimit()` (오늘 베팅 스테이크 합 ≥ `players.loss_limit`) | sports-live.html:1440-1448, :1979, :1502-1507 | **없음** — placeBetCore에 검사 부재 (sports-dashboard.html:850-861) | placeBetCore에 서버 loss_limit + 당일 positions/settlements 합산 가드 한 줄 |
| 3 | **Settled 목록** — settlements rebuild(payout=won?stake+pnl:0, per-leg r 복원), 상태칩 Won/Cashed Out/Push/Lost, leg 태그 Won/Lost/Push · refunded, 푸터 Win/Refund/Loss + `Settled · stamp`, refunded 표기 규칙 | sports-live.html:2406-2424, :2084, :2089, :2071-2075, :2117-2118 | **없음** — Settled 버튼 disabled ("다음 단계") — sports-dashboard.html:696 | My bets 위젯 Settled 탭: settlements 읽기 전용 렌더 (§3.2/3.4/3.6 문법 그대로, 돈 쓰기 없음) |

### 8.2 P2 — 편의·핵심 부가 UX

| # | 항목 | (a) 앱 근거 | (b) 대시보드 상태 | (d) 위젯 번역 제안 |
|---|------|-------------|-------------------|--------------------|
| 4 | 홈 검색바 = **리스트 필터** (matchGame: nm/ab/lg 부분일치로 게임 리스트 자체를 거름) | sports-live.html:796-800, :1689-1693, :3216-3218 | 부분 — 검색 팝오버는 점프+플래시 전용, 보드 필터 아님 (sports-dashboard.html:1032-1038) | 검색어 입력 중 renderBoard에 matchGame 필터 적용 (전 리그 관통) |
| 5 | **핀 고정** — ★ pin-btn, `alpexaPins` localStorage(UI 설정=로컬 OK), 정렬 최상단 + pinned 카드 강조 | sports-live.html:196-201, :1125-1130, :1678-1679, :1700-1704 | 없음 | obrow에 ★ 토글 → 보드/스케줄 정렬 최상단 |
| 6 | **정렬 위계** — live(0) → upcoming(1) → post(2) → kickoff 순 | sports-live.html:1698-1704 | 부분 — iso 단독 정렬이라 라이브가 위로 안 옴 (sports-dashboard.html:564, :589) | sort 키를 (live?0:1, iso)로 |
| 7 | **SOC 탭 컬럼헤더 숨김** — 1X2 셀이 자체 라벨을 가지므로 Today/Spread/Total/Moneyline 헤더 제거 | sports-live.html:1748-1749 | 부분 — selLg==='SOC'여도 obhead 유지 (sports-dashboard.html:582) | renderBoard에서 SOC면 `.obhead` display:none |
| 8 | **라이브 히어로/전용 뷰** — LIVE 필 + `liveCount` "Live Now N games" + In Play 헤더 + 라이브 전용 리스트 | sports-live.html:816-827, :1757 | 부분 — Leagues 행의 `LIVE n` 뱃지뿐 (sports-dashboard.html:550) | 보드 필터 세그 [All \| Live N] + 헤더 상태필에 라이브 총계 |
| 9 | **통계 4칸** — P&L / ROI / Win rate / Settled (정산 목록 집계, pos/neg 색, 자동 폰트 축소) | sports-live.html:2145-2156 | 없음 | My bets 위젯 상단 4스탯 행 (Settled 구현과 동시) |
| 10 | **Total Assets (all servers)** + Sports/Crypto/FX 스플릿 (`totalAssets()=Σ서버.bal+credit`) | sports-live.html:845-851, :2536, :2597-2607 | 없음 — 스포츠 잔고만 (sports-dashboard.html:755) | Wallet 위젯에 pullBalances 3키(sports/crypto/fx) 스플릿 행 추가 (표시 전용) |
| 11 | **입금 시트** — Card/Crypto 탭, MoonPay/MetaMask 프로바이더, 회사 입금주소+Copy, ERC-20 필, 금액 칩($50~$1,000), 제출(dpConfirm) + 입금 한도 checkDepositLimit | sports-live.html:993-1036, :2712, :1449-1455 | 없음 — 앱 딥링크 `<a href="sports-live.html">Deposit</a>` (sports-dashboard.html:758) | v2: 시트 이식 or 딥링크 유지 명시 (돈 경로는 서버/백오피스 승인 그대로) |
| 12 | **출금 시트** — `withdraw_hold` RPC(원자 홀드), 주소+QR 스캔, 네트워크 필, Withdrawable/수수료 요약(wdNote/wdSummary), 25/50/Max 칩 | sports-live.html:954-991, :2943-2963 | 없음 — 딥링크 (sports-dashboard.html:758) | 상동 |
| 13 | **이체 시트** — `app_transfer` RPC + 멱등 ref `xfer-...`, From/To 스왑(⇅), Available, %칩 | sports-live.html:925-952, :2783-2803 | 없음 — 딥링크 (sports-dashboard.html:758) | 상동 |
| 14 | **Recent Activity 필터** — 타입 4종(All/Deposits/Withdrawals/Bets) + 기간(7/30/90일/All time) 드롭다운 | sports-live.html:869-882, :2624-2634, :2659-2669 | 없음 (sports-dashboard.html:759) | Wallet 위젯 리스트 위에 필터 필 2줄 |
| 15 | **활동 행 소스 병합** — 서버 금융행 `serverFinTxns`(입금/출금/이체 서버 소스) + open∪settled 베팅행 dedupe + `ago()` 상대시각 + txIcon 모노라인 SVG 6종 | sports-live.html:2367-2392, :2636-2656, :2576-2595, :1576 | 부분 — 오픈 베팅 스테이크 행만, 🎫 이모지 (sports-dashboard.html:768-772) | serverFinTxns 포트(주석에 이미 "다음 단계" 명시 — :768) |
| 16 | **배당 포맷 설정** — american/decimal 선택 → fmtAm 분기 (설정 시트 ODDS_OPTS) | sports-live.html:1558-1564, :1309-1316 | 없음 — fmtAm 미국식 고정 (sports-dashboard.html:352) | 헤더 ⚙ 팝오버: Odds format 토글 (localStorage=UI 설정, 돈 아님) |
| 17 | **알림 시스템** — 벨+언리드 도트, notify() 피드(중복 4초 억제, 100개 캡), 패널+Mark all read, 베팅/정산/캐시아웃 이벤트 알림 | sports-live.html:754-757, :786-792, :1168-1176, :1458-1470, :2216 | 없음 | 헤더 벨 아이콘 + 팝오버 (Notifications 위젯도 가능) |
| 18 | **Reality check** — 주기 토스트 "You've been active for N min" (`realityCheckMin` 설정, 서버 players 동기) | sports-live.html:910-915, :1404-1415, :1502-1507 | 없음 | 전역 토스트 1개 (RG 계열이라 Settled보다 먼저 고려) |
| 19 | **계정 정보/사인아웃** — 계좌번호 탭 복사, 이메일, Sign out(auth.signOut+identity wipe) | sports-live.html:1236-1241, :1251-1258 | 없음 — 아바타 무기능 (sports-dashboard.html:338) | 아바타 클릭 → 계정 팝오버 (acct no 복사 + Sign out) |

### 8.3 P3 — 장식·보조

| # | 항목 | (a) 앱 근거 | (b) 대시보드 상태 | (d) 위젯 번역 제안 |
|---|------|-------------|-------------------|--------------------|
| 20 | 스켈레톤 로더 (첫 live_games 로드 전 shimmer 카드 5장, `__gamesLoaded` 게이트) | sports-live.html:35-40, :1743-1753 | 없음 — 로드 전 빈 보드 | 보드/스케줄에 skel 행 5개 |
| 21 | 빈 상태 문구 — `'No upcoming games'` / `'No live games right now'` / `No games match "q"` | sports-live.html:1750-1756 | 부분 — `No games for LG` (sports-dashboard.html:590) | 문구를 앱 리터럴로 통일 |
| 22 | 킥오프 로컬 표기 gameTimeLabel/fmtKick (타임존 설정 반영, iso→로컬) | sports-live.html:1540, :1518-1519 | 부분 — 피드 `g.time` 원문만 (sports-dashboard.html:570, :603) | iso를 브라우저 로컬로 재포맷 |
| 23 | **플레이 틱** — 라이브 카드의 play-tick(⏸/🔥/• + 팀 + txt + +pts, fresh 펄스 1회) | sports-live.html:1666-1677 | 없음 | 보드 라이브 행 아래 1줄 틱 (lastPlay 있으면) |
| 24 | **라이브 커버리지 스크린** — 📺 Watch live tracker 진입, 스코어보드(lead 강조), 야구 다이아몬드/카운트, PLAY-BY-PLAY 피드(14개 캡), BET LIVE 행 | sports-live.html:1684, :1773-1848 | 없음 | "Live tracker" 위젯 (경기 클릭 시 로드, 읽기 전용) |
| 25 | **+More Markets 프롭 시트** — 개수 배지 `(N)`, 카테고리 그룹(Player Props/Alternate Lines/Game Props/Team Totals), 상호배타 교체 toggleProps | sports-live.html:1665, :1685, :1849-1900 | 없음 | 표시 전용으로만 (프롭 place_bet 제출은 §6#9 금지 — 현재 피드 props:[]라 실익 낮음) |
| 26 | 서버 전환 드롭다운 — SERVERS 3종(FX/Crypto/Sports) 잔고 표시 + crypto-live/trading 이동 | sports-live.html:751-753, :780-783, :2453-2574 | 없음 — "Open app" 링크만 (sports-dashboard.html:335-337) | 헤더에 서버 스위처 팝오버 (링크 3개면 충분) |
| 27 | showToast 공용 토스트(아이콘+제목+부제) + 크레딧/캐시아웃 토스트(🎉/💵) | sports-live.html:1585, :1038-1043, :2217-2221 | 부분 — slipMsg 텍스트 1줄만 (sports-dashboard.html:802) | 우하단 토스트 스택 1개 구현, liveReason·캐시아웃 결과 공용 |
| 28 | 잔고 **Realtime 채널 + visibilitychange** 갱신 (10초 폴 외 즉시성) + 7초 로컬 가드 | sports-live.html:3290-3336 (§4.1) | 부분 — 10초 폴만 (sports-dashboard.html:668) | accounts Realtime 구독 + visibilitychange pull |
| 29 | 네브 배지 — 오픈 베팅 수 (`navBadge`) | sports-live.html:1091, :2207-2208 | 없음 | My bets 타이틀바 wsub에 `N open` |
| 30 | viewOnly 푸터 문구 `🔄 Open on another device` | sports-live.html:2113-2114 | 부분 — viewOnly 시 캐시아웃만 숨김, 문구 없음 (sports-dashboard.html:727-729) | 티켓 푸터에 리터럴 추가 (§7 존재 핀이기도 함) |
| 31 | 사운드 — playTapSound(베팅/캐시아웃 톤, 설정 토글 연동) | sports-live.html:1553-1556, :1225 | 없음 | 선택 사항 (설정 토글과 함께) |
| 32 | i18n 5개 언어(en/ko/ja/zh/es) + 언어 설정 시트 | sports-live.html:1106-1118, :1388-1396 | 없음 — 영문 하드코딩 | v1 영문 고정 허용, 라벨은 i18n en 리터럴과 일치 유지 |
| 33 | 다크/라이트 테마 토글 | sports-live.html:1215-1217 | 없음 — 레전드 다크 고정 | 디자인 스펙 소관 — 기록만 |
| 34 | 알림 설정 토글 4종 (Live game start / Bet results / Deposits & Withdrawals / Sound) | sports-live.html:1221-1226 | 없음 | 알림 시스템(#17) 도입 시 동반 |

### 8.4 감사 노트 (누락 아님 — 확인 기록)
- **campaign**: 앱에 'campaign' 문자열 0건 (grep 확인) — 앱에 없는 기능이므로 누락 목록 제외.
- **캐시아웃 버튼**: 앱은 현재 숨김(:2116 주석)인데 대시보드는 `cashout>0`이면 노출(sports-dashboard.html:727-729) — 대시보드가 앱보다 *많은* 케이스. 앱과 락스텝하려면 동일하게 숨기거나, 노출 유지 시 운영 결정 필요 (cash_out RPC 서버 캡이 권위라 돈 위험은 없음).
- **잠금·1X2·liveOK·slip 계산·place_bet/cash_out 경로·'···' 표기·Pending 의미**: 대시보드에 이미 앱 문법대로 존재 (sports-dashboard.html:595, :610-620, :363-375, :833-846, :875-877, :745, :688-691) — §1~5 계약 준수 확인됨.
