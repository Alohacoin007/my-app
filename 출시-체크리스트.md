# ALPEXA 출시 마스터 체크리스트
> 3종 전수감사(페이지 활성화 / 스포츠북 기능 / 보안) 통합. 근거 `file:line` 포함.
> 상태: ⛔ 출시차단 · ⚠️ 미완/위험 · ✅ 정상(확인됨)

---

## ⛔ A. 가짜잔고 / 돈 (최우선 — 한 뿌리, 클라가 서버 재검증 없이 돈을 적음)

| # | 구멍 | 근거 | 악용 |
|---|---|---|---|
| ~~A1~~ ✅ **닫힘** | ~~손실 캐시백 = LIVE 머니프린팅~~ → **제거 완료**. 클라 적립·로드재계산·claim 자가충전 삭제, UI 숨김. 코드 전체에 `credit+=` 0개 = credit 영구 0. RED→GREEN 테스트 통과. | `tests/cashback-exploit.test.js` | (재현 불가) |
| ~~A2~~ ✅ **닫힘** | ~~`ledger` 클라 임의 insert~~ → **`ledger` INSERT를 `is_admin()` 전용으로 잠금** + 클라 `syncSportsBal` 델타 완전 제거. 플레이어는 ledger에 한 줄도 못 씀. | `ledger_admin_ins` 정책 |
| ~~A3~~ ✅ **닫힘** | ~~베팅 승리 클라 자가지급 + 듀얼 정산~~ → **정산 서버 전용** (클라 `settleBets` no-op, Edge `betpay-id`가 유일). RED→GREEN. | `tests/settlement-server-only.test.js` |
| ~~A4~~ ✅ **닫힘** | ~~`crypto_holdings` 클라 쓰기~~ → **RLS가 admin-write 전용으로 이미 잠김** (`crypto_holdings_admin_write`). 클라 write-back(13502) 죽은코드 제거. 매매/스왑/입출금은 RPC/트리거. | DB 정책 |
| ~~A5~~ ✅ **닫힘** | ~~`crypto_stakes` 클라 쓰기~~ → **RLS admin-write 전용 잠김** + `stake_crypto`/`unstake_crypto` RPC 배포됨. 클라 write-back(13581) 제거. | DB 정책 |
| ~~A6~~ ✅ **닫힘** | ~~`accounts.balance` 본인 직접 UPDATE~~ → **accounts UPDATE를 `is_admin()` 전용 잠금**. 크립토 표시는 `sync_crypto_balance` RPC(서버가 holdings로 계산). 스포츠 잠금 우회로도 차단. | `accounts_lock.sql` |
| ~~A7~~ ✅ **닫힘** | ~~출금 보류 클라 위조 델타~~ → **`withdraw_hold` RPC**(원자적 차감+멱등) + 거절환불 서버 트리거. RED→GREEN. | `tests/withdraw-server-only.test.js` |
| ~~A8~~ ✅ **닫힘** | ~~`syncSportsBal` baseline drift~~ → **델타 경로 자체를 제거** (syncSportsBal은 표시 전용). 버그 클래스 소멸. | `sports-live.html:syncSportsBal` |

> **🏆 스포츠 가짜잔고 = 구조적으로 닫힘.** 모든 잔고 변동이 서버 RPC/트리거 경유(place_bet·cash_out·app_transfer·withdraw_hold·admin_*·sports-settle), 클라는 채택만. `ledger`는 admin/RPC만 쓰기. B5(매니저 admin 인증)도 완료.

## ⛔ B. 보안 / 인프라

| # | 구멍 | 근거 |
|---|---|---|
| ~~B1~~ ✅ **닫힘** | ~~`wipe-cloud.html` 인증0 전체삭제~~ → **삭제됨** (배포 브랜치에서 제거, 404) | — |
| ~~B2~~ ✅ **닫힘** | ~~`backup.html` DB 유출~~ → **삭제됨** | — |
| ~~B3~~ ✅ **닫힘** | ~~홈 푸터 → 백오피스/IB~~ → **링크 제거됨** | `index.html:650` |
| ~~B4~~ ✅ **닫힘** | ~~`crypto_trades` 전체 공개 읽기~~ → **owner/admin only** (네가 SQL 실행 완료) | `crypto_trade.sql:30-33` |
| ~~B5~~ ✅ **닫힘** | ~~백오피스 PIN 보안극장~~ → 매니저가 **Supabase admin 로그인 + `is_admin()`** (owner를 admins 등록). god-mode(set_balance/void)는 `is_admin()` RPC. PIN은 로컬 게이트로만 잔존. | `manager-mobile.html:1663-1704` |
| ~~B6~~ ✅ **닫힘** | ~~테스트 페이지 키 노출~~ → **삭제됨** (auth-test/edge-test/trading-test/launcher) | — |
| ~~B7~~ ✅ **닫힘** | ~~입금주소 가짜~~ → 실제 회사 주소로 교체. 스포츠/FX = ERC-20 USDT(메타마스크) 그대로 안전. 크립토앱 BTC/SOL=코인베이스 실주소, USDT/ETH=메타마스크, **TRC-20 제거**(주소 없음). 가짜 생성기/캐시 제거. | `crypto-live.html:275-282` |
| B8 | 🟡 **코드 준비됨(fail-closed) · 배포 대기** — `sports-settle`·`stake-accrue`를 fail-closed로 수정 + 테스트 🟢. **아직 미배포**: 함수 재배포 + `CRON_SECRET` 설정 남음. 현재 크론은 **평문 URL**로 옛(fail-open) 함수와 일치 → 정산 정상. 멱등이라 공개호출돼도 돈 안 샘 = 저위험, 의도적 보류. 닫을 때: 시크릿+재배포+`cron_secure.sql`(기존 잡 `sports-settle-5m`/`stake-accrue-daily`를 그 자리에서 토큰 URL로 교체). ⚠️ 기존 크론 잡이 이미 있었음 — 새 이름으로 추가 금지(중복). | `tests/cron-secret.test.js`, `supabase/sql/cron_secure.sql` |

## ⛔ C. 컴플라이언스 (실거래 합법성 — 규제시장 필수)

| # | 구멍 | 근거 |
|---|---|---|
| C1 | ⏳ **풀 KYC 보류(벤더 필요)** — 나이/Geo/약관은 닫음. 문서·셀피·제재조회는 Sumsub/Onfido/Persona 등 외부 벤더 연동 필요(내가 못 만듦). 연동 지점만 표시. | `signup.html` |
| ~~C2~~ ✅ **닫힘** | ~~나이 게이팅 없음~~ → **18세 미만 자동 차단**. 클라 게이트(`compliance.js meetsMinAge`, fail-closed) + **서버 트리거 `trg_guard_player_compliance`가 players INSERT에서 강제**. RED→GREEN. | `tests/compliance.test.js`, `compliance.js`, `supabase/sql/compliance_guard.sql` |
| ~~C3~~ ✅ **닫힘** | ~~Geo 차단 없음~~ → **제재국(OFAC: 쿠바·이란·북한·시리아·러시아·벨라루스) 자가신고 차단**. 국가선택 즉시 거부 + 서버 트리거 동시 강제. | `tests/compliance.test.js`, `compliance.js`, `compliance_guard.sql` |
| ~~C4~~ ✅ **닫힘(앱 경로)** | ~~자가배제 localStorage 지우면 풀림~~ → RG 설정은 이미 `players` 컬럼(서버소유)+로드시 `pullRG`. **일방향 잠금 트리거 `trg_guard_self_exclusion` 추가**: 비관리자는 `self_exclude_until`을 늘릴 순 있어도 줄이거나 0으로 못 씀(관리자만 해제). localStorage 비워 0 써도 서버가 거부. RED→GREEN. ⚠️ 더 깊은 층(베팅시점 거절)은 `place_bet` 커밋 후 RPC 상단 체크 추가(SQL에 주석으로 제공). | `tests/self-exclusion.test.js`, `supabase/sql/responsible_gaming.sql` |
| ~~C5~~ ✅ **닫힘** | ~~약관/법무 문서 실제 없음~~ → **실제 문서 생성 + 링크 연결**: `terms.html`·`privacy.html`·`risk.html`(모바일 스타일, 표준 조항). 동의 체크박스 `<span>`→`<a target=_blank>`(체크박스 토글과 분리). ⚠️ 변호사 검토·관할/엔티티 기입 필요(문서 내 명시). | `signup.html`, `terms.html`, `privacy.html`, `risk.html` |
| C6 | 📋 **런북 제공 — 네 실행 대기** — 가입 OTP가 Supabase 기본 발신자라 스팸行+시간당 한도. **단계별 설정 런북 작성**(`이메일-SMTP-설정.md`): Resend 도메인 인증→SPF/DKIM/DMARC→Supabase SMTP→한도상향→템플릿(`{{ .Token }}` 필수)→4단계 검증. 코드변경 0(설정 연결시 자동 적용). **너가 Supabase+DNS에서 실행해야 닫힘.** | `이메일-SMTP-설정.md` |

## ⚠️ D. 기능 버그 / 미완성

| # | 항목 | 근거 |
|---|---|---|
| D1 | **SGP 상관관계 미반영** — 같은경기 다리를 독립 팔레이로 곱함 → 북메이커 손실엣지 | `sports-live.html:1926` |
| D2 | 최소 베팅 없음 / 최대 베팅 클라전용 (서버 재검증 미확인) | `:1943-1944` |
| D3 | 단일 ML push 환불 정확성 검증 필요 | settle `:185`, `:2181` |
| D4 | UFC 베팅가능하나 `live_games` 피드엔 없음 (단일소스 위배) | `sports-games/index.ts:21-30` |
| D5 | 저장 비번 base64(평문급) 로컬 저장 | `login.html:256-259` |
| D6 | **핵심 RPC 미커밋** — `place_bet`/`app_transfer`/requests·RG 트리거가 repo에 없어 서버검증 감사 불가 | (Supabase 배포만) |
| ~~D7~~ ✅ **닫힘** | ~~`site/team.html`, `site/team-hub.html` 고아·미게이트 공개 렌더~~ → **삭제됨**. 배포 어디서도 링크 안 됨(참조 0개), 민감정보 없음(마케팅 프로토타입). git 히스토리 보존. | `git rm` |
| D8 | 죽은 링크 `Dashboard.html`(대문자) 외 6개 → 전부 고아 `crypto.html` 안에만 → **배포 안 하면 해결** | `crypto.html:807,2827,3525` |
| ~~D9~~ ✅ **확인됨(이상없음)** | ~~FX 출금 이중차감 의심~~ → **전수조사 결과 이중차감 없음**. FX 출금 분기(`trading.html:2545`)는 잔고를 한 줄도 안 건드리고 `pushRequest`로 pending 요청만 삽입. `withdraw_hold`는 sports-only 서버거부. **유일 차감 = 승인시 `apply_fx_withdraw_balance` 1회**. 로컬 캐시는 서버값 미러링만. 불변식 회귀 테스트로 잠금. | `tests/fx-withdraw-no-double.test.js` |
| ~~D10~~ ✅ **닫힘** | ~~출금 QR "데모 주소 쓰기" 버튼(샘플 BTC 채움)~~ → **제거됨**. 카메라 불가시 가짜 주소 주입 대신 "닫고 직접 입력" 안내. 샘플 주소 주입 0개. | `crypto-live.html` QrScanner |
| ~~D11~~ ✅ **닫힘** | ~~ALPXS 시세 차익 의심~~ → **차익 불가 증명 + 스왑 라우팅 수정**. (1) 클라 폴백은 A4 RLS 락이 adopt 때 서버진실로 되돌려 토큰 안 생김(차익 0). (2) ALPXS 스왑이 잘못된 `isSec` 사전분기로 클라경로 타던 것 → **`swap_crypto` RPC로 라우팅**(서버가 stock-prices 피드로 ALPXS 가격 공급, 120s 신선도). 가격 없는 자산만 `no price` 폴백. RED→GREEN. | `tests/alpxs-swap.test.js`, `crypto-live.html:13946` |

## 🗑️ E. 삭제 / 게이트 (배포 제외)
- **파괴적:** `wipe-cloud.html`, `backup.html`, `reset.html`
- **테스트/개발:** `auth-test.html`, `edge-test.html`, `trading-test.html`, `launcher.html`
- **백오피스(서버인증 뒤로):** `manager.html`, `manager-app.html`, `manager-mobile.html` + 고객사이트 링크 제거
- **고아 프로토타입/중복:** `crypto.html`, `crypto-app.html`, `sports.html`(레거시), `index-suite-intro.html`, `site/cashout-preview.html`, `site/team.html`, `site/team-hub.html`

---

## ✅ F. 이미 정상 / 안전 (확인됨 — 다시 안 건드림)
- `place_bet` · `cash_out` · `crypto_trade` · `swap_crypto` · `fx_open/close` = **서버권위 + 멱등 + auth.uid() 검증**
- `app_transfer` = 가장 견고한 머니 경로 (원자적, 멱등 ref)
- 서버 정산 `sports-settle` = 멱등 `betpay-id` + claim
- 웰컴보너스/리퍼럴 = 서버강제, 1인1회 PK
- **서비스키 유출 없음**, anon은 accounts/positions/settlements 못 읽음 (prices만 공개=정상)
- PWA/설치, 멀티디바이스 동기화, 라이브 스코어/배당, 베팅/팔레이/슬립, 입출금요청 흐름, 출금 무결성 가드(백오피스), 배당표시
- **`ledger.ref` UNIQUE 제약** (방금 추가 — 같은 ref 이중적용 DB 차단)

---

## 🔧 G. 닫는 순서 (임팩트 큰 것부터, 계약대로 RED→GREEN)
1. **A1 캐시백 익스플로잇 제거** — 유일하게 *지금 작동중인* 머니프린팅. 최우선.
2. **쓰기 잠금 SQL 1방** — `ledger`·`accounts`·`crypto_holdings`·`crypto_stakes` 쓰기를 admin/RPC 전용으로 → **A2·A4·A5·A6 동시 차단**.
3. **클라 정산/델타 경로 제거** — `syncSportsBal` 추측델타 삭제, 정산은 서버 전용 → A3·A8 차단. (각 경로 RED→GREEN)
4. **출금을 서버 RPC로** (`withdraw_hold` 멱등) → A7.
5. **파괴 페이지 배포제외 + 푸터 백오피스 링크 제거 + `crypto_trades` 읽기 제한 + 입금 더미주소 교체** → B1~B7.
6. **컴플라이언스** (KYC/나이/Geo/약관/책임도박 서버강제) → C1~C5. *실거래 트래픽 전 필수.*
7. **핵심 RPC 커밋 & 리뷰** (place_bet/app_transfer/트리거) → D6.
