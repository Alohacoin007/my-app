# 📌 Alpexa 리마인드 리스트 (할 일 / 나중에)

> 세션이 끊겨도 여기 보면 다음에 뭘 할지 알 수 있음. 끝나면 ✅ 표시.

## 🚀 첫 고객 받기 전 체크리스트 (실고객 전 반드시)
> "진짜 돈/진짜 사람" 받기 전에 이건 다 돼 있어야 함. 지금은 고객 0명 = 위험한 작업도 안전하게 할 황금기.

**🔐 보안 (제일 중요)**
- [ ] **🌐 대공사: 로컬저장 데이터 전부 → 서버화 (사용자 지시 2026-06-17)** — "서버가 유일한 진실"
  - 배경: Recent Activity가 기기마다 달랐음 → 원인은 베팅·재무내역을 localStorage에 기기별로 저장. 베팅 활동은 서버(positions)기반으로 고침✅. 사용자: "다른 로컬저장 데이터도 전부 서버화."
  - 순서: ① 스포츠 마무리(입금/출금/이체 활동을 서버 requests/ledger에서 읽기) ② 이체 서버화(RPC, 즉시이체) ③ 전체 localStorage 인벤토리의 "돈/상태 핵심" 항목 모두 서버화 ④ UI취향(테마/언어 등)만 로컬 유지.
  - 방식: 돈 흐름이라 **테스트환경 먼저**, 검증 후 라이브. 활동피드+이체를 묶어서 처리.
  - **마스터 인벤토리 (2026-06-17 전수조사, 56키). 돈/상태 핵심(A버킷) 현황:**
    - ✅ **이미 서버권위 (로컬은 캐시일 뿐)**: balances·serverBalances·sportsBalance·cryptoBalance(=accounts.balance, pullBalances) / cryptoHoldings·staked(=crypto_holdings·crypto_stakes+Realtime) / positions·fxLive(=positions+Realtime) / openBets(=positions+Realtime, 활동도 서버유래로 고침).
    - ⚠️ **남음(진짜 서버화 대상)**:
      1. **거래/활동 내역**: withdrawals·transactions·funding·txns → 서버 `requests`+`ledger`에서 읽기 (현재 기기별 로컬 → 불일치 원인). 
      2. **이체 처리**: appliedTransfers + 이체 크레딧 → 즉시이체 **RPC**로 (위조·중복 차단).
      3. **베팅 히스토리**: settledBets → 서버 `settlements`에서 읽기.
      4. **FX 주문 히스토리**: fxPending(미체결 지정가)·fxClosed(청산내역) → 서버에서.
    - B버킷(컴플라이언스, 중기): maxBet·maxDeposit·maxWithdraw·kyc·alpexaSettings(셀프exclusion/한도)·referralRedeemed·recurring → 서버 `controls`/계정설정으로.
    - C버킷(로컬 유지 OK): theme·prefs·leverage·notif·lastServer·favs·alerts·pins·dataVersion 등 UI취향.
    - D버킷(신원/캐시/관리자설정): me·cred·bio·companyAddr·margin·cashback·wdFee 등 — 일부(margin/cashback/wdFee/companyAddr)는 사실 **운영설정이라 서버 `controls`로 옮기는 게 맞음**(백오피스가 진실).
  - **작업 묶음 순서**: ⓐ 활동내역+이체 서버화(1+2, 테스트환경) → ⓑ 히스토리(3+4) → ⓒ 컴플라이언스(B) → ⓓ 운영설정(margin/cashback/fee/addr) 서버로.
  - **진행(2026-06-17)**: ✅ 스포츠 활동피드 서버화(positions+requests) / ✅ 스포츠 정산내역 서버화(settlements) / ✅ **이체 서버화 완전 종료 & 검증**: RPC `app_transfer`(SECURITY DEFINER, 소유권+잔액검증, ledger or crypto_holdings, 멱등성 ref) + 3개 앱 이체버튼 연결 + **옛 클라 이중입금 경로 전부 차단**(pullIncomingTransfers/per-leg ledger OFF) + 이체후 양쪽 잔고 즉시 갱신(스포츠 __sbLocalTs 가드 해제). 크립토→스포츠/FX 등 검증 완료. 옛 이중입금 $100k 정정 완료.
  - **이체 후속 픽스(2026-06-17)**: ① 낙관적 업데이트가 syncSportsBal 델타로 **이중기록**($1M→$2M) → __sbLastPushed=balance로 억제. ② 크립토 이체 버튼이 stale 캐시잔액으로 비활성화 → valid에서 잔액조건 제거(RPC가 진짜 가드). ③ 크립토 이체 실패 silent → 팝업 표시. ④ **app_transfer가 크립토 leg일 때 accounts.balance도 즉시 갱신**(update accounts ±p_amount) → 받는 쪽 1초 내 반영. (RPC는 SQL CREATE OR REPLACE로 갱신, Edge배포 아님). 스포츠 기본 이체방향 Sports→Crypto.
  - **추가 완료(2026-06-17)**: ✅ 운영설정→서버 controls(백오피스 저장+스포츠 fee/주소 읽기; 한도/마진/캐시백은 controls에 저장됨, 앱 enforce는 추후) ✅ 크립토 거래내역 서버 activity 병합(기기공통, 티켓dedup) ✅ FX 청산내역 server settlements(fx_close) 병합.
  - **남은 서버화**: ✅ maxBet 서버강제(controls). ✅ **자기제외/일일한도 → 서버(players 컬럼 self_exclude_until·deposit_limit·loss_limit·reality_check_min + guard_self_exclude 트리거로 일방향=고객이 못 줄임, 해제는 admin만)**. 스포츠 pullRG/pushRG. **SQL 실행 필요**(players 컬럼+트리거). N/A: 크립토 fee/주소(자체 생성주소). 
  - **의도적으로 로컬 유지**: ① **fxPending(FX 미체결 지정가)** — 로컬=단일기기라 이중체결 없음(안전). 서버동기화하면 두 기기 동시체결 위험 → positions급 claim-lock 필요 → 진짜 멀티기기 지정가 원할 때 전담작업. ② Face ID 자동로그인(cred/bio)=기기고정 인증(서버 가면 안 됨). ③ theme·lang·favs·pins 등 UI취향. 
  - **결론: "동작이 로컬에 의존"하던 기능 전부 서버화 완료** (fxPending만 의도적 로컬 유지=안전).
- [ ] **🎯 다음 세션(내일): "가짜 잔고 3구멍 닫기" — 기능별 서버화 후 잠금**
  - **2026-06-17 조사 결과 — 클라가 아직 직접 쓰는 곳(이체 서버화 후 남은 것):**
    - `ledger` insert: sports-live `ledgerPost`(1976) — `syncSportsBal`(2164)이 잔고 델타(+/−) 기록(베팅차감=음수, 캐시아웃/로컬승리=양수). → 음수만 허용 불가(캐시아웃 양수).
    - `accounts` update: crypto-live(1606) 크립토 hero잔고 push(코인값=실시간가격이라 서버계산 불가) / signup(766) 시작잔고 insert.
    - `crypto_holdings` upsert: crypto-live(13316) 매 거래.
    - `settlements` insert(P&L 리포트): sports(3195)/crypto(13886)/trading(4034) — 잔고 영향X, 별도.
  - **닫는 순서(기능별: 서버RPC로 옮김 → 테스트 → 그 테이블 직접쓰기 잠금):**
    1. 크립토 **매수/매도 → 서버 RPC**(trade_crypto: 검증후 crypto_holdings 갱신) → crypto_holdings 쓰기 잠금
    2. 스포츠 **캐시아웃(+ 로컬 승리크레딧 제거) → 서버 RPC** → ledger insert를 admin/트리거 전용 잠금(베팅차감도 RPC나 음수허용)
    3. **가입 시작잔고 → 서버 트리거**(accounts INSERT 시 서버가 고정금액) + **크립토 accounts.balance = 서버가 holdings USDT로 계산**(드롭다운엔 코인값 말고 USDT현금 표시 타협) → accounts update/insert 잠금
  - 골든타임: 고객 0명 → 잠가도 테스트계정만 영향(안전 실험). 🛡️ 출금가드가 실손실 막아서 **안 급함**.
  - 끝나면: Gap≈0 유지 + 패널이 보너스/크립토 holdings를 explained로 세게 다듬기 → Gap이 사기탐지기 됨.
  - 2026-06-17 해커 시점 감사 결론: 남의돈/정보·관리자·키는 다 막힘✅, cust_id 우회로도 닫음✅. **남은 단 하나 = 로그인 유저가 "자기 화면 잔고"를 위조하는 3경로.** 출금가드로 인출은 막혀서 실금전피해는 0 → **당장 안 급함, 실고객 받기 직전에.**
  - 닫을 순서(감사 확정): ⓐ `accounts` UPDATE/INSERT를 admin/트리거 전용으로 + 크립토 hero balance push(crypto-live:1606) 중단(서버 보유수량에서 산출) + 가입 시작잔고 서버(트리거/RPC)로 ⓑ `ledger` INSERT를 admin/트리거 전용 + FX이체(trading:3428/3443)·스포츠델타(sports:1960)를 RPC로 ⓒ `crypto_holdings` 쓰기를 매수/매도 RPC로.
  - 방식: FX 동기화처럼 **테스트환경에서 먼저 만들고 검증 후 라이브.** 3개 앱+signup 다 손댐 = 중간규모 집중세션.
- [~] **RLS 보안 잠그기 — 거의 완료**
  - ✅ crypto_holdings/crypto_stakes: 본인계좌 읽기/쓰기 + 관리자전체 (is_admin() OR owns_acct(acct_no)). crypto_holding_ledger: 읽기만(쓰기는 트리거 SECURITY DEFINER 전용=고객 가짜장부 차단). 검증: 폰·컴·백오피스 다 정상.
  - 나머지 테이블(accounts/requests/positions/ledger 등)은 **이미 잠겨 있었음**(is_admin()/owns_cust/owns_acct 패턴). 헬퍼 is_admin()·owns_cust()·owns_acct() 존재.
- [~] **★ 잔액쓰기 → 서버함수 이전 — 진행중 (스포츠 정산✅ + 출금가드✅)**
  - ✅ **스포츠 서버 정산 엔진**: Edge Function `sports-settle`(ESPN결과→채점→claim→ledger 크레딧, service_role) + cron 5분(job 7). E2E 검증: 테스트베팅 Knicks ML 자동정산 $500→$525.
  - ✅ **출금 가드**(Mgr v85): 무결성🚩 계정 출금 승인/지급 강경차단 → 가짜잔액 만들어도 돈 못 빼감(실제 피해 차단).
  - 남음(완전닫기): ① 클라 베팅정산 크레딧 제거(서버가 유일 정산자로) + 정산베팅 화면을 서버에서 읽기 ② 캐시아웃·FX이체-입금 서버화 ③ ledger insert를 "음수(차감)만 클라 허용" 잠금. 이 3개 끝나면 가짜잔액 자체가 불가.
  - debug: sports-settle?debug=1 → ESPN 결과맵 반환.
- [ ] **(원래 항목) 잔액쓰기 → 서버함수(RPC) 이전 세부**
  - ★ **보안 감사 완료 (2026-06-17, RLS 정책 전수 덤프 기준)**: 결과 ↓
    - ✅ #1 남의 데이터 읽기: 전 테이블 SELECT가 `is_admin() OR 본인소유`로 잠김 → 크로스유저 읽기 불가. controls/pricing_marks만 public read(비밀 아님).
    - ✅ #2 관리자 위조: `is_admin()`=admins테이블+auth.uid(), **admins 테이블 RLS on + 정책 0개 = 전면차단**(유저가 자길 admin 못 넣음). 위조 불가.
    - ✅ #4 키 노출: service_role 하드코딩/누출 없음, 클라엔 publishable만.
    - 🔴 #3 가짜잔고(자기것만): **3경로 확정** — ① `accounts` UPDATE 본인허용(크립토앱 crypto-live:1606이 balance 직접 push 중) ② `ledger` INSERT 본인허용(FX이체·스포츠) ③ `crypto_holdings` INSERT/UPDATE 본인허용(qty). +가입시 accounts INSERT balance, settlements INSERT(리포트만). **단 출금가드로 인출은 막힘=실금전피해 0.**
    - ⚠️ 확인필요: `players` UPDATE가 cust_id 변경을 안 막음(with_check는 auth_id만) → cust_id UNIQUE 없으면 cust_id 바꿔치기로 타인 ledger/positions 접근 가능. cust_id UNIQUE/PK 제약 확인할 것.
    - ✅ **닫힘(2026-06-17): cust_id에 UNIQUE/인덱스 없어 #1급 우회로 열려있었음**(공격자가 자기 players.cust_id를 피해자 것으로 UPDATE → owns_cust로 타인 ledger/positions/requests/settlements 읽기·쓰기). **조치 완료: `ALTER TABLE players ADD CONSTRAINT players_cust_id_unique UNIQUE (cust_id);` 실행 성공.** 이제 충돌로 바꿔치기 불가 → 크로스유저 접근 차단. (더 깊게는 cust_id 변경 자체 금지 트리거도 가능 — 선택)
  - 발견: ledger insert 정책이 `is_admin() OR 본인 cust_id` → **고객이 콘솔로 자기 ledger insert해서 잔액 무한정 부풀리기 가능**(트리거가 적용). 같은 구멍: 스포츠 베팅 장부델타, FX 이체(v119) — 둘 다 클라가 ledger 직접 insert.
  - 정확한 닫기 순서(감사 확정): ⓐ accounts UPDATE/INSERT를 admin/트리거 전용으로 + 크립토는 hero balance push 중단(서버 보유수량에서 산출) + 가입 시작잔고는 서버(트리거/RPC)로 ⓑ ledger INSERT를 admin/트리거 전용으로 + FX이체·스포츠델타를 RPC로 ⓒ crypto_holdings 쓰기를 매수/매도 RPC로. 3개 앱+가입 다 손댐 → FX처럼 테스트환경 집중세션.
  - 실질방어: 출금=백오피스 승인 + 무결성패널이 "입금<<잔액" 잡음 → **돈은 못 빼감**. 그치만 가짜잔액 가능 → 실고객 전 막아야.
  - 올바른 설계: ledger/settlements **직접 insert를 관리자/트리거 전용**으로 잠그고, 베팅·이체·거래를 **서버 RPC**(SECURITY DEFINER, 검증 후 ledger 기록)로. 고객은 "요청"만.
  - FX처럼 큰 작업(핸들러 다 손댐) → 테스트환경에서 집중 세션으로. FX 이체는 현재 작동(RLS 통과).
  - **결정한 다음 스텝(나중에): "출금 가드 강화"** — 무결성 플래그(입금<<잔액, 🚩)된 계정은 **출금 승인 자동 차단/강경경고**. 가짜잔액 만들어도 돈 못 빼가게(실제 피해 차단). 바운드·고가성비. 백오피스 승인 로직 + acctIntegrity/INTEGRITY_FLAG_GAP 활용.
  - 근본수정(더 나중): 스포츠 서버정산 엔진(Edge Function+결과폴링+cron) → 그 후 ledger insert를 admin/트리거 전용으로 잠금.
- [ ] **출금 2FA 진짜로 (TOTP)** — 지금은 데모(코드 화면에 보임). 인증앱 방식, 무료.
- [ ] **로그인 패스키(passkey)** — 폰에 비번 저장(base64) 대신 보안칩.
- [ ] **백오피스 권한 다단계** — admin auth는 있음. 직원/에이전트 권한 분리.

**💾 데이터 안전**
- [ ] **PITR 켜기** (월 $100, Supabase 대시보드) — "지웠어도 직전 시점 복원". ← 첫 고객 직전에 켜기(지금은 지킬 데이터 없어 돈 아낌).
- [~] **소프트 삭제 정책** — 코드에서 진짜 delete 금지 → status='closed'.
  - ✅ 고객(deletePlayer): players.status='closed'로 소프트삭제(accounts/positions/commands 안 지움, 복구가능). 버튼 "Close account (keeps data)". (Mgr v82)
  - ✅ 요청(deleteRequest): status='voided'로 소프트삭제(audit 보존, 승인합계서 제외). 버튼 "Void record". (alpexa-sync v9/Mgr v84)
  - 남음: settlements.delete(538) 하드삭제 — 거래정산 기록이라 지양 권장(status 컬럼 없어 별도 설계 필요, 드문 관리자 정정용). positions.delete(청산정리)는 transient라 OK.
- [ ] **자동 외부 백업** — 매일 외부 저장소로 export (Supabase 자체 사고 대비). "백업 서버" 개념을 별도서버 말고 자동export로.
- [ ] **별도 Supabase 스테이징** — 테스트 완전 분리(지금은 production 공유). 출시 직전.
- 참고: 잔액은 거래기록(requests/settlements/activity)으로 재구성 가능(이미 있음). ⚠️대량삭제 절대 금지.

**🧹 제품/운영**
- [ ] **(결정 대기) 가입 기본 서버 / 스포츠 전용 여부** — 신규 시작잔고를 스포츠 $500 / 크립토 $0 / FX $0으로 바꿈(2026-06-16, signup.html rows + localStorage seed, 백오피스 INTEGRITY_OPENING 700→500). 근데 가입 기본 선택은 아직 **Spot(FX)** → 그대로 가입하면 FX 앱이 $0으로 열려 어색. "일단 스포츠만"이면:
  - **A.** 가입 기본 선택을 **Alpexa Sports**로 (가입→스포츠 앱 $500 바로 열림, 추천·되돌리기 쉬움)
  - **B.** 가입화면에서 Spot·Crypto 숨기고 **Sports만** 노출 (강한 스포츠 전용)
  - **C.** 지금처럼 3개 다 선택 (Spot/Crypto는 $0으로 열림, 입금해 사용)
  - 코드 위치: signup.html `data.server`(기본 'live') · step0 SERVERS · goToApp 라우팅(demo→sports-live, stocks→crypto-live, else→trading).
- [~] **데모 데이터 청소 — 앱 데이터 전부 삭제 완료 ✅ (2026-06-16)** — players/accounts/ledger/positions/requests/settlements/activity/logs/crypto_holdings·stakes·holding_ledger/agent_links 등 전부 hard-delete(백지). `agents`(추천코드 설정)·`prices`(시세)·시스템 테이블은 보존. 남음: ① auth Users(로그인 계정) 대시보드에서 삭제 ② 버전 스탬프(?ver) 제거 ③ 본인 새로 가입해 첫 계좌 시작.
- [x] **인트로(랜딩) 페이지 ✅** — index.html = 스플래시 → 3페이지 스와이프(마켓·보안·보너스). 메인=SpaceX(SPCX) 실시간(Finnhub), FX·BTC 실시간. 점 클릭/스와이프, 푸터 2버튼 통일. Get Started/Claim→가입.
- [x] **★ 가입 인증메일 도달률 (SMTP) — 완료 ✅** — Resend SMTP(자체 도메인 alpexa-suisse.com, 인증완료) 연결됨. Supabase Auth → Resend로 발송. 로그상 "Alpexa verification code" 메일 Delivered/Opened 확인. (Bounce는 오타 주소뿐). 발신 noreply@alpexa-suisse.com.
- [x] **★ 자체 도메인 연결 — 완료 ✅ (구조 A)**
  - **홈 = `alpexa-suisse.com`(Webflow), 앱 = `app.alpexa-suisse.com`(GitHub).** 둘 다 라이브 확인됨.
  - GoDaddy DNS: `@` A `198.202.211.1`(Webflow) · `www` CNAME `cdn.webflow.com`(Webflow) · `app` CNAME `alohacoin007.github.io`(앱). GitHub CNAME 파일=`app.alpexa-suisse.com`. Webflow "all domains updated" + Publish 완료.
  - 앱 코드에 옛 github.io 하드코딩 없음(전부 상대경로) → 새 도메인서 자동 동작.
  - **고객 가입링크 = `https://app.alpexa-suisse.com/`** (인트로→가입).
  - 남은 마무리(작음): Webflow 홈에 **"앱 열기/로그인" 버튼** → `https://app.alpexa-suisse.com`. (GitHub Enforce HTTPS ✅ 완료)
- [ ] **실고객 입출금 실전 테스트** — 실제 결제 연동 / MetaMask 출금.
- [ ] **법률/라이선스** — 진짜 돈이면 국가별 규제·약관·KYC/AML 확인(소유자 트랙).

---

## 🔴 높음
- [x] **테스트 환경 (FX 작업용) — 세팅 완료 ✅**
  - FX 샌드박스: `trading-test.html` (trading.html 복사본, 항상 ⚠TEST 배너). 여기서 서버-잔액 개발 → 검증되면 라이브 trading.html에 적용.
  - **테스트 계정(test1@test.com)**: player_id `658f1724-aca1-43d6-a692-2e5029a11937` / FX **FX-348713**($100) / crypto CR-529168 / sports SP-364668. FX 테스트는 FX-348713으로만(메인 FX-478719 안전).
  - 계정 링크 컬럼: accounts.**player_id** (cust_id 아님). 메인 player_id=b1bf6cc8-2fe6-4bd0-9a0a-1fcceb7836ea.
  - 같은 Supabase(production) 공유 → DB 트리거는 공유됨(스코프·additive로 신중, 테스트 계정만 승인하며 테스트). 실고객 직전 **완전 별도 Supabase 스테이징** 권장(gold standard).
- [ ] **★ 출시 전 필수: 데이터 안전 (진짜 고객 받기 전 반드시)**
  - **대량 삭제 절대 금지** — 무조건 한 줄씩, 바꾸기 전 백업/보여주고 확인. "전체 초기화" 다신 안 함.
  - **PITR(초 단위 복원) 켜기** — Supabase 대시보드, 월 $100 (지금은 매일 백업 7일치만 있음)
  - **소프트 삭제 정책** — 고객/계좌를 진짜 delete 하지 말고 status='closed' 표시 (항상 복구 가능)
  - 잔액 문제는 거래기록(requests/settlements/activity)으로 재구성해 복원 (백오피스 무결성 패널)
- [x] **스포츠 라이브 베팅 2단계: 배당 빠른 갱신 — 완료 ✅ (2026-06-17, 스마트폴링)**
  - 1단계 완료 ✅: 득점 순간 자동 잠금(ESPN 9초 폴링, ~15s), 오래된 배당 잠금(>90s), 백오피스 ON/OFF 토글(controls.live_betting), 🔒+사유 표시
  - 2단계 완료 ✅: sports-odds Edge Function = **스마트폴링**(ESPN 무료로 라이브경기 감지 → 라이브 종목만 1분 호출, 나머지 >9분 stale만 호출, idle은 skip=0크레딧). cron job 4 = `* * * * *`(1분). $59/100K 플랜 안에서 라이브 작동. ?force=1/?sport= override. 함수 응답에 live/polled/skipped.
  - (선택, 나중) 라인 급변 시 잠금, 라이브 last-look 재호가 추가
- [x] **FX 서버-잔액 마이그레이션 — 완료 ✅ LIVE (trading.html v119)**
  - 추가 트리거: apply_fx_deposit_balance / apply_fx_withdraw_balance (requests UPDATE→ledger→accounts.balance, ref=local_id, FX만). 기존 apply_settlement_to_balance(거래손익)와 함께 FX 잔액을 서버가 소유.
  - 앱 변경(4): 전체-equity push 제거(clobber해결) / 입출금 로컬 addBalance 제거(트리거가 함) / 이체는 ledger 델타 / adopt를 idle기준(fxFlat 제거) / 거래손익 로컬 addBalance는 즉시표시 유지(트리거가 서버 반영, adopt 정합).
  - 샌드박스(trading-test.html/테스트계정 FX-348713)에서 입금·출금·거래청산 전부 검증 후 라이브 적용. 메인계정 확인: $100 정상, clobber 없음.
  - 주의(엣지): 라이브 전환 시 **열린 포지션이 있으면** 옛 push가 equity를 저장해둬서 미실현만큼 잠깐 부풀 수 있음 → flat일 때 전환하면 안전(메인은 flat이었음).
  - 잔여: 이체 완전 정합은 상대 앱 leg도 ledger화 필요(현재 FX leg만). USDT/현금 완전 델타화는 테스트환경에서 추후.
- [x] **전체 서버-잔고 마이그레이션 — 3개 앱 완료 ✅ (스포츠·크립토·FX)**
  - 스포츠 잔액✅, 크립토 코인·입금·출금·스테이킹✅, FX 입금·출금·거래손익✅ — 전부 "서버가 주인"
  - 핵심 설계: 앱은 잔액 **통째 push 안 함**. 변화는 **델타(거래/입출금)만 서버 기록**(트리거/ledger), 잔액은 **서버에서 받아 표시만**. clobber·이중계산 구조적 차단.
  - 잔여 다듬기: 앱간 **이체** 완전 정합(양쪽 leg ledger화), 크립토 USDT 완전 델타화. (실시간 푸시는 3개 앱 다 적용됨 ↓)
- [x] **3개 앱 실시간(Realtime) 동기화 — 완료 ✅ LIVE (DB 퍼블리케이션 켜짐)**
  - FX(positions, REPLICA IDENTITY FULL)·크립토(crypto_holdings·crypto_stakes)·스포츠(positions·accounts) 전부 Supabase Realtime 구독 추가 → 폴링 기다리지 않고 거의 즉시 전파. 폴링은 안전망으로 유지.
  - 크립토/스포츠는 기존 adopt/pull 루프를 Realtime 이벤트가 "깨우는" 방식(7초 busy 가드 유지 → 내 거래 중엔 clobber 안 함).
  - **DB 퍼블리케이션 켜짐 (2026-06-17)**: positions·accounts는 이미 멤버였고, crypto_holdings·crypto_stakes를 supabase_realtime 퍼블리케이션에 추가함(멱등 DO 블록으로 실행, 성공).
- [x] **FX 멀티기기 포지션 동기화 + 실시간 + 크레임 잠금 — 완료 ✅ LIVE (trading.html)**
  - 서버 `positions` 테이블 = 공유 진실(acct_no로 매칭, cust_id는 기기마다 흔들려서 안 씀). 한 기기에서 연 포지션이 모든 기기에 뜨고, 한 곳에서 닫으면 모든 곳에서 사라짐.
  - **Supabase Realtime** 구독(`channel().on(postgres_changes, filter: acct_no=eq.X)`)으로 거의 즉시 전파. 폴링은 4초 안전망으로만. DELETE 이벤트가 acct_no를 담도록 `ALTER TABLE positions REPLICA IDENTITY FULL;` 적용함.
  - **크레임 잠금(claim-lock)**: 닫을 때 open 행을 원자적 delete+`.select()`. 행을 받은 단 한 기기만 손익을 잔액에 반영 → 두 기기서 동시에 닫아도 이중계산 안 됨. 진 기기는 "Already closed" 토스트. 1회 재시도, 오프라인/단일기기면 로컬 처리.
  - 테스트(trading-test.html)서 완전 검증 후 라이브 적용. 실시간이 너무 빨라서 더블클로즈할 틈도 없음 + 크레임 잠금이 최후 안전망.
- [x] **크립토 멀티기기 보유(holdings) 동기화 — 완료 ✅ (수량 모델, v99)**
  - 서버 `crypto_holdings`(acct_no, asset, qty) + ledger/트리거. 코인을 **수량**으로 저장(진짜 거래소).
  - 앱: 진짜 사용자 변화(매수/매도/입금)에만 qty 스냅샷 기록. **로드/채택 echo땐 안 씀**(clobber 방지, __cbSkipWrite/__cbLocalTs).
  - 채택(adopt): idle(>7s)일 때 서버 qty를 진실로 받아옴. value=qty×가격, USDT=현금. **accounts.balance를 앵커로 쓰면 hero push와 피드백 루프로 폭주($150→$662) → 안 씀.** 서버 보유수량이 총액 진실.
  - 기기 간 $1~2 차이는 정상(실시간 가격). 복구툴: crypto-live.html?reset=1
  - 동기화 견고화(v100~v106): 수량(qty) 시그니처로 비교(가격변동/반올림에 안흔들림), 첫게이트 제거(매수 누락 버그), mount-adjust 끔, useLiveAssetTicks 인덱스 정렬(새코인 가짜 +900% 수정), 디버그(?dbg=1) 추가 후 제거.
- [x] **크립토 입금 서버크레딧 — 완료 ✅ (v106)**
  - DB트리거 apply_crypto_deposit_holding: 크립토 입금 승인 순간 crypto_holding_ledger에 USDT +net 기록 → crypto_holdings USDT 올림. **앱 꺼져 있어도/모든 기기** 반영. 중복방지(ref=dep-<local_id>).
  - 앱은 클라이언트 USDT 크레딧 제거(이중계산 방지) → adopt로 받아서 표시. 검증: 앱 닫고 승인해도 오름.
- [x] **크립토 스테이킹 멀티기기 — 완료 ✅ (v107)**
  - 서버 `crypto_stakes`(acct_no, asset, usd원금, period, since). 코인 동기화와 같은 패턴(시그니처 비교, echo 방지, idle adopt, busy 게이트 공유). 검증: 폰 스테이킹 → 컴에도 뜸.
- [x] **크립토 출금 서버크레딧 — 완료 ✅ (v108)**
  - DB트리거 apply_crypto_withdraw_holding: 승인 시 crypto_holdings USDT −gross(ref=wd-<local_id>). 앱 클라 차감 제거. 입금/출금 승인 시 forceCryptoAdopt()로 즉시 재동기화(clobber창 거의0+즉시반영). 검증: 출금 됨.
  - **크립토 = 코인·입금·출금·스테이킹 전부 서버가 주인 ✅** (거의 완전 전환)
  - 잔여 엣지: USDT가 앱 스냅샷 + 서버 델타 혼용 → 완벽엔 USDT도 완전 델타화(거래핸들러) 필요. 테스트환경에서 나중에.
  - 남음(다음): **FX 잔액 서버화**(테스트환경 권장). 실시간 푸시(8s폴링→Realtime).
- [~] **멀티기기 잔액 실시간 동기화** (남은 다듬기)
  - ✅ **FX 포지션 멀티기기 동기화 완료 (LIVE, 2026-06-17)** — 서버 positions를 공유 진실로: 비파괴적 push(acct_no 기준, 내 포지션만 갱신), 닫으면 서버삭제, 3초 adopt + 포커스 시 즉시. 두 브라우저 검증 후 trading.html 적용. (acct_no로 매칭 = cust_id drift 안전)
  - 베팅 보기·캐시아웃·정산은 모든 기기에서 됨 ✅ (positions.meta + claim 잠금)
  - 단, 한 기기서 정산/캐시아웃하면 **다른 기기 잔액은 새로고침/재로그인해야 갱신**됨
  - 목표: 잔액도 서버에서 실시간으로 따라오게 (현재는 로그인 시 서버 잔액 채택)
  - 같은 패턴(meta+claim)을 FX/크립토 포지션에도 확장 가능

## 🟡 중간
- [~] **출금 2FA — 크립토 완료 ✅ (진짜 TOTP, Supabase MFA, v110)**
  - TwoFAFullScreen 재작성: 등록(QR+비밀키→verify) / 검증(인증앱 코드) / prompt(설정 or 건너뛰기). 데모코드 제거. 검증: 등록+verify 성공.
  - 남음: ① **FX·스포츠 출금**에도 같은 2FA 확장(지금 크립토만). ② 지금은 "Not now"로 건너뛰기 가능 = 선택 → 출시 전 **필수**로. ③ Account → Security에 2FA 상태/해제 UI. ④ Supabase 대시보드에서 MFA(TOTP) 활성화돼 있어야 함(돼 있음, 등록 성공).
- [ ] **나머지 테이블 RLS 잠그기 (보안 패스)**: crypto_holdings, crypto_holding_ledger, crypto_stakes, ledger, agents, agent_links, agent_payouts, logs, hedges — 한꺼번에 정책 잠금 (지금 앱이 키로 쓰는 테이블들)
- [ ] **로그인 보안 강화: 저장된 비밀번호 → 패스키(passkey)로 교체**
  - 지금은 빠른 로그인(Face ID) 위해 비밀번호를 폰에 저장(base64) = 편하지만 안전성 낮음
  - 출시 전 WebAuthn 패스키 전용으로 전환 → 비번 자체를 어디에도 저장 안 함 (폰 보안칩 + Face ID)
- [ ] **인트로(랜딩) 페이지** — 로그인 페이지 열리기 전에 보여줄 소개/환영 페이지 (브랜드 소개, "시작하기"→로그인/가입)
- [ ] **SpaceX 상장 → 티커 나오면 stock-prices에 연결** (지금은 시뮬 $185)
- [ ] **나머지 테이블 RLS 잠그기**: agents, agent_links, agent_payouts, logs, hedges
- [ ] **주식 실시간 유료 전환** (Finnhub 무료 → 상업화 시 약관상 유료 필요할 수 있음)
- [ ] **MetaMask 출금 실전 테스트**
- [ ] 백오피스 PIN → 다단계 관리자 권한 / 에이전트 실제 Auth / 직원 권한

## 🟢 낮음 (런칭 마무리)
- [ ] 프롭베팅 정산
- [ ] 제대로 된 PWA (오프라인 지원)
- [ ] 런칭 청소: 버전 스탬프 제거, 데모 잔고/계정 정리 (예: $5.3M 테스트 계정)
- [ ] 법률/라이선스 (소유자 트랙)

## ✅ 완료 (참고)
- 실시간 데이터: 크립토(Binance+Coinbase), FX(Polygon), 주식(Finnhub), 스포츠(Odds API)
- FX 실제 캔들 차트 / 크립토 실제 차트
- FX 아비트라지 방어 (앵커체결 + 라스트룩 + fast-market + 최소 스프레드)
- RLS 보안 전체 잠금 + 관리자 인증
- 로그인 필수 / 정체성 자동복구
- 매니저 Realtime / 딜링데스크 실제가
- Supabase Pro (안정+백업)
- FX 거래손익 서버 트리거 + 조작 감지 모니터
- 신규 계좌 시작 잔고 (스포츠 $500 / 크립토 $100 / FX $100)
- 앱 아이콘 (고객=Alpexa 글자, 백오피스=네온 점3, 파비콘=파란 점3)
- 구독료 월 알림 (캘린더, 매월 8일, ~$133/월)
- 싸인아웃 정상화(3개 앱) + 로그인 유지(자동 통과) + Face ID 빠른 로그인
- 테스트 잔고 시드(오너 $1M / 기본 $10k) 제거 → 로그인이 서버 잔액을 기준으로 적용
- 정체성 "가장 오래된 계좌로 고정"(중복 가입 시 계좌 흔들림 방지)
- 백오피스 무결성 시작금 = 실제 $700 (스포츠 500 + 크립토 100 + FX 100)
- 진짜 비밀번호 찾기(재설정 메일) + 버전 딱지 고객 숨김(?ver=1로만 보임)
- 멀티기기 스포츠 베팅: 어느 기기서든 보기·캐시아웃·정산 (서버 positions.meta + 이중정산 claim 잠금)
- 스포츠: 진짜 경기만 표시(데모 제거), Recent Activity 버튼 모바일 정렬 수정
- 스포츠 라이브 베팅 1단계: 득점 자동 잠금 + 오래된 배당 잠금 + 백오피스 ON/OFF 토글
- **스포츠 서버-잔액(서버가 주인) ✅** — 입금=DB트리거 자동크레딧(승인순간, 앱꺼져도, 클로버없음), 베팅/정산=장부 델타, 앱은 잔액 안 밀고 서버값 표시. 멀티기기 일치 확인. (장부 패턴: ledger + apply_ledger 트리거 + apply_request_balance 트리거)
